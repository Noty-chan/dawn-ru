name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/pages.yml"
      - "site/**"
      - "apps/companion/**"
      - "media/tutorials/**"
      - "media/tokens/**"
      - "source/translation/**"
      - "requirements.txt"
      - "VERSION"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Verify generated data and companion
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -r requirements.txt
          python apps/companion/build_data.py
          git diff --exit-code -- apps/companion/data.js
          cd apps/companion
          npm run readiness
          npm run docs:rules
          npm test

      - name: Assemble static site
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p _site/companion _site/tutorials _site/media/tokens
          cp -R site/. _site/
          cp apps/companion/*.js _site/companion/
          cp apps/companion/*.css _site/companion/
          cp apps/companion/*.html _site/companion/
          cp apps/companion/*.webmanifest _site/companion/
          cp apps/companion/*.svg _site/companion/
          sed -i "s/__BUILD_VERSION__/${GITHUB_SHA}/g" _site/companion/sw.js _site/companion/index.html
          cp -R media/tutorials/. _site/tutorials/
          cp -R media/tokens/. _site/media/tokens/
          cp media/tutorials/00_оглавление.html _site/tutorials/index.html
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
