# Интеграция боя и инструментов ГМа — 2026-08-08

База интеграции: `origin/main` (`8e507a5`). Перенесён набор `0f07708` и вручную совмещён с 39 более новыми коммитами основной ветки.

## Готово

- Сохранены сетевой стол, права Нарратора, встречные и запрошенные броски, Черты Антагониста, часы Сцены и IndexedDB-медиа из `main`.
- Добавлены библиотека именных противников/боссов, собственные имена и токены, сохранение расстановок столкновений и быстрое развёртывание.
- Добавлены режимы ведения `auto / guided / manual`, пульт Нарратора, ручные исходы и базовый `Шаг` врагов без выдачи им базовых действий героев.
- Сведены автоматизации выбранных сборок героев, именные профили Леона и духов, полный smoke-test каталога.
- Pages теперь публикует `media/tokens`, поэтому встроенные токены доступны на собранном сайте.

## Требует следующего прохода

- Запрос пользователя этого же прохода про сетевые броски ещё не реализован полностью: уведомление «Нарратор запросил бросок» должно плавно исчезать; ответный бросок должен атомарно закрывать запрос; в интерфейсе нужна явная цепочка «Запрос → Ответ»; общий журнал бросков должен быть виден всем участникам. Свободные броски должны остаться доступными.
- После ручного сведения конфликтов нужно прогнать весь `npm test` и браузерный сценарий: создать именного врага, сохранить/развернуть столкновение, провести Ход врага (`Шаг` + профильное действие), Реакцию героя, ручной исход ГМа и новый Раунд.
- Проверить сетевой сценарий в двух клиентах (Нарратор + игрок), особенно делегирование действий и владение `pendingPrompt`.
- Если тесты зелёные, отправить интеграционный коммит fast-forward в `main`, дождаться GitHub Pages и проверить `/companion/` и `/media/tokens/leon-academy-mage.png`.

## Ключевые решения конфликтов

- Схема героя оставлена версии 6; боевые ЗД/ОД/Раны/Эффекты сохраняются при синхронизации листа.
- Новый конвейер кубиков и сетевые хуки из `main` сохранены; поддержка Эмпата встроена поверх него.
- Полная реализация Мастера Оружия из `main` сохранена вместо раннего упрощённого селекта.
- Сетевые проверки прав на управление prompt и составными действиями сохранены.
- Семейство автоматизированных атак врагов расширено именными профилями; толчок духа Вайю проходит через общий post-displacement pipeline.

Коммит интеграции должен ссылаться на этот файл в сообщении.
