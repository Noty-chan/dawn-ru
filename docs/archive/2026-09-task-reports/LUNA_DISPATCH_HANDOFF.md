# Luna: задания до следующего ревью

База: `f81ce7d`, ветка `codex/lionwing-rules-kernel`. Получена fast-forward;
полный `npm test` прошёл до запуска агентов. Новый коммит подключает
`geometry-move` к ручным пакетам. Полнота последовательных маршрутов ещё требует
ревью: вложенные планы строятся по исходной геометрии.

Запущены Luna, reasoning `max`:

- `/root/luna_execution`: один срез сохраняемого продолжения при Ране/Сопротивлении
  и цене Здоровьем. Владеет engine/execution и отдельными тестами.
  Отчёт: `LUNA_EXECUTION_RESULT.md`.
- `/root/luna_geometry_ui`: подтверждение/отмена/добавление маршрута в пакет,
  устаревание, права, понятные сообщения. Владеет lionwing-ui и отдельным тестом.
  Отчёт: `LUNA_GEOMETRY_UI_RESULT.md`.

Третий агент dice не создан: tool вернул `agent thread limit reached`.
Несмотря на объявленные четыре слота, фактически доступны только два дочерних
запуска. Работа над dice никому не назначена.

Агентам запрещены коммиты и пуш из общего дерева, изменения чужих файлов,
массовая автоматизация и повышение статусов готовности. Координатор останавливается
по просьбе пользователя; результаты пока не проверены. При следующем запуске
проверить статусы агентов, отчёты, diff, адресные тесты, затем полный npm test.
Проверить настоящую нормализацию сохранения и границы прав; после ревью коммитить
каждый принятый блок отдельно и пушить рабочую ветку. Не сливать в main.

Не трогать `source/supplements/` и посторонний `site/dead-gods/maps/`.
Прежние проценты готовности были приблизительными оценками, не результатом
измеренной приёмки. Большой список в чате смешивает уже готовое, проверки и новые
возможности; он не заменяет сверку кода и `LIONWING_COMPLETION_PLAN.md`.
