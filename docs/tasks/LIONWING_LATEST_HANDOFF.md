## Актуальная точка передачи — 2026-09-10, Technician I–III canonical slice (`codex/luna-technician-v2`)

Сверен новый EN-канон `source/editions/dawn-en-lionwing-cb2f8e67/canonical/archetypes/powerhouse.json`, стр. 70, и RU overlay в `apps/companion/edition-lionwing-ru.js`. Полные canonical sourceDigest: Technician I `79946bc3df6de994901a8519030345403e62c0fac627a76aaa8bc0ee45edda83`, II `87d635215f2088e683f229d48bc51b0f2bc34d6a88bc1dea707fcea12e4be250`, III `69e9007f8f65def64ef7640b8441616852def68765ad7a717f09587b1039ecfe`.

I: авторитетная Charge открывает clock `powerhouse.technician.stretch` до конца следующего собственного Хода; завершённое `Skirmish → Finisher` в том же Ходе даёт 1 AP. II: тот же подтверждённый combo выдаёт `ceil(Tier / 2)` Armor до начала следующего собственного Хода. III: существующий combo/ActionPlan pipeline проверяет последнее authoritative действие, списывает 1 AP и добавляет push на 3 клетки. Все три уровня имеют honest `partial` coverage; выбор/сложные ветви остаются ручным fallback. Client `combo`/Charge booleans не используются как доказательство.

Добавлены проверки правильного и неправильного порядка, лишнего/чужого Хода, истечения clock, duplicate/reload, cancel pending action и forged provenance, включая реальный scheduler dispatch. Производные `TECHNIQUE-FOUNDATION-MAP.md`, `AUTOMATION-READINESS.md`, `LIONWING-AUTOMATION-MAP.*`, registry и implementation spec обновлены; E2E certification не добавлялась. Targeted Technician test, связанные adapter/engine tests и полный `npm --prefix apps/companion test` проходят. Коммиты: `13d8d27` (код и тесты), `b7b7399` (карты и handoff); ветка отправлена в `origin/codex/luna-technician-v2`.

## Актуальная точка передачи — 2026-09-10, принят read-only аудит Luna helper

Прочитан итог отдельной задачи `Luna helper — LionWing automation ledger`.
Его архитектурные требования уже покрывает машинный реестр: 333 canonical
levels считаются отдельно от registry subrules, implementation отделён от
review и certification, пустой evidence не повышает доверие. Дополнительно
закрыта найденная разница: каждая будущая test evidence теперь обязана содержать
не только существующий путь к файлу, но и конкретный `case` или `command`.

## Актуальная точка передачи — 2026-09-10, lifecycle и numeric foundations

В `451d8e9` интегрирован нейтральный планировщик границ Scene/Round/Turn:
канонические own/any Turn start/end, приватные стабильные receipts, защита от
повторной доставки, reload/undo и необязательные решения через общий choice-flow.
Monastic Sage, Technician, Opportunist и Sniper из исходной экспериментальной
ветки не переносились; синтетический тест не повышает статус Техник.

В `be734f2` интегрирован общий числовой порядок
`replace -> multiply -> add -> floor -> cap`. Старые stat/range/roll hooks
проецируются в него; ядро повторно применяет quote к характеристикам, пулу
Атаки, урону до защиты, итоговому урону и цене Действия. Конфликтующие замены
блокируются, composer доступен ручному ядру напрямую. Consumer-адаптеры старой
ветки не переносились. Полный `npm --prefix apps/companion test` проходит.

## Актуальная точка передачи — 2026-09-10, аудит интеграции завершён

Проверенный срез находится в `main` на `6212afd`. Добавлен воспроизводимый
реестр всех 333 уровней, который раздельно хранит наличие обработчика,
канонический provenance, явное review, evidence по поверхностям и E2E
certification. Текущее распределение: 222 manual, 32 partial, 48 decision,
31 full; 327 уровней остаются `unreviewed/inherited`, 6 имеют явную запись
исправления, сертифицированных E2E уровней пока 0. Все 15 новых уровней LionWing
остаются manual/unreviewed, Servant's Call в активный реестр не попадает.

Независимое Luna-ревью исправило digest: теперь он считается по полному
каноническому payload и совпадает с provenance gate обработчиков. Полный
`npm --prefix apps/companion test` проходит. Итог, изменения игрового стола,
границы метрик и следующий порядок работ описаны в
`docs/tasks/LIONWING_AUDIT_COMPLETION_REPORT_2026-09-10.md`.

## Актуальная точка передачи — 2026-09-09, универсальная панель Техник (codex/luna-technique-control-ui)

## Сверка перед автоматизацией Will-O-Wisp — 2026-09-09

Канон LionWing: `source/editions/dawn-en-lionwing-cb2f8e67/canonical/archetypes/altruist.json`,
`Will-O-Wisp` (переименования нет), стр. 90; русский overlay —
`apps/companion/edition-lionwing-ru.js`. Полные digest уровней из проверенного
реестра: I `718c1072bf9fb744208642697c7e52e01925efc76cb61a9e04e7a5bf85789c30`,
II `fbd6b0c3eae8b30f64d4a9c59e9c15870dd290fa7590ee08968b537dba88e63f`,
III `73fc31ad0348f634cb773ccd09f4dbb3ba9f0b16eea3ced967977e838286adfb`.
I: после первой за Сцену Зарядки — добровольное создание Пламени в пределах
1 клетки; выбранный тип фиксируется, аура — клетка и смежные клетки, движение
до 4 клеток после Шага или в конце Хода, толчок на 1 при выборе клетки Пламени
или стоящего в ней персонажа целью Атаки. II: союзник, вышедший из Пламени,
может вызвать перенос рядом с новой позицией; попытка выхода врага открывает
реакцию владельцу, расход 1 Фокуса останавливает после первой клетки выхода.
III: второй отличный тип; при создании — одно Пламя с обоими свойствами или два
раздельных. Существующий код уже покрывает большую часть этих условий, но до
этого не давал выбрать начальную клетку, а автоматическая остановка врага
должна оставаться до downstream enter-триггеров.

Собран общий вертикальный срез управления любыми известными Техниками LionWing.
Панель подключена к выбранному участнику на игровом столе и к Нарраторскому
пульту. Она строит список только из канонического `edition-lionwing.js`,
показывает английский текст и `source` из LionWing отдельно от состояния
автоматизации, а русский overlay выводит как отдельный перевод. Записи
`technique-engine.js` и их notes не используются как источник текста правила.

Для каждого изученного уровня видны группа и переименования, статус
`Автоматически` / `Авто частично` / `Автоматизация выключена` / `Ручной режим`,
состояние последней квитанции и общий ручной fallback. Доступность базовых
действий вынесена в сворачиваемую сводку с причиной и стоимостью. Pending
решения Техник проходят тот же generic choice surface: варианты, цели,
вариант/режим, стоимость, срок, подтверждение и отмена. Владелец героя и
Нарратор видят только допустимые им решения; чужие private предложения игрока
скрыты. Повторный клик дедуплицируется до изменения версии Сцены, кнопки и
свёрнутые группы сохраняют состояние после перезагрузки.

Панель не содержит ветвлений по `techniqueId` и не исполняет правила сама:
авторитетными остаются адаптеры и LionWing engine. Нарратор может записать
ручное решение с заметкой и выбранными на поле целями; для частичных и
выключенных адаптеров это видимый fallback/override. В список адаптеров добавлены
технические `techniqueId`, `level` и `enabled`, чтобы поверхность могла
сопоставлять их с каноническими уровнями без чтения устаревшего реестра.

Основные файлы: `apps/companion/lionwing-technique-surface.js`, его generic
тест `apps/companion/tests/lionwing-technique-surface.mjs`, интеграция в
`lionwing-ui.js`, `scene-ui.js`, `scene-actions-ui.js` и responsive-стили
`app.css`; загрузка добавлена в `index.html` и service worker.

Коммиты: `6a0e3c8` (контракт поверхности, provenance, адаптерный metadata,
тест и загрузка), `f7aaa23` (подключение к игровым и Нарраторским панелям).
Ветка отправляется в `origin/codex/luna-technique-control-ui`.

Проверка: полный `npm --prefix apps/companion test` проходит. Ограничения:
ручная кнопка фиксирует решение через существующий общий конвейер и не
подменяет сложную механику; выбор клеток/целей для таких правил по-прежнему
делается существующими инструментами поля. Конкретные новые семейства и
непроверенные адаптеры должны добавляться поверх этой поверхности после
сверки с каноническим EN текстом.
## Актуальная точка передачи — 2026-09-10, общий контракт `information-query`

Сделан gap-аудит от свежего `origin/main` (`14f1632`): прочитаны этот handoff,
`TECHNIQUE-FOUNDATION-MAP`, readiness и requirements review, canonical EN JSON/PDF
extraction, RU overlay, `scene-query/actions/triggers/responses`, Detective/Siren и
network/private projection tests. До работы у capability `information-query` не
было общего модуля: Study жил в базовом действии и разрозненных адаптерах, а
registry notes не использовались как источник механики. Основной worktree и `main`
не изменялись.

В `apps/companion/lionwing-information-query.js` добавлен типизированный
automation-facing контракт `DAWN_LIONWING_INFORMATION_QUERY` (schema 1,
edition `dawn-en-lionwing-cb2f8e67`, contract digest
`lionwing-information-query:study:v1`). `recordStudy/study` принимает только
реально разрешённый LionWing `action.resolve` с точными `actionInstanceId` и
единственной целью, подтверждённый строкой `actor.lionwing.history`; клиентские
флаги, чужая цель, поддельный action или старая редакция не принимаются. Stable
receipts и bounded journal поддерживают duplicate delivery, reload, replay и undo.
Запись хранит владельца, цель, Turn/Round/Scene/Chapter serials, scopes, статусы,
категории, revealed fact ids и provenance.

Контракт предоставляет `availableCategories`, `confirmReveal/reveal` с ролью
Нарратора и ручным override для неоднозначного правила, `cancelReveal`,
`handout`, `studies`, `studyCount`, `studiedThisTurn`, `firstStudy`,
`revealedFact(s)`, `markedTarget`, `knownHealth/knownHealthAtMost` и
`followupStatus`. Скрытые факты, цели и actor metadata отфильтрованы в player
projection; receipts, journal, warnings, fingerprints и source digests наружу не
выдаются. SQL migration распространяет только public information state.

Перед каждым consumer зафиксированы полный canonical EN текст, reviewed RU текст,
`sourceDigest` и semantic diff. Evidence находится в том же модуле для:
`powerhouse.predator.1` (`a8cae751…`), `.2` (`88f405e…`),
`vagabond.cunning-fighter.1` (`4373bc4…`), `.3` (`42e5ed6…`),
`bulwark.absolute-bastard.1` (`91c7070…`), `altruist.battle-instructor.1`
(`186549b…`), `vagabond.dim-mak.1` (`86bc280…`) и `.2` (`d63edd4…`).
Тест сверяет эти строки непосредственно с `edition-lionwing.js` и RU overlay.

Opt-in adapters подключили безопасные части Predator I–II, Cunning Fighter I/III,
Absolute Bastard I и Battle Instructor I. Общая история Study также используется
Detective/Dim Mak I–II без переписывания уже работающих веток; всего покрыто 8
информационных уровней, каждый помечен `partial` и canonical digest. Action quote
передаёт Predator I `cost=0`, Swift, range `[Body]`, health-only categories и
Battle Instructor Swift; query surfaces дают health threshold, per-target/per-turn
counts, marked target и Cunning III follow-up availability. Полные KO-heal, движение
и derived Speed Predator, Cunning Plan/Focus и второй Study, Taunt Absolute Bastard,
ally movement Battle Instructor и другие lifecycle части оставлены частичными.

UI показывает историю целей, категории, факт и ожидающий выбор Нарратора на русском;
кнопки раскрытия/отмены вызывают тот же authority contract. Map/readiness обновлены:
`information-query` теперь `ready`, версия foundation map — 9. Прямой следующий
consumer для Frost Veiler, Assassin, Poacher, Empath, Grim Ascendant, Will-O-Wisp и
Spellcrafter не добавлялся: их нужные уровни требуют отдельной canonical сверки и,
для отмеченных stale registry rows Frost Veiler II, Grim Ascendant II и Empath III,
сначала исправления semantic diff. Новый query уже прямо пригоден для их будущих
known-health, marked-target и private fact checks.

Коммиты: `096573b` (контракт, engine, adapters), `055f40e` (UI, SQL projection),
`a272319` (targeted test, package gate, maps/readiness), `e9e97fe` (reference
spec), `7e11790` и `ad3a872` (handoff/validation), `f0c38c6` (скобки в SQL
projection для совместимости PostgreSQL/PGlite). Ветка
`codex/luna-information-query` отправлена в
`origin/codex/luna-information-query` (`f0c38c62f3cb9faed7b131d85b350266a26e0917`),
worktree чистый. Полный `npm test` проходит; отдельно
`node tests/lionwing-projection-sql.mjs` проходит на локальном
`@electric-sql/pglite` 0.3.16 и проверяет replay миграции, pending responses,
скрытые Duel references, private history и public entity filtering.

## Актуальная точка передачи — 2026-09-09, Master at Arms III и общий выбор центра области

В ветке `codex/luna-master-three-area-ui` поверх контракта Armament добавлена полная
серверная обработка `vagabond.master-at-arms.3`: только Talent Finisher читает
авторитетное `ruleModes` состояние и его canonical source digest. Клинок проверяет
сегментный путь до 2 клеток и выводит цели пересечения; Древко строит линию из 2
клеток, проверяет Spike Подброшенной цели и создаёт typed Difficult Terrain на срок
Сцены; Цепь строит 1×1 от смежного центра, расширяет область по проверенным Critical
(1×1 → 3×3 → 5×5) и добавляет урон. Dispatch повторно сверяет digest, геометрию,
цели, границы, удалённые клетки и большие тела; клиентские mode/area/target claims
не принимаются.

Общий UI выбора центра области теперь оставляет Bombardier I–III и другие area
Техники в одном потоке: наведение подсвечивает форму, клик фиксирует центр,
появляются подтверждение и отмена, а `techniquePreview` повторно строит plan.
Master III получил тот же выбор клетки движения/центра. Добавлены тесты Armament
веток, path crossing, line/terrain, 0/1/2 Critical, clipping, large bodies,
forged/stale dispatch и отмену; targeted test включён в `npm test`.

Коммиты: `65ae422` (runtime/UI), `fe53c76` (тесты, карты и reference docs).
Ветка отправлена в `origin/codex/luna-master-three-area-ui`. Полный
`npm --prefix apps/companion test` проходит: 391 уникальное правило, 107 семейств,
321 уровень и 5671 парная сборка каталога.# LionWing — последняя передача, 2026-09-06

## Актуальная точка передачи — 2026-09-09, Siren III (codex/luna-siren-three)

В ветке `codex/luna-siren-three` полностью подключена `disruptor.siren.3` по
каноническому digest `231b63c69615f78497650a97d3a5225a98f298a882d4adb2eedaebdff16c5b7e`.
После реально разрешённого Mind или Spirit Finisher с единственной живой целью
opt-in адаптер открывает в UI явный отменяемый выбор. Ядро само вычисляет всех
живых вражеских персонажей с `negative.испуган`, а не принимает список клиента,
и последовательно ведёт каждого к цели на его Speed через проверенный
`forced-towards-group`/сегментную геометрию с препятствиями, большими телами,
частичным остановом и сохранением continuation stack при reload/replay.

После каждого фактического маршрута смежность пересчитывается по актуальным
пространствам; только реально подошедший враг наносит цели `[Tier]` урона с
собственным provenance. Нокауты, исчезновение Испуган и устаревшие цели
безопасно пропускаются, повтор урона дедуплицируется. Подделанные target,
actionInstanceId, Tier и списки отвергаются, старые издания изолированы.
Добавлены targeted `lionwing-siren-three.mjs`, общий `test:families`, generated
foundation/readiness/reference maps и ручной Narrator path остаётся доступным.


## Актуальная точка передачи — 2026-09-09, общая геометрия областей и Бомбардир

В ветке `codex/luna-area-automation` добавлен общий JSON-контракт
`DAWN_LIONWING_GEOMETRY_RUNTIME.areaPlan/areaPrepare/areaPreview/revalidateArea`.
Он использует существующий `spatialShapeStatus`, поддерживает смежную область
с центром, квадраты 3×3 и 5×5, учитывает границы, удалённые клетки, большие тела,
команды, Исчезновение и пустые клетки. Снимок формы, центра, целей и
`emptyTargetCount` повторно сверяется при подготовке и авторитетном применении;
переданные клиентом цели/счётчик формы не принимаются.

Подключены opt-in адаптеры `ruiner.bombardier.1–3` с canonical digest. Духовное
Завершение получает центр и цели из проверяемого плана: I — центр плюс смежные
цели в дальности 4, II — зона 3×3 при расходе минимум 2 Фокуса в дальности 5
и авторитетный бонус за пустые клетки с cap `Tier+2`, III — зона 5×5 при
расходе минимум 4 Фокуса в дальности 6. Обычное Завершение без opt-in не
изменяется. Полноценный выбор центра в панели пока не добавлен: runtime
принимает `areaCenter`/`techniqueAnchor`, а старый вызов с единственной целью
использует её клетку как совместимое значение по умолчанию.
## Актуальная точка передачи — 2026-09-09, Detective / attached markers

В ветке `codex/luna-weak-point-markers` добавлен общий контракт прикреплённых
маркерных записей: `hostActorId`/`sourceActorId`, относительная клетка, политика
потери источника, сериализация и безопасная публичная проекция. Маркер не может
самостоятельно переместиться в клетку, не соответствующую актуальному носителю;
вторая Слабая точка в той же клетке отклоняется, а удаление носителя очищает
связанные записи. `actor.enter` проверяет фактическую клетку и принимает
стабильный `segmentId`, что позволяет дедуплицировать вход после replay/reload.

Для канонического LionWing `vagabond.dim-mak` добавлены opt-in action quotes:
повторное Изучение той же цели становится Быстрым, а третье Изучение бесплатно.
Третий результат автоматически замедляет всех живых врагов с действующим
Помечен; старый miss/evasion-путь сохранён только для старых сцен. Вход владельца
в Слабую точку открывает общий choice continuation; подтверждённый путь удаляет
точку и наносит фиксированный `[Mind/2]` Джеб с неизменяемой целью. В LionWing
движение также журналирует endpoint `actor.enter`.

Покрытие Detective III пока `partial`: в текущем continuation-контракте нет
готового общего выбора teleport к маркеру и бесплатного Mind Finisher после
трёх удалений; эти части оставлены ручным путём Нарратора. Старые издания,
supplements и карты не изменялись.
## Актуальная точка передачи — 2026-09-09, общий контракт экипировки и цепочек (codex/luna-equipment-chains)

Ветка добавляет типизированный сериализуемый контракт `ruleModeContract` и
состояние `ruleModes` для взаимоисключающего экипированного Armament. Ядро
возвращает условия геометрии/целей, Swift/range и typed `postOperations`, а
квитанции считают каждую смену один раз за текущий Ход и переживают reload/replay.
Master at Arms I–II подключён для Клинка, Древка и Цепи; второй Armament за Ход
даёт 1 ОД и Ускорен. UI использует тот же quote и показывает причину недоступного
варианта. Для Master at Arms III оставлен нейтральный hook будущего Talent
Finisher; area-ветка не дублируется.

Siren I теперь после проверенного Study предлагает потратить 1 Фокус на
изученную цель. Siren II после первого Fear за Ход предлагает проверяемое
принудительное приближение до 3 клеток; Daze и +1 Focus доступны только после
фактического входа в смежность и принятого Daze. Все новые события сохраняют
canonical `sourceDigest`, а ручные действия Нарратора остаются доступными.

Добавлен targeted `npm run test:equipment-chains`; targeted QA и существующие
регрессионные тесты проходят. Полный `npm test` доходит до generated-doc gate и
останавливается на четырёх уже устаревших reference documents; generated docs,
supplements и maps в этой ветке не менялись. Ветка содержит порционные коммиты
для последующего cherry-pick/merge.

## Актуальная точка передачи — 2026-09-09, закрытие швов ядра и UI-приёмка

Ветка `codex/lionwing-rules-kernel` доведена до общего ручного runtime для
оставшихся сложных семейств. Отдельными проверенными коммитами добавлены:

- `e58507e` — автоматические `aura.enter`/`aura.exit` после движения, в том числе
  для больших тел и перемещения источника ауры;
- `325fcf6` — видимый стек вложенного продолжения Атака → Реакция → урон →
  Рана → Сопротивление;
- `2fdbeb8` — чистые планы placement, teleport и displacement;
- `78a4fba` — типизированные Banish, Vanish и Compound с reload/replay/undo;
- `310eb08` — применение пространственных планов общим reducer: placement и
  teleport без ложного следа движения, displacement через сегментный курсор.

Полный `npm --prefix apps/companion test` после этих блоков прошёл: 391 уникальное
правило, 107 семейств, 321 уровень и 5671 парная сборка каталога. В отдельной
браузерной проверке новый стол открыл сохранённое столкновение; форма массовки
проверена на viewport 826×900, удаление выбранного участника вынесено из
свёрнутых настроек, а удаление сущности и пространства сделано явной текстовой
кнопкой. Консоль браузера: 0 ошибок, 0 предупреждений. Для обновлённого пакета
поднят cache revision service worker.

После этого среза ручной фундамент можно считать примерно на 95% готовым.
Ближайшие работы:

1. Пройти полный живой сценарий кликами Атака → Реакции → урон →
   Рана/Сопротивление → сохранение → откат на чистом профиле Нарратора. Его
   механика и reload/replay уже покрыты VM- и интеграционными тестами.
2. Дать новым placement/teleport/displacement отдельные удобные кнопки и preview
   в Пульте; API и авторитетное применение уже готовы.
3. Провести приёмку двумя реальными клиентами Supabase и отдельно применить
   `202609080001_harden_lionwing_private_state.sql` к общей базе.
4. Продолжать адаптеры Техник пакетами по готовым потребителям. Реестр описывает
   333/333 требований, но сейчас автоматизировано 34 уровня LionWing; частично
   английский раздел Даров остаётся отдельной контентной задачей.

Старую редакцию и `source/supplements/` не менять. Посторонний
`site/dead-gods/maps/` остаётся untracked.
## Актуальная точка передачи — 2026-09-09, начало автоматизации окончаний Хода

`powerhouse.monastic-sage.1` теперь покрыт полностью: кроме +2 Брони при
Усилении, персонаж автоматически получает 2 Уклонения в конце собственного
Хода, если в этот момент Ускорен. Общий boundary-контекст передаёт проверенный
список действующих эффектов до их истечения; операция применяется самим ядром.
Это первый небольшой блок дальнейшей автоматизации окончаний Хода.

## Актуальная точка передачи — 2026-09-09, числовые цепочки (codex/luna-numeric-bonuses)

Добавлены частичные, opt-in адаптеры с canonical digest для Monastic Warrior I
(+2 Armor пока Strengthened), Acrobat I (Advantage Skirmish по клеткам текущего
Jump, максимум Talent), Eradicator III (Advantage только явно помеченному Rapid
Fire Cast), Bombardier II (Advantage Spirit Finisher за пустые клетки с лимитом
Tier+2), Ritualist II (+Tension Advantage и +3 range первому Spirit Finisher
в Spell Circle за Turn) и Enchained III (Tier×2 только при явно переданном
движении Cast этой цепочки). Для chain-зависимых случаев ядро использует
проверяемый контекст, а не один actionId; Jump provenance сохраняется в журнале
и читается после reload. Push 4 Enchained и Monastic Warrior I Evasion в конце
Turn сознательно не автоматизированы: безопасного сквозного контракта пока нет.
Тесты положительных/отрицательных условий, наложений, reload/replay и полный
`npm test` проходят. Ветка отправлена в origin для последующего fast-forward/merge.
## Актуальная точка передачи — 2026-09-09, action modifiers

В ветке `codex/luna-action-modifiers` добавлен общий read-only контракт
`DAWN_LIONWING_ADAPTERS.actionQuote(actor, context)`. Он возвращает фактические
`cost`, `resource`, `swift`, `range`, строго перечисленные `ignoreRequirements`,
`modifierIds`, источники с canonical `sourceDigest` и `coverage`. Цена проходит
через единый порядок замен; разные одновременные замены дают явный конфликт.
Адаптеры opt-in и не меняют Сцену: единственным писателем остаётся LionWing
engine. `actionStatus`, `availableActions`, `prepare` и `dispatch` повторяют один
и тот же quote перед показом, оплатой и записью `usedActions`/history; клиентская
подмена цены не принимается.

Подключены безопасные части уровней: `powerhouse.martial-artist.2` (первая
Стычка Хода Swift), `altruist.fog-walker.3` и `altruist.bardic-savant.2`
(Передышки Swift), `vagabond.drunkard.3` и `ruiner.creation-ascetic.2`
(первая Передышка Хода Swift), `ruiner.creation-ascetic.2` (Зарядка за 1),
`altruist.artist.2` и `altruist.alchemist.2` (первое Взаимодействие Хода за 0
и Swift), а также `vagabond.assassin.1` (первый Hide после проверенного
Развёртывания с нулевой ценой и исключениями только для края поля и
начального Исчезновения). `vagabond.weaponsmith.2` активируется только при
доверенном `actor.lionwing.formSwapTurnSerial`; пока lifecycle смены Формы не
пишет это поле, правило остаётся неактивным. `powerhouse.improvisational-fighter.2`
не угадывает, что Interact не приведёт к Атаке, и ждёт подключённого доверенного
semantic ActionPlan.

Добавлен поведенческий тест `tests/lionwing-action-modifiers.mjs`: stacking
Swift/cost, first-per-Turn, opt-in, конфликт/повтор, reload-safe history,
Assassin requirement exceptions, trusted-state gating и forged cost через
prepare/dispatch. `npm run test:families` проходит. Полный `npm test` нужно
повторить после интеграции этой ветки в общий рабочий поток.
## Актуальная точка передачи — 2026-09-09, типизированные события после разрешения

В ветке `codex/luna-after-events` добавлен общий read-only вход
`DAWN_LIONWING_ADAPTERS.afterEvent(actor, event, context)`. Он принимает только
завершённые события Сцены и возвращает сериализуемые операции и явные choices;
движок по-прежнему единственный писатель состояния. Для событий добавлена
устойчивая приватная квитанция `lionwing.afterEventReceipts`, поэтому повтор
того же события после JSON reload/replay не выдаёт награду повторно. Квитанции
исключаются из публичной проекции и SQL-проекции.

Подключены opt-in адаптеры с canonical digest:

- `bulwark.rising-challenger.1` — после успешного Clash +1 Focus; перемещение
  на Tier клеток остаётся отдельным выбором (partial, выбор клетки передаётся
  через continuation);
- `powerhouse.berserker.3` — первый фактически нанесённый урон за устойчивый
  экземпляр собственного Хода даёт +1 Focus (partial; Charge без Cost оставлен
  до готовности отдельного окна/выбора атаки);
- `powerhouse.intimidator.3` — после KO врага источником этого персонажа +2
  Focus и +1 AP (partial; Fodder Zone не включена без однозначного `Impose`);
- `disruptor.siren.2` — после Fear отдельный выбор forced movement до 3 клеток
  к владельцу, затем отдельный выбор Daze и +1 Focus при соседстве (partial);
- `disruptor.chemist.2` — после Weaken отдельный запрос текущего Health,
  приватный health-check и KO/+2 Focus при пороге `Health <= Mind` (partial).

`altruist.gourmand.3` намеренно не подключён: в текущем runtime нет устойчивых
идентификаторов Meal и Bond. Полный автоматический Berserker Charge и
Intimidator Fodder Zone также остаются ручными. Поведение проверяет opt-in,
неверный источник, ограничения собственного Хода, отмену выбора, reload/replay
и отсутствие повторной награды.

## Актуальная точка передачи — 2026-09-09, простые части всех Техник

После отдельной сверки всех `canonical/archetypes/*.json` набор нового ядра вырос
до 34 адаптеров уровней. В этом проходе добавлена 21 безопасно отделимая часть:

- остальные безусловные бонусы к Cast, Skirmish и всем Attack: Martial Artist III,
  Flame Heart III, Sellsword's Call I и Knife Juggler II;
- условные, но полностью вычислимые бонусы: Lancer I по расстоянию, Street Fighter
  II по числу активных Эффектов ошеломлённой цели, Flame Heart II в Порче,
  Assassin II из Исчезновения;
- Giant Frame II к максимуму Здоровья и минимум Скорости 3 из Iron Bodied I;
- старт Сцены/Хода: Focus у Absolute Bastard I и Siren I, стартовый Focus
  Spellcrafter II, Vigilance Stalwart Sentry II, Focus и лечение соседнего союзника
  у Empath III;
- начальные специальные ресурсы: Compassion, Bullets, Weapons, Material, Tenacity
  и Innovation. Они используют общий ресурсный контракт, включая замену Focus/AP,
  запрет недопустимых источников получения и обновление Tenacity по Раундам.

Общий lifecycle теперь отдаёт типизированные операции на начало Сцены, Раунда и
Хода; выбор автоматизаций Нарратора сохраняется при сбросе Сцены. Максимум
Здоровья считается динамически и одинаково используется лечением, Ранами,
Сопротивлением и двумя представлениями листа. Пулы Атак получают только
семантический контекст: ID действия, Атрибут, расстояние, активные Эффекты,
источник Taunted и теги подключённой Техники.

Не переносить в «простые числа» оставшиеся зависимые случаи. Modified Meister I
требует вместе со стартовым Heat реализовать взрыв при 6; Replicator III зависит
от адресных Echo; Braggart — от размера и заполнения Pride; Inner World — от
сохраняемого пространства; Do No Harm и ряд комбо требуют узнаваемого экземпляра
модификатора. Их ядро частично умеет выразить, но готового сквозного адаптера пока
нет. Каноническое правило округления — вверх; автоматическая выборка помощника,
предложившая `floor`, была отвергнута после сверки `lionwing.core.general.rounding`.

## Актуальная точка передачи — 2026-09-09, первые массовые адаптеры

Поверх завершённого ручного ядра начата массовая автоматизация Техник. Добавлен
общий read-only контракт числовых вкладов адаптеров: несколько включённых правил
могут независимо дать Преимущество к конкретному броску или прибавку к параметру,
а ядро суммирует их и остаётся единственным местом, которое меняет Сцену.
Переключатели Нарратора уже существовали и используются без отдельного нового UI.

Добавлено 11 адаптеров уровней:

- полностью: Iron Bodied II (`+[Body/2] Armor`), Rising Challenger III
  (`+2 Advantage` и `Spirit +2` только в Clash), Absolute Bastard III
  (`+[Tier/2] Advantage` только против цели, спровоцированной этим персонажем);
- честно как частичное покрытие пассивной фразы: `+1 Advantage` к Cast у
  Chronomancer II, Feral Arcanist III и Frost Veiler II;
- честно как частичное покрытие пассивной фразы: `+1 Advantage` к Skirmish у
  Grappler II, Bloodletter II, Constrictor III, Gunslinger II и Skirmisher III.

Для каждой записи закреплён digest актуального LionWing-источника. Частичные
адаптеры явно возвращают `coverage: partial`: остальные фразы этих уровней ещё не
считаются автоматизированными. Бонусы проходят через настоящее приготовление
Атаки, проверку пула при dispatch, урон с Бронёй и разрешение Clash; сохранены
opt-in, JSON reload, replay и адресность источника Taunted. Все тесты companion
проходят, новый поведенческий тест включён в `test:families`.

Следующие адаптеры стоит брать не по порядку строк Реестра, а группами на готовых
потребителях: сначала остальные простые числовые модификаторы, затем триггеры с
одним Эффектом/ресурсом, после них правила с выбором, перемещением и собственным
счётчиком. Реестр уточнять в момент создания конкретного адаптера; странный или
неполный текст обязательно сверять с canonical JSON/PDF, а не превращать в
догадку кода. Новое семейство для этого пакета не потребовалось.

## Актуальная точка передачи — 2026-09-08

Рабочая ветка `codex/lionwing-rules-kernel` отправлена до `6c686f6`. Все ранее
запущенные агенты завершили работу; незакоммиченного WIP LionWing нет. Посторонний
`site/dead-gods/maps/` остаётся untracked и не затрагивался. Main, production и
общая Supabase в этом цикле не обновлялись.

Последними отдельными блоками добавлены и проверены:

- сохраняемый реестр сущностей и его восстановление из JSON (`2761b88`);
- чистый мост ActionPlan → execution descriptor без ложного утверждения об оплате
  (`816bc75`);
- просмотр реестра сущностей Нарратором (`2b85d97`);
- постоянные кубы, их операции, журнал и подключение к Scene runtime (`2d7db79`);
- создание и удаление сущностей в Пульте Нарратора, загрузка всех новых runtime-
  модулей со страницы и из service worker, а также сквозной тест семейств
  (`17ea097`);
- единый ActionPlan для ручной Атаки и typed `entity.remove` с атомарной очисткой
  зависимостей (`6c686f6`).

`npm --prefix apps/companion test` проходит полностью. Сквозной тест проводит
ActionPlan, сохраняемый бросок, Атаку/Реакцию, два варианта урона,
Рану/Сопротивление, источник Эффекта, пороговый счётчик, ауру, сегментное движение,
JSON reload и undo/replay сущности. Отдельный UI-тест проверяет отмену и
подтверждение создания/удаления, сохранение и запрет игроку менять реестр.
Реальный браузер загрузил новый стол без ошибок и предупреждений консоли; панель
«Сущности» Нарратора и её empty state отрисовались. Полный create/remove в браузере
не прокликивался, потому что в пустой локальной Сцене не было владельца и backing;
эта мутация проверена поведенческим VM-тестом.

Следующие ограничения не считать завершёнными:

1. Дать Нарратору и владельцу удобные элементы для адресного reroll/change/remove/
   lock постоянных кубов. Существующий интерфейс броска работает, runtime готов.
2. Завершить прерывания и возобновление вложенных последствий на границах Реакции,
   Раны и Сопротивления во всех путях ручного UI.
3. Закрыть placement/teleport/displacement, вход/выход из ауры и специальные случаи
   Banish, Vanish и Compound.
4. Провести живую приёмку с двумя клиентами Supabase после нового состояния
   семейств. Миграция `202609080001_harden_lionwing_private_state.sql` локально
   проверена в PGlite, но к общей базе не применена.
5. После стабилизации потребителей начать массовые адаптеры Техник. Реестр содержит
   333/333 классифицированных требований, но это ещё не 333 автоматизированные
   Техники. Частично английский раздел Даров остаётся отдельной контентной задачей.

При продолжении сначала сделать пункты 1–3 небольшими отдельными коммитами с
поведенческими тестами, затем повторить полный реальный браузерный сценарий
Атака → Реакции → урон → Раны/Сопротивление → сохранение → откат. Приоритет
остаётся у работоспособности и удобства стола; проверки доверия не должны ломать
обычный игровой путь.

### Приёмка `6c686f6`

Ручная Атака всегда создаёт ActionPlan, повторно сверяет descriptor перед оплатой,
платит ровно один раз и переносит plan ID через Реакции, разрешение урона и выбор
Сопротивления. Проверены устаревший план, подмена цены, преждевременное разрешение,
повтор события, reload и continuation.

Удаление сущности теперь проходит через общий Scene event `entity.remove`. В одной
операции очищаются реальные ссылки аур, подписок, choices, deferred/paused chains,
pending action/prompt, очередей и actor runtime; защищённая либо неразрешённая
зависимость откатывает всю операцию. UI использует тот же event и сохраняет полный
undo snapshot. Полный `npm --prefix apps/companion test` прошёл. Реальная страница
загрузила engine, ActionPlan и entities без пропавших ресурсов; консоль браузера:
0 ошибок, 0 предупреждений. Полный сценарий с кликами и два клиента Supabase ещё
не пройдены.

## Постоянный продуктовый приоритет, 2026-09-08

Пользователь уточнил порядок решений: работоспособность опубликованного стола и
удобный игровой путь важнее защиты от специально сконструированных действий
злоумышленника. Безопасность остаётся обязательной, когда защищает реальные
данные и авторитетность Сцены, но не должна заранее скрывать нужное игрокам
состояние, усложнять обычные действия или задерживать работающий UI. Сначала
проверять реальный игровой сценарий, затем усиливать границы доверия без его
ухудшения.

Коммит `a4a9d6a` следует этому правилу: новая SQL-миграция закрывает приватную
историю, квитанции и внутренний курсор, но сохраняет публичные сущности для
игроков через фильтр видимости. Миграция проверена локально в PGlite и ещё не
применена к общей базе.

## 2026-09-07: проверяемое движение подключено к ручному пакету

Составной ручной пакет Нарратора теперь принимает `geometry-move`: клетка выбирается
на поле, маршрут и фактическая остановка показываются до добавления, а подтверждение
добавляет движение в список без немедленного изменения Сцены. В списке видны клетка
и длина маршрута; ошибочную операцию можно убрать отдельно. Кнопка пакета явно
сообщает, что сначала проверит итог, затем применит его.

`prepare` строит недостающие планы также для вложенных операций `plan`/`batch`.
Цена, движение и остальные последствия проходят одной транзакцией. Устаревший или
занятый маршрут отклоняет весь пакет без расхода цены и частичных последствий.
Проверено сценарным тестом ручного пакета в `lionwing-geometry-commit.mjs`.

Это первый прямой потребитель семейств в общем ручном ядре. Следующий важный блок —
сохраняемая остановка между последствиями/сегментами при Ране, Сопротивлении или
Реакции. Массовые адаптеры Техник в этом пакете не добавлялись.

## Geometry: подтверждение плана в runtime

Добавлена операция `geometry-move`. `prepare` строит чистый план, dispatch повторно
проверяет версию, геометрию, автора и перемещаемого участника, затем применяет
фактический маршрут всего тела. Журнал содержит actor.move и geometry.route.commit.
Проверены обход стены, терминальная местность, повтор ID и занятая после preview
цель. Игрок по сети не может отправлять эту Нарраторскую операцию напрямую.
UI выбора/предпросмотра и сегментные окна ещё не подключены.

## History: строгий экземпляр собственного Хода

Открытый Ход имеет `activeTurnInstanceId`, который сохраняется при reload и
попадает в факты. Старое сохранение получает детерминированный ID без actorId.
Scope `ownerTurn` сопоставляет конкретный экземпляр и отклоняется вне собственного
Хода; `anyTurn` сохраняет прежний смысл любого текущего Хода.

## History: события Сцены

Факт теперь явно различает `subjectKind: actor|scene`; nullable actorId не
подменяется владельцем или целью. Команда урона без участника-источника создаёт
attempt/damage/healthLoss с владельцем `scene` и остаётся доступна по targetId.
Старые факты без subjectKind читаются прежним способом.

## Execution/history: экземпляры действий

К совместимому `actionId` добавлены явные `actionDefinitionId` и
`actionInstanceId`. Два применения одной Передышки теперь различаются в фактах;
scope action умеет выбрать конкретный экземпляр. Происхождение сохраняется через
ожидающую Атаку, Эффекты и нормализацию сохранения. Старые факты без новых полей
остаются читаемыми через прежний actionId/rootActionId.

## Этап 0: терминальная геометрия

Исправлен известный пробел плана `ef0fa2b`: маршрут сохраняет `terminal` и
`stopReason`, при входе в Трудную местность остаток равен нулю. Линейный маршрут
за местность записывает фактическую остановку и `partial`, а не недостигнутую цель.
Исключение `ignoreTerrain` проверено отдельно. Это завершает подтверждённые пункты
1–3 этапа 0; вопросы ревью из того же этапа проверять по мере подключения runtime.

## Основной план дальнейшей реализации

Следующему исполнителю начать с [LIONWING_COMPLETION_PLAN.md](LIONWING_COMPLETION_PLAN.md).
Это подробный порядок завершения десяти семейств, сквозной приёмки и перехода
к адаптерам, с решениями, примерами контрактов и заданиями для Sol/Terra.
Исходный срез `ef0fa2b`; этап 0 перечисляет конкретные оставшиеся ошибки маршрута.
Этот документ уточняет исторические планы ниже. Раздел о неполном браузерном
тесте в предыдущем итоговом блоке относится к личной проверке прежнего координатора:
полученный `4b62b81` отдельно документирует полную локальную проверку источников
другим агентом; сетевой приёмкой двух клиентов она не является.

## Итог синхронизации и закрытия хвостов — 2026-09-07

Получены `feb5c0d` (каноническая остановка движения в Трудной местности,
иммунитет связанной области при старте внутри неё) и `4b62b81` (нормализация
и сохранение независимых источников Эффектов). Дублирующий WIP ядра отброшен.
Из работы Sol принят body-aware планировщик: один детерминированный поиск
достижимости, проверка всех клеток тела и их рёбер, обход стены подходящим путём,
остановка при Трудной местности под любой клеткой тела. Полный `npm test` проходит.

Принят `3ff0cef`: явные источники Эффектов в Пульте Нарратора, адресное снятие,
подавление и восстановление, защита неснимаемых источников и тест HTML-отрисовки.
Браузер показал панель и источник на выбранном герое; полный цикл новых трёх
кнопок после последней правки не пройден. Геометрические планы остаются read-only:
следующий runtime-блок должен подключить preview/confirm/cancel и сегменты к UI.
Подавление аур, составных участников, Исчезновения и Изгнания ещё не готово.

## 2026-09-07: принята браузерная проверка источников и исправлено сохранение

После `feb5c0d` исправлена нормализация `sceneCore`: подавленный Эффект сохраняет
источники, их ID, отдельные сроки, владельца границы, защиту снятия и подавление.
Сохраняются источники Сцены и независимые источники после удаления их автора.
Отсутствующие поля старого источника не подменяются выдуманным сроком/серийным
номером: остаётся прежний механизм восстановления срока. Старая редакция сохраняет
прежнюю нормализацию. Новый тест включён в `test:families`.

Sol воспроизвёл ошибку на локальном столе в реальном браузере, затем после правки
прошёл подавление → перезагрузка → возврат → откат; также снятие точного источника
→ откат. Старший проверил diff и добавил проверку исполнения после нормализации:
конец Хода удаляет только истекающий подавленный источник, второй остаётся и
снимается по своему ID. Это локальная браузерная приёмка, не проверка двух клиентов.
Полный `npm --prefix apps/companion test` после обеих порций прошёл.

Следующая ограниченная порция: подключение geometry-планов к существующим командам
и UI с сохранением/откатом; затем ауры. Не считать все десять семейств завершёнными.
Реестр обрабатывается преимущественно вместе с конкретными адаптерами. Main и
production не обновлялись. В Windows при CRLF checkout исходный `docs:rules --check`
может ругаться только на окончания строк: `npm --prefix apps/companion run docs:rules`
устранил это без содержательных изменений справочников; не менять их digest вручную.

## 2026-09-07: исправлена трудная местность

Получена ветка до `3ff0cef`: четыре коммита после `762f6a6`, около 230 добавленных
строк. Продолжение ограничено двумя небольшими блоками с двумя Sol и старшим ревью:
местность и фактическое сохранение источников Эффектов.

Вход в трудную местность теперь завершает движение и обнуляет Скорость до конца
текущего Хода; удвоенной стоимости больше нет. Прыжок игнорирует её, сохраняя
проверку препятствий. Проверены принудительное движение, клетки противника в
кинематографическом бою, обход участка, клетки большого тела, сериализация,
остаток Шага и конец Хода. Геометрический план сообщает фактическую остановку.
Основание: `lionwing.core.terrain.difficult`, правило кинематографического боя и текст
Прыжка в каноническом LionWing JSON. Полный npm test прошёл; отдельная проверка
окончания исключения местности перед Прорывом добавлена при старшем ревью.

Границы: связанные клетки фиксируются на начало Хода; динамическое расширение
участка в этот Ход ещё не поддержано. Поиск обхода всем большим телом и подключение
geometry-планов к UI остаются отдельным следующим блоком. Браузерная приёмка
местности в этом пакете не заявляется. Реестр уточняется при создании адаптеров.

При браузерной проверке источников найдено: `sceneCore` теряет подавленные
effectStates и новые поля источников. Исправление и повторная приёмка — следующий
отдельный коммит; см. более новую запись выше, если она уже появилась.

## Готовый промпт продолжения — 2026-09-07

Последний UI-пакет принят по тесту реальной отрисовки в VM и синтаксису:
источники показываются по целям (без целей — у управляемого участника),
снятие/подавление/возврат адресуют точный источник. Полный браузерный цикл
новых кнопок **не завершён**; проверить первым при продолжении.

> Продолжи базу десяти семейств LionWing в Noty-chan/dawn-ru, ветка
> `codex/lionwing-rules-kernel`. Сначала проверь git status, получи GitHub-коммиты
> и безопасно объедини изменения без потери локальной работы. Не трогай
> `source/supplements/` и посторонний `site/dead-gods/maps/`. Main и production
> не обновляй: прошлое разрешение выпуска относилось только к прежнему релизу.
>
> Прочитай CODEX.md по месту, этот файл, LIONWING_FAMILY_IMPLEMENTATION.md,
> LIONWING_FAMILY_AGENT_PLAYBOOK.md и LIONWING_REMAINING_FAMILY_CONTRACTS.md.
> Исторические разделы могут устареть: проверяй фактический код. База всех десяти
> семейств ещё НЕ завершена; 333/333 строк реестра не означают 333 адаптера.
>
> Уже отправлены: 639cbd5 — честные открытые вопросы реестра; 485adf3 — первые
> геометрические планы; 4d8c93f — независимые источники Эффектов, подавление,
> защита снятия и раздельное истечение; 1cc24c9 — Страх после выведения источника.
> Получены чужие f095596 и 762f6a6 с уточнениями реестра и переводом общих правил.
>
> Сначала проверь результат последних двух блоков, которые на момент написания
> этого промпта ещё проходят приёмку: UI источников Эффектов и геометрия всего тела
> с исправлением трудной местности. Не повторяй работу, если они уже закоммичены.
> Канон `source/editions/dawn-en-lionwing-cb2f8e67/canonical/core-rules.json`:
> вход в Difficult Terrain немедленно заканчивает движение и обнуляет Скорость
> до конца Хода; начавший Ход внутри игнорирует связанные участки до конца Хода.
> Старая стоимость 2 клетки неверна. Проверить обычное/принудительное движение,
> игнорирование местности, сохранение, границу Хода, весь footprint и обходной путь.
>
> Далее двигайся небольшими работающими блоками: подключение geometry-планов
> к командам/UI; эффекты-ауры и жизненный цикл источников; курсор вложенных
> последствий и оплата с Сопротивлением; actions/turns; entities; модификации dice
> и границы history. Уточни порядок по зависимостям, не строй отдельные декларативные
> API без потребителей. Счётчики, наложение и обработка Эффектов обязательны.
> Техники пока не автоматизируй массово: реестр уточняй перед конкретным адаптером.
>
> Пользователь явно разрешает Sol для реализации и аудита. Экономь его лимиты:
> максимум два ограниченных задания параллельно, fork none, модель gpt-5.6-sol,
> разные файлы, один писатель engine. Дай точные критерии и негативные тесты.
> Проверяй diff лично: прошлые агенты пропускали независимые сроки/подавление,
> брали правила старого ядра без сверки канона, делали regex-тесты по коду и
> повторяли поиск пути для каждой клетки. Требуй тесты поведения и доступный UI.
> После каждого принятого блока — отдельный коммит, push в рабочую ветку и запись
> сюда. Не откладывай всё до большого финального коммита.
>
> Минимум проверки: `npm --prefix apps/companion test`; новые тесты должны входить
> в package.json. Для UI проверь реальный браузер, перезагрузку и откат. Не выдавай
> unit-тест за проверку двух клиентов. Перед сетевым выпуском остаётся SQL-блокер:
> public_scene_projection должен исключать lionwing.history новой миграцией;
> не переписывай уже применённую миграцию. Старую редакцию сохрани.

Текущий локальный браузер проверки: playwright-cli session `lionwing-families`,
`http://127.0.0.1:8876/apps/companion/`. Это изолированная тестовая Сцена с пресетом
Бегунов Дюн и героем; на другом устройстве создать заново. Незакоммиченные файлы
нельзя считать проверенными только по этой заметке — смотри следующие записи.

## Обновление: общие правила и дальнейшая обработка Реестра

Основная содержательная обработка полного Реестра теперь выполняется вместе с
созданием адаптеров: перед реализацией конкретные строки превращаются из общего
черновика в точные исполняемые условия, а вопросы сразу сверяются с PDF.

Проверены перевод и перенос общих правил LionWing: 19 Эффектов, 17 базовых
Действий и 109 системных правил. Исправлены четыре неполные или неточные
формулировки; подробности в `LIONWING_CORE_TRANSLATION_AUDIT.md`.

## 2026-09-07: принятый фундамент источников Эффектов

Источники одного Эффекта хранят отдельные ID, сроки, владельцев границы,
защиту от ручного снятия и подавление. Истечение обрабатывает также подавленные
источники; старые сохранения используют прежние сроки как запасной вариант.
Проверка включает два источника с разными сроками и сериализацию перед окончанием
Раунда. Подавление Составного, Исчезновения и Изгнания пока явно отклоняется.
Полный npm test проходит. Браузерная приёмка и ауры ещё не выполнены.
Следующий ограниченный блок Sol: геометрия всего тела и явный UI источников.

## Быстрые исправления реестра

Следующий пакет geometry: стоимость маршрута берётся из LionWing movement,
проверяется каждое ребро большого тела без обхода заблокированного ребра,
версия/недоступность цели после JSON-загрузки. Целевые тесты проходят.
Это чистый план: runtime/UI и прерывание по сегментам ещё не подключены;
поиск обхода для большого тела и стоимость местности под всем телом ещё требуют работы.

Генератор допускает непустые вопросы в requirements-draft и выводит их отдельным
разделом. У Dragonslayer III восстановлены два нерешённых вопроса вместо общих
ответов. Berserker I/III согласованы с уже принятыми решениями SOURCE_VERIFICATION;
новой проверки PDF этот пакет не заявляет. Проверка 333 строк проходит.
Замечание по Untouchable III и остальной точечный аудит ещё актуальны.
Два агента Sol отдельно дорабатывают geometry и effects; до их приёмки эти блоки
не считать законченными. После каждого принятого блока — отдельный коммит в ветку.

## Последнее: первый блок семейств после ревью

Коммит полного реестра `fe64124` получен и включён в ветку. Структурная проверка
333/333 прошла; содержательное выборочное ревью выявило шаблонные ответы,
не разрешающие исходные вопросы. Оценка и точные следующие задания:
[LIONWING_REQUIREMENTS_REVIEW.md](LIONWING_REQUIREMENTS_REVIEW.md).

Добавлены проверенные части execution/history/costs/health/dice и чистый модуль
geometry. Все десять семейств **не завершены**. Полный npm test проходит.
Точная карта результата, ограничения, серверный блокер перед выпуском и следующие
шаги: [LIONWING_FAMILY_IMPLEMENTATION.md](LIONWING_FAMILY_IMPLEMENTATION.md).
Пользователь сообщил о новом коммите полного разбора техник с другого устройства:
после сохранения текущего блока получить изменения и оценить разбор.

## Текущий приоритет: база семейств после выпуска стола

По отдельному поручению пользователя изменения до `cbab4ea` отправлены в main.
GitHub Pages успешно выпущен. `cbab4ea` исправляет выбор участника в Пульте,
подписи/маршрутизацию быстрых действий и ширину кнопок LionWing. Полный npm test
прошёл локально и в Actions. Браузером на опубликованном столе проверены расстановка,
добавление героя, начало Хода, ручная Атака, ответ цели и применение урона.
Полный многораундовый и реальный двухклиентный сценарии не завершены.

Миграция `202609060001_lionwing_public_projection.sql` применена к проекту Dawn
`ejxzsunagpxsiwpmuovp` через SQL Editor после входа пользователя. На сервере проверены
сохранение публичных выборов, фильтрация скрытого участника, закрытые продолжения
и снимки отката; сетевые RPC batch settlement и удаления своей кампании существуют.
Старое утверждение ниже «не применена» теперь неактуально.

Пользователь изменил порядок: сначала реализовать базу десяти семейств, а разбор
оставшихся техник может подождать. Общие инструменты уточнять вместе с адаптерами.
Часть Даров на английском — отдельная задача. Для экономии использовать Sol/Terra
с ограниченными заданиями и старшим ревью. Инструкция и порядок блоков:
[LIONWING_FAMILY_AGENT_PLAYBOOK.md](LIONWING_FAMILY_AGENT_PLAYBOOK.md).
Новая разработка семейств идёт в рабочей ветке; прежний разовый выпуск стола
не означает автоматическую публикацию всех следующих блоков в main.
## Обновление: полный Реестр, 2026-09-06

Реестр требований завершён: подробно разобраны все **333 из 333** Уровней.
У каждого есть условия, цена, порядок операций, срок, наследование, расширенные
заметки реализации и сценарии проверки. `openQuestions` везде пуст: найденные
неоднозначности сразу получают решение с основанием в `resolvedRulings`.

Приоритет источников: новый LionWing PDF, затем воспроизводимо извлечённый
английский JSON, затем русский перевод и старые материалы. Повторная сборка
111 Техник из PDF не дала изменений корпуса; PDF-страница 75 дополнительно
проверена визуально. Обнаружено, что
`source/translation/pages-071-076-vagabond-techniques.md` относится к старой
редакции, хотя заголовок вводит в заблуждение; он не должен определять механику
LionWing. Ограничение записано в протоколе сверки без изменения старого digest.

Все 333 Уровня укладываются в существующие 10 семейств; новых семейств пока
не требуется. Неясный порядок больше не перекладывается на Нарратора:
Реестр задаёт устойчивое решение, а Нарратор может его явно переопределить.
Общие нормы собраны в `LIONWING_TECHNIQUE_RULINGS.md`, протокол сверки — в
`LIONWING_SOURCE_VERIFICATION.md`.

Полный `npm --prefix apps/companion test` прошёл после объединения Реестра.

## Обновление: первые адаптеры, 2026-09-06

Уточнение автора: проигрыш боевой Дуэли даёт **1 Рану**, код и карточки
обновлены (коммит `d66a7b7`). Сомнительный исходный текст следует перепроверять
по PDF: [задача сверки](LIONWING_SOURCE_VERIFICATION.md).

Добавлен первый механизм сохраняемой замены последствия и включаемый Нарратором
адаптер Берсерка II (`de25212`), затем afterEffect и Самобичеватель I. Проверены ядро, сериализованные намерения и браузерный
сценарий с перезагрузкой и откатом. Подробности и границы:
[LIONWING_AUTOMATION_PROGRESS.md](LIONWING_AUTOMATION_PROGRESS.md).

Ниже сохранена предыдущая передача; её утверждение о выборе 1/2 Ран, полном
отсутствии адаптеров и неполном Реестре теперь устарело. Следующий шаг — выбирать
небольшие группы адаптеров, которые проверяют общие контракты на реальных
потребителях, и сразу сверять подозрительный текст с PDF.

Расширение реестра по поручению пользователя: все 3 блока завершены, 33→66.
Подробности: [LIONWING_REGISTRY_EXPANSION.md](LIONWING_REGISTRY_EXPANSION.md).

## Где остановились ранее

Ветка `codex/lionwing-rules-kernel`, репозиторий Noty-chan/dawn-ru.
Накопленная реализация ручного ядра отправлена на GitHub коммитом `72288f2`.
Следующий коммит содержит этот файл и первый блок изучения требований Техник.
Пользователь попросил остановиться после его отправки из-за остатка лимитов.

## Последние решения пользователя

- Сначала описать и реализовать общие особенности срабатываний в ядре,
  затем автоматизировать сами Техники.
- Общие операции, порядок исполнения, замены последствий, прерывания,
  эффекты, счётчики, журнал, откат и сеть должны иметь общие контракты.
- После каждого существенного блока делать отдельный коммит.
- Коммиты и пуш в текущую ветку разрешены; main не сливать, приложение
  не развёртывать, миграцию к общей базе без отдельного поручения не применять.
- `source/supplements/` и посторонний `site/dead-gods/maps/` не трогать.

## Что уже отправлено в 72288f2

Ручной LionWing: Атаки/Реакции, повторный урон, Раны/Сопротивление,
сохранённые продолжения и локальный откат, пакеты, исправления Нарратора,
Эффекты, пользовательские ресурсы/часы и автоматические сбросы.
Дуэль сверена по PDF; спорные 1/2 Раны — явный выбор Нарратора.
Исправлены канонические сроки Эффектов, источники Страха/Провокации,
Регенерация составного тела и задержка Хода Подброшенного.
Леон с духами скрыт из каталогов с сохранением старых профилей для игр.
Светозар: удалены тест и встроенная расстановка.
Полный npm test прошёл; локальная SQL-проекция проверена через PGlite.
Живая двухклиентная сессия и полный обновлённый бой ещё не пройдены.

## Новый блок: требования и семейства

- `docs/tasks/LIONWING_TECHNIQUE_REQUIREMENTS.md`: сгенерированный реестр
  всех 333 Уровней; 30 имеют подробный ручной черновик, 303 ожидают разбора.
- `docs/tasks/lionwing-technique-requirements.review.json`: 30 записей
  с моментом срабатывания, условиями, ценой, порядком операций, сроком,
  наследованием уровней, сценариями проверки и открытыми вопросами.
- `docs/tasks/lionwing-rule-families.json`: 10 предварительных контрактов,
  зависимости и критерии проверки. Статусы реализации ещё не аудированы.
- `tools/content/build_lionwing_requirements.mjs`: строит реестр, проверяет
  уникальные ID, источник/digest, обязательные поля и отсутствие циклов
  в графе зависимостей. Классификатора по ключевым словам нет.

Подробно начаты Berserker, Assassin, Last Hope, Will-O-Wisp, Autophage,
Spellcrafter, Frost Veiler (`ruiner.cryomancer`), Mecha Pilot,
Temporal Sage (`altruist.chronomancer`) и Zealot — по три Уровня.
Это черновики требований, НЕ сертификация и НЕ автоматизация.
Открытые вопросы нельзя молча превращать в каноническое поведение.
Семейства: execution, history, costs, health, dice, geometry, effects,
actions, turns, entities. Их зависимости описывают порядок разработки,
а не запрещают двусторонние событийные связи.

## Что делать после возобновления

1. Проверить ветку, git status и изменения GitHub, сохранив локальную работу.
2. Прочитать `LIONWING_AUTOMATION_STRATEGY.md` и новый реестр требований.
3. Продолжать буквальный разбор оставшихся 303 Уровней небольшими блоками;
   добавлять требования вручную и фиксировать digest прочитанного источника.
   Не переносить статусы и обработчики 321 Уровня старой редакции в LionWing.
4. По реальным потребителям уточнять семейства и порядок реализации.
   Особое внимание: before/replace/apply/after, происхождение события,
   отдельное попадание vs Действие, отмена до оплаты, сохранённый выбор,
   действие вне Хода vs прерывание полного Хода, вычисляемые ауры,
   замена ресурса Здоровьем, уровень заменяет нижнее правило.
5. Перед массовой автоматизацией реализовать и проверить общие контракты
   ядра на обезличенных сценариях; затем подключать малые адаптеры.
6. После существенного блока — проверка и отдельный коммит.

## Проверки и актуальные планы

Большой план: `docs/tasks/LIONWING_MANUAL_BATTLE_QA.md` («Бегуны Дюн»).
Он обновлён под ядро, но остаётся ручным сценарием, не единым автотестом.
`apps/companion/BATTLE-CYCLE-QA.md` теперь ссылается на него.
Старая матрица `docs/TECHNIQUE-CORE-HARDENING-TRACKER.md` полезна как список
условий устойчивости; старые статусы не подтверждают LionWing.

Проверка нового блока (пройдена):
`node tools/content/build_lionwing_requirements.mjs --check`.
Пересборка: та же команда без `--check`.
Общий runtime: `npm --prefix apps/companion test` (прошёл перед 72288f2;
после него runtime не менялся).

Локальный сервер ранее работал на http://localhost:8876/; его состояние
при возобновлении проверить. SQL-миграция
`supabase/migrations/202609060001_lionwing_public_projection.sql`
подготовлена и проверена локально, но не применена к общей базе.

## Блок Detective III (ветка codex/luna-detective-three)

Сверен новый canonical `vagabond.dim-mak.3` из `source/editions/dawn-en-lionwing-cb2f8e67/canonical/archetypes/vagabond.json`; sourceDigest: `8a5ddc5d808d41166abd99dd0c207a6070ebeacf382fe4b0f3275304d7f532dd`.
Добавлена авторитетная замена обычного Шага телепортацией к собственной живой Слабой точке в пределах проверенной дальности движения. План проходит `lionwing-geometry-runtime` и повторно проверяется на commit (тело, клетка, геометрия, stale plan); замена расходует тот же Шаг и ОД, а остаток движения уменьшается по фактической дистанции.
После ровно трёх удалений принадлежащих Детективу Слабых точек одного hostActorId в собственном `activeTurnInstanceId` появляется сохранённый optional choice. Выбор клетки рядом с целью повторно валидирует runtime; затем один бесплатный Завершающий удар атрибутом Mind по исходной цели. Повтор/replay/чужая цель и отключённая автоматизация не создают бесплатное действие.
UI: в обычной панели действий появляется выбор телепортации, допустимые точки подсвечиваются на поле, доступен preview и confirm/cancel. Добавлен `tests/lionwing-detective-three.mjs`; он включён в `test:families`.
Изменён Jab I на LionWing `[Mind/2]` с округлением вверх во всех трёх путях проверки (adapter, scene event, rule response). Остаток: live browser QA требует поднятого локального сервера; основные unit tests и `node --check` пройдены.
## Gap-audit — общий контракт инвентаря и зарядов (2026-09-09, `codex/luna-inventory-resources`)

Перед реализацией проверено состояние `origin/main`. Ядро уже является единственным
писателем для `ruleResources`/`ruleClocks`: определения имеют стабильные id, owner/source,
`ruleId`, scope/lifetime, min/max/initial/current, проверяемые create/configure/set/add/reset,
конфликт замены Фокуса/ОД, пороги, replay/reload/undo и сетевую квитанцию. Те же primitives
покрывают специальные ресурсы Сострадания, Пуль, Оружия, Материала, Упорства и Инновации.
Есть отдельный registry typed entities и нормализация app-core; обычные `focus`, `ap`,
`health` остаются базовыми показателями. Плоский `actor.inventory` сейчас совместимостный
словарь положительных чисел, а `inventory.change` — слабый legacy reducer без owner,
source, max, reserve или atomic cost semantics. UI умеет вручную править rule resources и
clocks, но не показывает typed item records, charges, slots, recorded/selected values;
служебные receipts не отделены от редактируемых полей. Сетевая authority проверяет общие
ресурсы, но отдельного typed inventory projection/operation contract нет.

Минимальный gap: расширить существующие primitives отдельным typed registry под
`actor.lionwing.inventory` с декларациями stack/count/charges/slots/recorded-value/
selected-item, validated operations и cost reservation/rollback; сохранить legacy
`actor.inventory` только как read-compatible зеркало. Нужны boundary reset для Scene/Round/
Turn/Intermission/persistent, duplicate/replay/reload/undo, transfer и source removal,
visibility-aware public/private projection, а также UI статусы действий и причины блокировки.
Техники подключаются через opt-in typed adapters с canonical digest; engine не получает
technique-id switch и не принимает balance/max/ownership от клиента.

## Передача: typed inventory/resources, 2026-09-10

Срез завершён в ветке `codex/luna-inventory-resources` от свежего `origin/main`.
Основные коммиты: `4acd975` (foundation), `8ccc58d` (canonical consumers),
`90faab2` (UI и network intents). Итоговый docs-коммит с этой передачей указан
в сообщении родительской задачи после push.

### Что теперь умеет ядро

`apps/companion/lionwing-inventory.js` расширяет существующие primitives и хранит
типизированные записи в `actor.lionwing.inventory`; старый `actor.inventory` остаётся
только числовым read-compatible зеркалом. Стабильная декларация проверяет `owner`,
`source`, `ruleId`, `sourceDigest`, label/localization, minimum/maximum, initial/current,
boundary lifetime, visibility, uniqueness, multiple instances, replacement level/group,
edition и alternate-resource metadata. Поддержаны `stack`, `count`, `charges`, `slots`,
`recorded-value` и `selected-item` (включая повторяющиеся selected values).

Авторитетные операции: `configure/create`, `gain/add`, `spend`, `set`, `remove`,
`transfer`, `record`, `select`, `reset`. Cost API — `prepareCost`, `reserve`,
`commitReservation`, `cancelReservation`, `applyCosts`; reservations учитывают уже
занятый баланс, несколько одинаковых cost entries, атомарный rollback и duplicate
operation/replay protection. `resetActor`, `persistentState`, `resetScene` и
`removeSource` покрывают Scene/Round/Turn/Intermission/persistent, отменяя затронутые
резервации. `status`, `project`, `export`, `import`, `replay`, `undo` и
`alternateResourceStatus` дают typed public/private/network boundary. Engine остаётся
единственным writer, валидирует actor/source/role и не принимает от клиента balance,
max или ownership; private records, receipts и reservations видны только владельцу
в нужной проекции или Narrator/GM.

В `lionwing-engine.js` добавлены inventory event routing, typed projection, intermission
reset и корректные turn/round reset boundaries. `network-v2.js` принимает только
ограниченный набор player operations для собственных записей, сервер сам назначает
operation id. `lionwing-ui.js` показывает current/max/reserved, доступные `+`, `−`,
spend/remove действия и причину блокировки; recorded/selected values и служебные
receipts игрок не редактирует.

### Canonical consumers

Включены opt-in adapters с `knows level`, `sourceDigest` и `coverage: partial`; EN
canonical text, RU interface text и semantic diff зафиксированы в
`docs/tasks/LIONWING-INVENTORY-CANONICAL-REVIEW.md`:

- `altruist.gourmand.1`: Meal stack/count и расход независимой еды;
- `vagabond.malicious-mimic.1`: named Impression count instances и round reset;
- `altruist.surgeon.2`: Bandages и Disinfectant charges;
- `altruist.deckbuilder.1`: cards и recorded values;
- `altruist.deckbuilder.2`: captured card recorded value;
- `altruist.bardic-savant.1`: Verses selected items, repeated entries и Breathe choice;
- `ruiner.mana-blades.1`: Arsenal selected items и Forges stack;
- `ruiner.long-draw.1`: Prep charges.

Это только проверяемые inventory slices: броски, цели, эффекты, action economy,
полная валидация оружия/материалов и другие будущие families намеренно остаются
manual/partial. Не добавлялись дубли для Will-O-Wisp (его marker/selection/push
pipeline уже существует), Spellcrafter Innovation и Grim Ascendant Corruption;
Assassin, Poacher и Empath не получили независимого inventory slice в этом срезе.
Старые или расходящиеся записи Frost Veiler II, Grim Ascendant II и Empath III не
подключались. Registry notes не использовались как источник механики: каждый новый
consumer сверялся с EN PDF/canonical JSON, digest и RU semantic diff.

### Проверки и следующие шаги

Пройдены targeted `node tests/lionwing-inventory.mjs`, `npm --prefix apps/companion
run test:families`, `node --check` для inventory/engine, `git diff --check`, затем
полный `npm --prefix apps/companion test` (391 unique rule ids, OK). Generated
maps/docs обновлены только для доказанного coverage; supplements и maps не трогались.

Следующий безопасный шаг — подключать ещё один независимый partial slice только
после той же canonical сверки и расширять contract tests для нового consumer. Все
неопределённые случаи должны оставаться blocked с уведомлением и ручным Narrator
override; расширение до full technique automation требует отдельных movement,
lifecycle, derived-stats/combat-meter и action-copy contracts.

## Каноническая повторная сертификация 2026-09-10

Добавлен обязательный provenance gate `tests/lionwing-provenance.mjs`: каждый доступный
адаптер и каждый registry-уровень со статусом `full/decision` теперь обязан иметь
полный SHA-256 digest стабильного canonical payload и явный `sourceLevelId` для
внутренних subrules. Gate проверяет также объявленный coverage и включён в
`test:families`.

Исправлены усечённые digest Детектива I–II, канонические ветки Cryomancer II,
Grim Ascendant II, Empath III и Assassin III. Старый платный Empath Support и
старые half-damage/Regeneration claims Grim больше не считаются источником
автоматизации. Подробные counts и оставшийся partial/manual surface:
`docs/tasks/LIONWING_CANONICAL_RECERTIFICATION_AUDIT.md`.

## Интеграционный аудит authority и information pipeline, 2026-09-10

Каноническая пересертификация объединена с generic Technique UI,
Will-O-Wisp, information-query и typed inventory в ветке
`codex/integration-audit-20260910`. Конфликтующие списки модулей, адаптеров и
тестов объединены вручную; generated reports пересобраны после слияния.

Закрыты два блокирующих дефекта. Player intent больше не может напрямую
`gain/add/remove/select` запись typed inventory: игроку доступны только расход
собственной записи и передача из неё, а получение проходит через проверенное
правило или решение Нарратора. Кнопка `+` скрыта от игрока и остаётся в ручном
пульте Нарратора. Дополнительная проверка находится и внутри inventory contract.

Подтверждение и отмена Изучения из UI теперь проходят через
`LionWingEngine.prepare` и `commitSceneEvents`. Прямой вызов мутации
information-query удалён, поэтому решения попадают в единый event journal и
сохраняют обычные replay/undo/network границы.

После исправлений пройдены provenance gate (66 адаптеров и 84 executable
registry levels), targeted inventory/information tests и полный `npm test`:
391 уникальный rule id. Следующая отдельная проверка перед main — живой
двухклиентный smoke-test и поэтапный аудит turn/movement/numeric branches.

## Movement lifecycle integration audit, 2026-09-10

Принят общий adapter-facing lifecycle `movement.prepare/start/leave/segment/enter/cross/end/stop`. Факты пути, дистанции, режима и причины остановки вычисляет геометрия и журналирует ядро; клиентский путь не считается доказательством. Обычный Jump публикует тот же контракт. API предназначен как фундамент для массовых адаптеров и сам по себе не объявляет 118 зависимых уровней автоматизированными.
