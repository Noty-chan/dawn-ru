# DAWN VTT — передача «Тактического кокпита»

Статус: вариант 1 выбран пользователем. Его field-first принципы перенесены в основной `apps/companion/index.html`; `vtt-concepts.html` теперь только стенд проверки.

## Зафиксированные требования

1. Игрок видит и использует только свой лист. Нарратор видит одновременно компактные листы всех противников Сцены.
2. Лист игрока разделён на «Основной», «Био», «Боевой» и «Утилитарный».
3. Лист открывается боковым или нижним drawer без затемнения и блокировки всего поля.
4. Область Техники на мыши должна предварительно показываться под курсором до подтверждающего клика; тап остаётся мобильным fallback.
5. Справочник и базовые правила доступны из постоянного нижнего dock.
6. Броски являются публичными событиями Сцены.
7. У Фокуса нет верхнего предела. Производное значение — стартовое, но не максимум.
8. Базовые действия доступны и в боевом листе, и из нижнего dock.
9. Каждый изученный Уровень Техники обязан присутствовать в боевом листе; неполная автоматизация должна быть видимой, а не молчаливой.

## Что сделано в этом срезе

- удалено переключение между тремя концептами, оставлен «Тактический кокпит»;
- персональный лист игрока привязан к `viewerId`, лист другого игрока не показывается;
- Нарратор получает сетку всех компактных листов врагов с пассивами, действиями и Козырями;
- добавлены четыре раздела листа, личные заметки, публичный конструктор XD6, часы и быстрые полевые утилиты;
- все базовые действия из `data.js` выведены в боевой раздел;
- все 321 Уровня Техник получают статус `full`, `assist` или `manual`; 19 структурированных правил используют preview/commit/undo, остальные не теряются и разрешаются явной ручной транзакцией;
- preview области следует за курсором на desktop и подтверждается кликом/тапом;
- последний бросок показывается поверх поля;
- `maxFocus` отсутствует, а тест защищает инвариант неограниченного Фокуса;
- GitHub Pages собирает движок и кокпит.
- добавлен `scene-engine.js`: единый immutable `dispatch`, версия Сцены и append-only события для действий, ресурсов, движения, урона, бросков, Ходов и Раундов;
- `availableActions(actorId)` теперь является источником доступности базовых действий в боевом листе;
- «Шаг», «Прыжок», «Стычка», «Заклинание», «Завершение», «Передышка» и «Зарядка» проходят через подготовку и пакет событий; атаки фиксируют предложение Реакции, публичный бросок, урон с учётом Брони и разрешение действия;
- нижний dock, завершение Хода, ручное перемещение токена и публичный конструктор броска также пишут события Сцены.
- атака теперь создаёт `pendingAction` и ждёт отдельного ответа каждой цели: без Реакции, Блок или Уворот; Уворот требует выбрать свободную клетку в пределах 2 клеток;
- Броня снижает урон Атаки минимум до 1, Уклонение принимает оставшийся урон вместо Здоровья, а обнуление Здоровья выдаёт Рану/Влияние либо выводит участника из строя и повышает Напряжение;
- `turn.start`, `turn.end`, `round.end` и `actor.enter` обслуживают ОД, рост Напряжения, длительность объектов и вход врага в Газ;
- нижняя панель показывает очередь Реакций, а отдельный «Журнал Сцены» раскрывает версию и публичную последовательность событий.

## Обновление 2026-07-21

- основной компаньон загружает `scene-engine.js` и `technique-engine.js` напрямую;
- команды структурированных Техник переводятся через `toEvents()` в общий версионируемый журнал;
- общий стол получил публичную проекцию, append-only события, идемпотентность, последовательную очередь мутаций и приватные RLS-политики;
- игрок публикует личный лист в `characters`, а Нарратор принимает его в Сцену через `join_hero`;
- постоянная колонка заменена на dock и немодальные рабочие панели; добавлены масштаб, прокрутка, drag токенов и компактный отряд всех врагов;
- утилитарный бросок собирается из Атрибута, Навыка, Способности, Преимуществ и Помех и публикуется в Сцену.

## Что намеренно не закончено

- миграцию `202607210001_dawn_event_stream.sql` ещё нужно применить к отдельному Supabase DAWN;
- 19 правил структурированы, но большая часть 321 Уровня ещё требует расширения registry и событийного ядра;
- pending-action синхронизируется в состоянии и журнале, но ещё не имеет серверного таймера и автоматической политики пропущенной Реакции;
- для undo совместной игры нужен журнал обратных операций с проверкой версии, а не snapshot всей Сцены;
- остаются измерительная линейка и отдельная загрузка полноразмерной боевой карты; масштаб, прокрутка, фон и токены уже работают в основном столе;
- случайность публичного броска пока создаётся клиентом и подтверждается Нарратором; доверенный RNG остаётся серверной задачей.

## Следующий сильный срез

1. Применить и проверить новую миграцию в отдельном dev-проекте Supabase DAWN двумя реальными сессиями.
2. Реализовать встречный бросок «Столкновения», перенаправление «Блока» за союзника и вторичные эффекты, отменяемые полным Уклонением.
3. Заменить snapshot-undo компенсирующими событиями и добавить серверный таймер Реакций.
4. Расширять registry Техник группами, начиная с Техник, меняющих Реакции, урон и длительность Эффектов.

## Сообщение для возобновления в другом Codex

> Продолжи DAWN VTT в `D:\Projects\Active\dawn-ru`. Сначала прочитай `docs/tasks/PROJECT_CONTEXT.md`, `docs/tasks/GAME_ENVIRONMENT.md`, `docs/tasks/VTT_ARCHITECTURE.md`, `docs/tasks/TECHNIQUE_ENGINE.md` и `docs/tasks/VTT_COCKPIT_HANDOFF.md`, затем проверь последние коммиты. Основной стол уже field-first, использует общее ядро действий/Техник и миграцию `202607210001_dawn_event_stream.sql` для приватных листов, публичной проекции и append-only событий. Сначала проверь миграцию двумя сессиями Supabase, затем реализуй «Столкновение», перенаправление «Блока» и компенсирующий undo. Сохрани неограниченный Фокус и ручной fallback всех непокрытых Техник. Не трогай PDF-файлы и чужие незакоммиченные изменения. Заверши тестами и отдельным коммитом.
