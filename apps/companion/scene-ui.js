"use strict";

let sceneEnemyFilter="",sceneReferenceQuery="",sceneReferenceSection="combat",sceneReferenceMarkup="",scenePreviewCells=new Set(),sceneTopologyCells=new Set(),playerSceneTool="select";
const SCENE_TYPE_NAMES={attack:"Атака",gas:"Газ",terrain:"Местность",difficult:"Трудная местность",high:"Высокая местность",low:"Низкая местность",crowd:"Зона массовки","deploy-hero":"Развертывание героев","deploy-enemy":"Развертывание врагов",objective:"Цель сценария",danger:"Опасность",portal:"Переход",custom:"Особое правило"};
const SCENE_DURATION_NAMES={instant:"мгновенно",endTurn:"до конца текущего Хода",nextTurn:"до начала следующего Хода владельца",round:"до конца Раунда",scene:"до конца Сцены",persistent:"постоянно"};
const SCENE_MARKERS={mark:{name:"Метка",symbol:"◆"},damocles:{name:"Дамокл",symbol:"†"},bomb:{name:"Бомба / заряд",symbol:"✹"},ritual:{name:"Круг / печать",symbol:"◉"},trap:{name:"Ловушка",symbol:"⌁"},summon:{name:"Точка призыва",symbol:"✦"},weapon:{name:"Оружие",symbol:"⚔"},objective:{name:"Цель сцены",symbol:"⚑"},countdown:{name:"Отсчёт",symbol:"◷"},hidden:{name:"Скрытое ГМ",symbol:"?"},custom:{name:"Свой знак",symbol:"●"}};
const sceneMarkerType=kind=>SCENE_MARKERS[kind]||{name:String(kind||"Маркер"),symbol:"●"};
const TOKEN_SYMBOLS=["","✦","☠","♟","♞","⬢","✹","◎","⚠","◈","◆"];
const TECH_SCENE_TEMPLATES=[
  {tech:"ruiner.bombardier",level:1,kind:"area",name:"Бомбардир I: клетка + смежные",type:"attack",shape:"adjacent",duration:"instant",source:"Техника: Бомбардир — Взрыв!! (Explosion!!)",hint:"центр в пределах 4 клеток"},
  {tech:"ruiner.bombardier",level:2,kind:"area",name:"Бомбардир II: зона 3×3",type:"attack",shape:"square3",duration:"instant",source:"Техника: Бомбардир — Взрыв!!! (Explosion!!!)",hint:"при трате 2+ Фокуса; центр в пределах 5 клеток"},
  {tech:"ruiner.bombardier",level:3,kind:"area",name:"Бомбардир III: зона 5×5",type:"attack",shape:"square5",duration:"instant",source:"Техника: Бомбардир — ВЗРЫВ!!!! (EXPLOSION!!!!)",hint:"при трате 4+ Фокуса; центр в пределах 6 клеток"},
  {tech:"ruiner.rapid-fire-sorcery",level:2,kind:"area",name:"Выжженная земля",type:"difficult",shape:"cell",duration:"scene",source:"Техника: Револьверное колдовство — Выжженная земля (Scorched Earth)"},
  {tech:"ruiner.ritualist",level:1,kind:"marker",name:"Заклинательный круг",markerKind:"ritual",duration:"scene",color:"#6fc9d8",source:"Техника: Ритуалист — Лей-линии (Ley Lines)"},
  {tech:"ruiner.student-of-stars",level:2,kind:"area",name:"Бесформенная сила: линия",type:"attack",shape:"lineH",duration:"instant",source:"Техника: Ученик звезд — Бесформенная сила (Formless Strength)"},
  {tech:"ruiner.student-of-stars",level:2,kind:"area",name:"Бесформенная сила: зона 2×2",type:"attack",shape:"square2",duration:"instant",source:"Техника: Ученик звезд — Бесформенная сила (Formless Strength)"},
  {tech:"ruiner.ego-arm",level:2,kind:"marker",name:"Маркер Дамокла",markerKind:"damocles",duration:"scene",color:"#d04f64",source:"Техника: Эго-оружие — Покажи свои цели (Show Your Targets)"},
  {tech:"ruiner.sellsword-s-call",level:1,kind:"marker",name:"Точка призыва наёмника",markerKind:"summon",duration:"scene",color:"#6fc9d8",source:"Техника: Зов наемника — Реприза воина (A Warrior's Reprise)"},
  {tech:"bulwark.servant-s-call",level:1,kind:"marker",name:"Точка призыва слуги",markerKind:"summon",duration:"scene",color:"#6fc9d8",source:"Техника: Зов слуги — Честь подчиненного (A Subordinate's Honor)"},
  {tech:"disruptor.wave-rider",level:1,kind:"marker",name:"Печать волны",markerKind:"ritual",duration:"scene",color:"#3fa9d4",source:"Техника: Всадник волн — Мягкие волны (Gentle Waves)"},
  {tech:"disruptor.hunter",level:1,kind:"marker",name:"Малая ловушка",markerKind:"trap",duration:"scene",color:"#c28a45",source:"Техника: Охотник — Стальные челюсти (Steel Jaws)"},
  {tech:"disruptor.hunter",level:3,kind:"area",name:"Яма-ловушка 2×2",type:"terrain",shape:"square2",duration:"scene",source:"Техника: Охотник — Яма-ловушка (Pit Trap)"},
  {tech:"disruptor.gale-strider",level:1,kind:"area",name:"Тайфун: клетка + смежные",type:"danger",shape:"square3",duration:"scene",source:"Техника: Шагающий по буре — Растущие ветра (Growing Winds)"},
  {tech:"disruptor.chemist",level:1,kind:"area",name:"Сублимация: Газ 3×3",type:"gas",shape:"square3",duration:"nextTurn",source:"Техника: Химик — Сублимация (Sublimation)"},
  {tech:"disruptor.inner-world",level:2,kind:"space",name:"Домен контроля: Внутренний мир",spaceName:"Внутренний мир",width:3,height:3,source:"Техника: Внутренний мир — Домен контроля (Domain Of Control)"},
  {tech:"bulwark.giant-frame",level:1,kind:"area",name:"Огромные руки: зона 2×2",type:"attack",shape:"square2",duration:"instant",source:"Техника: Гигантская фигура — Огромные руки (Big Arms)"},
  {tech:"powerhouse.warring-ascendant",level:3,kind:"area",name:"Дюрандаль: бесконечная Линия",type:"attack",shape:"lineH",duration:"instant",source:"Техника: Воинственный Вознесенный — Святой меч, Дюрандаль (Saintly Sword, Durandal)"},
];
const GIFT_SCENE_TEMPLATES=[
  {gift:"Spite",kind:"marker",name:"Цель Злобы",markerKind:"mark",duration:"scene",color:"#d04f64",source:"Дар: Злоба (Spite)"},
  {gift:"The Sneaky Superposition",kind:"area",name:"Скрытная суперпозиция: точка появления",type:"portal",shape:"cell",duration:"instant",source:"Дар: Скрытная суперпозиция (The Sneaky Superposition)"},
  {gift:"Convenient Transport",kind:"token",name:"Удобный транспорт",tokenName:"Транспорт",tokenSymbol:"⬢",tokenColor:"#527f62",source:"Дар: Удобный транспорт (Convenient Transport)"},
];
function activeSceneSpace(){return Scene.spaces.find(space=>space.id===Scene.activeSpace)||Scene.spaces[0]}
function activeSceneTool(){return activeSceneView()==="player"?playerSceneTool:Scene.tool}
function canControlSceneActor(actor){return activeSceneView()==="gm"||Boolean(actor?.heroId&&actor.heroId===S.id)}
function canControlScenePrompt(prompt=Scene.pendingPrompt){if(!prompt)return true;return prompt.controller==="narrator"?activeSceneView()==="gm":canControlSceneActor(Scene.actors.find(actor=>actor.id===prompt.sourceActorId))}
function canDragSceneActor(actor){return activeSceneView()==="gm"||Boolean(actor?.heroId&&actor.heroId===S.id)}
function moveSceneActorFromBoard(actor,x,y,{manual=false}={}){if(!actor)return;if(Scene.pendingAction)return toast("Сначала завершите текущую цепочку Реакций");if(actor.x===x&&actor.y===y)return;const space=activeSceneSpace(),deploying=!sceneCombatStarted(Scene),deploymentTeam=actor.team==="enemy"?"enemy":"hero",occupancy=SceneEngine.effectCellOccupancyStatus(Scene,actor.id,{space:Scene.activeSpace,x,y,placement:true});if(!occupancy.available)return toast(occupancy.reason);if(deploying&&activeSceneView()!=="gm"&&!deploymentCells(space,deploymentTeam).includes(`${x},${y}`))return toast(`Развертывание ${deploymentTeam==="enemy"?"врага":"героя"} разрешено только в зоне его стороны`);if(deploying||activeSceneView()==="gm"&&(manual||Scene.tool==="place")){commitSceneEvents(`${deploying?"Развертывание":"Ручная перестановка"}: ${actor.name}`,[{type:"actor.move",actorId:actor.id,payload:{space:Scene.activeSpace,x,y,movement:deploying?"Развертывание":"Ручная перестановка",placement:true,trace:false,allowKnockedOut:Boolean(actor.knockedOut&&activeSceneView()==="gm")}},{type:"actor.enter",actorId:actor.id,payload:{space:Scene.activeSpace,x,y,movement:deploying?"Развертывание":"Ручная перестановка",placement:true,trace:false}}]);return}if(activeSceneView()==="player"||Scene.activeActorId===actor.id){const step=SceneEngine.actionByKey(D,"step"),prepared=SceneEngine.prepareAction(Scene,D,{actorId:actor.id,actionId:step?.id,targetIds:[],destination:{x,y}});if(!prepared.ok)return toast(prepared.errors.join(" "));commitSceneEvents(`Шаг: ${actor.name}`,prepared.events);return}commitSceneEvents(`Ручная перестановка: ${actor.name}`,[{type:"actor.move",actorId:actor.id,payload:{space:Scene.activeSpace,x,y,movement:"Ручная перестановка",placement:true,trace:false}},{type:"actor.enter",actorId:actor.id,payload:{space:Scene.activeSpace,x,y,movement:"Ручная перестановка",placement:true,trace:false}}])}
function measurementPath(start,end){if(!start||!end)return[];const cells=[],stepX=Math.sign(end.x-start.x),stepY=Math.sign(end.y-start.y);let x=start.x,y=start.y;cells.push(`${x},${y}`);while(x!==end.x){x+=stepX;cells.push(`${x},${y}`)}while(y!==end.y){y+=stepY;cells.push(`${x},${y}`)}return cells}
function enemyAreaCells(rule,anchor){const space=activeSceneSpace(),width=Number(rule?.area?.[0]||0),height=Number(rule?.area?.[1]||0),cells=[],startX=anchor.x-(width%2?Math.floor(width/2):0),startY=anchor.y-(height%2?Math.floor(height/2):0);for(let dy=0;dy<height;dy++)for(let dx=0;dx<width;dx++){const x=startX+dx,y=startY+dy;if(x>=0&&y>=0&&x<space.width&&y<space.height)cells.push(`${x},${y}`)}return cells}
function topologyShapeCells(anchor){return SceneEngine.spatialShapeStatus(Scene,{space:Scene.activeSpace,shape:$("scene-topology-shape")?.value||"cell",anchor}).cells}
const sceneUsesLionwing=()=>Scene.rulesEdition==="lionwing";
function sceneEffects(){return sceneUsesLionwing()&&LionwingTable?LionwingTable.effects(localizedLionwingCoreRules()):D.effects}
function sceneEffectList(){const effects=sceneEffects();return[...effects.positive,...effects.negative]}
// Retired local profiles remain resolvable in saved legacy games.
const RETIRED_ENEMY_PROFILE_IDS=new Set(["enemy.named.leon-academy-spatial-mage","enemy.named.leon-s-vayu-spirit","enemy.named.leon-s-agni-spirit"]);
function allEnemyProfiles(){return sceneUsesLionwing()&&LionwingTable?LionwingTable.profiles(localizedLionwingCoreRules()):Object.values(D.enemies||{}).flat()}
function enemyProfiles(){return allEnemyProfiles().filter(enemy=>!RETIRED_ENEMY_PROFILE_IDS.has(enemy.id))}
function enemyProfile(id){return allEnemyProfiles().find(enemy=>enemy.id===id)}
function antagonistTraits(){return sceneUsesLionwing()?[]:D.enemies?.antagonistTraits||[]}
function antagonistTrait(id){return antagonistTraits().find(trait=>trait.id===id)}
function antagonistDefense(actor){return antagonistTrait(actor?.antagonistTraitId)?.rules?.find(rule=>rule.kind==="defense-reaction")||null}
function enemyAutomationName(value){return value==="attack"?"бросок + урон":value==="full"?"полностью авто":value==="state"?"состояние авто":value==="effect"?"эффект авто":"с подтверждением"}
function enemyStepButtonHtml(actor){const step=SceneEngine.actionByKey(D,"step"),state=step&&SceneEngine.availableActions(Scene,D,actor.id).find(item=>item.id===step.id),label=state?.continuation?`Продолжить Шаг · ${state.remaining} кл.`:`Шаг · до ${actor.speed||0} кл.`;return `<button type="button" class="enemy-basic-step" data-enemy-step="${actor.id}" ${state?.available?"":"disabled"} title="${esc(state?.reason||"Каноническое базовое действие врага · 1 ОД")}"><strong>${esc(label)}</strong><small>${state?.continuation?"0 ОД · сохранённое движение":"1 ОД · единственное базовое действие врага"}</small></button>`}
function enemyRuleOptionsHtml(rule,actor,state=null){
  if(rule.id!=="enemy.common.pugilist.action.take-stance")return "";
  const current=Number(actor.ruleState?.pugilistStance||1);
  return `<div class="enemy-rule-options" aria-label="Выбрать шаг Пассива">${[1,2,3,4].map(step=>`<button type="button" data-enemy-rule="${esc(rule.id)}" data-enemy-actor="${actor.id}" data-stance-step="${step}" class="${current===step?"on":""}" ${state?.available?"":"disabled"}>Шаг ${step}</button>`).join("")}</div>`;
}
function enemyRuleStateLabel(actor){
  const state=actor.ruleState||{},parts=[];
  if(state.pugilistStance)parts.push(`Стойка ${state.pugilistStance}`);
  if(state.martialPerfection)parts.push("Боевое совершенство");
  if(state.growth)parts.push(`Рост ${state.growth}`);
  if(state.imposingPresence)parts.push("Внушительное присутствие");
  if(actor.extraTurns)parts.push(`Доп. Ход ×${actor.extraTurns}`);
  return parts.join(" · ");
}
function sceneSnapshot(){return sceneCore(Scene)}
function sceneEvent(text){Scene.log.unshift({id:uid(),at:new Date().toISOString(),text,type:"legacy.note",actorId:null,payload:{label:text},visibility:"public"});Scene.log=Scene.log.slice(0,200)}
function sceneActorEffects(actor){return actor?SceneEngine.effectiveEffects(Scene,actor.id):[]}
function sceneEffectTitle(actor,effect){const status=SceneEngine.effectStatus(Scene,actor.id,effect.id),sourceNames=status.sourceActorIds.map(id=>Scene.actors.find(item=>item.id===id)?.name).filter(Boolean);return[effect.text,status.direct?`Срок: ${status.expiresAt}`:status.ambient?"Источник: область, маркер или связанный Эффект":"Не наложен",sourceNames.length?`Наложил: ${sourceNames.join(", ")}`:"",status.direct&&!status.removable?"Нельзя снять до конца Сцены":""].filter(Boolean).join("\n")}
function sceneResourceChips(actor){const definition=SceneEngine.ruleResourceDefinitions(actor)[0],secondary=definition?`${SceneEngine.ruleResourceStatus(Scene,actor.id,{resource:definition.resource}).balance} ${definition.label}`:`${actor.focus} Фокус`,clocks=SceneEngine.ruleClockDefinitions(actor).map(item=>SceneEngine.clockStatus(Scene,actor.id,item.clockId)).filter(status=>status.active).map(status=>`<i>${status.value}/${status.size} ${esc(status.label)}</i>`).join(""),modes=SceneEngine.ruleModeDefinitions(actor).map(item=>actor.ruleModes?.[item.groupId]?.label?`<i>${esc(item.label)}: ${esc(actor.ruleModes[item.groupId].label)}</i>`:"").join("");return`${definition?.replaces?.includes("ap")?"":`<i>${actor.ap} ОД</i>`}<i>${secondary}</i>${clocks}${modes}`}
function clockEventText(event){if(!event.type?.startsWith("rule-clock."))return"";const actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Система",payload=event.payload||{},delta=Number(payload.appliedDelta||0);return`${actor}: ${payload.label||payload.clockId||"часы"} ${payload.value??"—"}/${payload.size??"—"}${delta?` (${delta>0?"+":""}${delta})`:""}`}
function eventText(event){const actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Система",payload=event.payload||{},target=Scene.actors.find(item=>item.id===payload.targetId)?.name||payload.targetId||"цель",effect=sceneEffectList().find(item=>item.id===payload.effect)?.name||payload.effect||"Эффект",resource=payload.resolvedResource||payload.resource||"ресурс",delta=Number(payload.resolvedDelta);return ({"action.prepare":`${actor}: готовит «${payload.name||"действие"}»`,"reaction.offer":`${actor}: доступна Реакция`,"reaction.respond":`${actor}: ${payload.choice||"ответил"}`,"roll.public":`${actor}: ${payload.formula||"бросок"} → ${payload.successes||0} успехов`,"roll.redirect":`${actor}: Тёмный порыв перенаправлен на ${target}; результат броска сохранён`,"rule-mode.set":`${actor}: экипировано Вооружение «${payload.label||payload.modeId}»${payload.replacedModeId?` вместо «${payload.replacedModeId}»`:""}`,"damage.apply":payload.ignored?`${target}: урон пропущен — цель уже вне боя`:`${actor}: ${target} получает ${payload.dealt??payload.amount??0} урона`,"actor.heal":`${actor}: ${target} восстанавливает ${payload.restored??payload.amount??0} Здоровья`,"actor.wound":`${actor}: Раны ${Number(payload.appliedDelta??payload.delta)>0?"+":""}${payload.appliedDelta??payload.delta??0}`,"actor.knockout":`${actor}: ${target} выведен из строя`,"effect.apply":`${actor}: ${target} получает «${effect}»`,"effect.remove":`${actor}: с ${target} снят «${effect}»`,"inventory.change":`${actor}: ${Number(payload.delta)>0?"+":""}${payload.delta} ${payload.item}`,"rule.trigger":payload.status==="queued"?`${actor}: триггер «${payload.triggerId}» ждёт очереди`:payload.status==="cancelled"?`${actor}: триггер «${payload.triggerId}» отменён — ${payload.reason||"условия изменились"}`:`${actor}: сработал триггер «${payload.triggerId}»`,"rule.prompt":`${actor}: сработало «${payload.title||"правило"}»`,"rule.respond":`${actor}: решение правила — ${payload.choice}`,"action.resolve":`${actor}: разрешено «${payload.name||"действие"}»`,"enemy.action.prepare":`${actor}: использует «${payload.name||"действие"}»`,"enemy.action.resolve":`${actor}: завершает «${payload.name||"действие"}»`,"resource.spend":payload.ignoredReason?`${actor}: ${payload.ignoredReason}`:`${actor}: ${Number.isFinite(delta)&&delta>0?"+":"−"}${Math.abs(Number.isFinite(delta)?delta:Number(payload.amount||0))} ${resource}`,"resource.gain":payload.ignoredReason?`${actor}: ${payload.ignoredReason}`:`${actor}: ${Number.isFinite(delta)&&delta<0?"−":"+"}${Math.abs(Number.isFinite(delta)?delta:Number(payload.amount||0))} ${resource}`,"rule-resource.configure":`${actor}: настроен ресурс «${payload.label||resource}»`,"rule-resource.spend":`${actor}: −${payload.amount||0} ${resource}`,"rule-resource.gain":`${actor}: +${payload.amount||0} ${resource}`,"rule-resource.set":`${actor}: ${resource} = ${payload.value}`,"rule-resource.reset":`${actor}: ${resource} сброшен до ${payload.value}`,"actor.move":payload.placement?`${actor}: ручная перестановка`:`${actor}: ${payload.topologyCrossings?.length?"телепорт через разрыв":"перемещение"}`,"actor.enter":`${actor}: вошёл в клетку`,"turn.start":`${actor}: начал Ход`,"turn.end":`${actor}: завершил Ход`,"turn.grant":`${actor}: получает дополнительный Ход`,"round.end":`Завершён Раунд ${Math.max(1,Scene.round-1)}`,"attack.pending":`Атака «${payload.name||""}» ожидает Реакции`,"attack.clear":payload.cancelled?`Цепочка Атаки прервана: ${payload.reason||"без причины"}`:`Атака разрешена`,"technique.prepare":`${actor}: готовит Технику «${payload.name||""}»`,"technique.resolve":`${actor}: применил Технику «${payload.name||"Технику"}»`,"technique.manual":`${actor}: вручную разрешил «${payload.name||"Технику"}»`,"technique.state":payload.key==="study"?`${actor}: Хитрый план +1`:`${actor}: Хитрый план ${Number(payload.delta||0)>0?"+":""}${payload.delta||0}`,"actor.state":`${actor}: состояние правила «${payload.key||""}» обновлено`,"area.create":`Создана область «${payload.label||"Техника"}»`,"area.remove":`${actor}: убрана область «${payload.label||payload.id||"местность"}»`,"object.damage":`${actor}: «${payload.label||payload.objectId||"местность"}» получает ${payload.dealt??payload.amount??0} урона${payload.destroyed?" и разрушается":""}`,"marker.create":`Поставлен маркер «${payload.label||"Техника"}»`,"marker.move":`${actor}: перемещён маркер «${payload.label||payload.markerId||"Техника"}»`,"marker.remove":`${actor}: убран маркер «${payload.label||payload.markerId||"Техника"}»`,"topology.cells.remove":`Удалены клетки поля (${payload.count||payload.cells?.length||0}): «${payload.label||"Разрыв поля"}»${payload.crossing==="opposite"?" · переход насквозь":""}${payload.destroyedTerrainIds?.length?` · уничтожено местности: ${payload.destroyedTerrainIds.length}`:""}`,"topology.cells.restore":`Восстановлены клетки поля (${payload.count||payload.cells?.length||0}): «${payload.label||"Разрыв поля"}»`,"space.ensure":`Открыто пространство «${payload.name||""}»`})[event.type]||event.text||event.type||"Событие"}
const baseEventTextV27=eventText;
eventText=function(event){const actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Система",payload=event.payload||{};if(event.type==="action.plan")return`${actor}: начат составной план «${payload.actionName||"действие"}»`;if(event.type==="action.plan.update")return`${actor}: план «${payload.actionId||"действие"}» перешёл к фазе ${payload.phase}`;if(event.type==="action.plan.cancel")return`${actor}: составной план отменён — ${payload.reason||"без затрат"}`;if(event.type==="rule.share")return`${payload.sharedBy||"Нарратор"} показал правило «${payload.title||"Правило"}»`;if(event.type==="session-clock.create")return`Добавлены ${payload.kind==="progress"?"хорошие":"плохие"} часы «${payload.name}»`;if(event.type==="session-clock.set")return`Часы Сцены: ${payload.before??0} → ${payload.value??0}`;if(event.type==="session-clock.rename")return`Часы Сцены переименованы в «${payload.name}»`;if(event.type==="session-clock.kind")return`Изменён тип часов: ${payload.kind==="progress"?"хорошие":"плохие"}`;if(event.type==="session-clock.size")return`Размер часов: ${payload.size} сегм.`;if(event.type==="session-clock.remove")return`Удалены часы Сцены «${payload.name||""}»`;if(event.type==="action.prepare"&&payload.declaredActionName&&payload.declaredActionName!==payload.name)return`${actor}: «${payload.declaredActionName}» превращается в «${payload.name}»${payload.actionTransform?.ruleId==="bulwark.grappler.2"?" · Перелом позвоночника":""}`;if(event.type==="attack.pending"&&payload.attackModifierIds?.length)return`${baseEventTextV27(event)} · модификаторов до броска: ${payload.attackModifierIds.length}`;return baseEventTextV27(event)};
const baseEventTextReadableReactions=eventText;
eventText=function(event){if(event.type==="reaction.respond"&&event.payload?.label){const actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Система";return`${actor}: ${event.payload.label}`}return baseEventTextReadableReactions(event)};
const baseEventTextRuntime=eventText;
eventText=function(event){if(event.type==="actor.runtime.set"){const actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Герой",labels={hp:"Здоровье",wounds:"Раны",focus:"Фокус",influence:"Влияние",ap:"ОД"};return`${actor}: ${labels[event.payload?.key]||event.payload?.key} = ${event.payload?.value}`}return baseEventTextRuntime(event)};
const baseEventTextMovementTraces=eventText;
eventText=function(event){if(event.type==="movement-traces.clear")return"Нарратор очистил линии передвижения";return baseEventTextMovementTraces(event)};
const baseEventTextChallenges=eventText;
eventText=function(event){const payload=event.payload||{};if(event.type==="challenge.request"){const target=Scene.actors.find(actor=>actor.id===payload.actorId)?.name||"герой";return`${payload.requestedBy||"Нарратор"} запросил бросок у ${target} · цель ${payload.target}`}if(event.type==="challenge.clear")return"Нарратор закрыл запрос броска";return baseEventTextChallenges(event)};
const baseEventTextOpposed=eventText;
eventText=function(event){const payload=event.payload||{},opposed=Scene.opposedRoll;if(event.type==="opposed.request")return`${payload.requestedBy||"Нарратор"} создал встречный бросок: ${(payload.participants||[]).map(item=>item.name).join(" против ")}`;if(event.type==="opposed.reroll")return`Ничья во встречном броске · переброс ${opposed?.attempt||""}`;if(event.type==="opposed.tie.resolve")return"Нарратор разрешил обе совместимые Награды";if(event.type==="opposed.clear")return"Нарратор завершил встречный бросок";return baseEventTextOpposed(event)};
const baseEventTextActorSpawn=eventText;
eventText=function(event){if(event.type==="actor.spawn"){const actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Система";return`${actor}: призывает «${event.payload?.actor?.name||"существо"}»`}return baseEventTextActorSpawn(event)};
const baseEventTextScheduler=eventText;
eventText=function(event){const payload=event.payload||{},labels={"reminder.create":`Отложено: «${payload.label||"решение"}»`,"reminder.due":`Пора разрешить: «${payload.label||"отложенный эффект"}»`,"reminder.resolve":`Разрешено: «${payload.label||"отложенный эффект"}»`,"reminder.remove":`Удалено напоминание «${payload.label||""}»`,"area.duration":`Срок области «${payload.label||"область"}» изменён`,"marker.duration":`Срок маркера «${payload.label||"маркер"}» изменён`};return labels[event.type]||baseEventTextScheduler(event)};
function syncHeroFromScene(){const actor=Scene.actors.find(item=>item.heroId===S.id);if(!actor)return;Object.assign(S.runtime,{hp:Math.min(actor.hp,actor.maxHp),maxHp:actor.maxHp,wounds:actor.wounds,stress:actor.stress,focus:actor.focus,influence:actor.influence,ap:actor.ap,effects:[...(actor.effects||[])],sacrifices:[...(actor.sacrifices||[])]});S.runtime.tension=Scene.tension}
function commitScene(label,mutator){const before=sceneSnapshot(),sync=Sync?.state?.()||{};if(sync.sceneId&&!sync.canNarrate)return toast("Каноническую Сцену изменяет Нарратор");try{mutator(Scene);validateTableEdit(before,Scene)}catch(error){Scene=normalizeScene({...before,undo:Scene.undo,redo:Scene.redo,turnUndo:Scene.turnUndo});toast(error.message);return null}Scene=normalizeScene(Scene);Scene.redo=[];if(sync.sceneId){Scene.undo.unshift({id:uid(),label,state:before});Scene.undo=Scene.undo.slice(0,20);sceneEvent(label);syncHeroFromScene();persist();if(store.mode==="play")renderPlay();else renderScene();queueNetworkV2Snapshot(sceneSnapshot(),label);return{queued:true}}Scene.version=Number(Scene.version||0)+1;Scene.undo.unshift({id:uid(),label,state:before});Scene.undo=Scene.undo.slice(0,20);sceneEvent(label);syncHeroFromScene();persist();if(store.mode==="play")renderPlay();else renderScene();return{scene:Scene}}
let lastSceneEventSubmission={fingerprint:"",at:0};
function captureSceneFxContext(events=[]){const wrap=$("scene-board-wrap");if(!wrap)return{};const wrapRect=wrap.getBoundingClientRect(),points={};for(const event of events.filter(item=>item.type==="actor.move")){const token=wrap.querySelector(`[data-scene-actor="${CSS.escape(event.actorId||"")}"]`),rect=token?.getBoundingClientRect();if(rect)points[event.actorId]={x:rect.left-wrapRect.left+wrap.scrollLeft+rect.width/2,y:rect.top-wrapRect.top+wrap.scrollTop+rect.height/2}}return{points}}
function commitSceneEvents(label,events){const fingerprint=JSON.stringify([label,events]),now=performance.now();if(fingerprint===lastSceneEventSubmission.fingerprint&&now-lastSceneEventSubmission.at<450)return toast("Двойное нажатие пропущено; это не подтверждение сервера");lastSceneEventSubmission={fingerprint,at:now};const fxContext=captureSceneFxContext(events);let sharedResult;try{sharedResult=submitNetworkV2Events(label,events)}catch(error){lastSceneEventSubmission={fingerprint:"",at:0};toast(friendlySyncError(error,"Это действие пока нельзя отправить за общий стол"));return null}if(sharedResult)return sharedResult;const before=sceneSnapshot(),expectedVersion=Number(Scene.version||0);let result;try{result=SceneEngine.dispatchMany(Scene,events,{expectedVersion})}catch(error){lastSceneEventSubmission={fingerprint:"",at:0};return toast(error.message||"Не удалось применить события Сцены")}Scene=normalizeScene(result.scene);if(result.events.some(event=>event.type==="round.end"))Scene.turnUndo=[];if(result.events.some(event=>event.type==="turn.start"))Scene.turnUndo=[{id:uid(),label:"До начала Хода",state:before,checkpoint:"turn-start"},...(Scene.turnUndo||[])].slice(0,120);Scene.undo.unshift({id:uid(),label,state:before});Scene.undo=Scene.undo.slice(0,20);Scene.redo=[];syncHeroFromScene();persist();if(store.mode==="play")renderPlay();else if(store.mode==="tools")renderToolsWorkspace();else renderScene();renderChallengeRequestDock();playSceneEventFx(result.events,fxContext);return result}
function restoreSceneHistory(step,source,target,prefix){const current=sceneSnapshot(),remaining=source.slice(1),restored=sceneCore(step.state),opposite=[{id:uid(),label:step.label,state:current},...target].slice(0,20),turnHistory=source===Scene.turnUndo?remaining:[...(Scene.turnUndo||[])];restored.version=Number(current.version||0)+1;if(source===Scene.undo){restored.undo=remaining;restored.redo=opposite}else if(source===Scene.turnUndo){restored.undo=[];restored.redo=opposite}else{restored.redo=remaining;restored.undo=opposite}restored.turnUndo=turnHistory;Scene=restored;sceneEvent(`${prefix}: ${step.label}`);syncHeroFromScene();persist();if(store.mode==="play")renderPlay();else renderScene();if(!queueNetworkV2Snapshot(sceneSnapshot(),`scene.history:${prefix}:${step.label}`))Sync?.queueScene(sceneSnapshot(),`scene.history:${prefix}:${step.label}`)}
function undoScene(){const source=Scene.undo.length?Scene.undo:Scene.turnUndo||[],step=source[0];if(!step)return toast("В журнале нет обратимого действия");const countDelta=key=>Math.abs(Number(step.state?.[key]?.length||0)-Number(Scene[key]?.length||0)),structural=Number(step.state?.round||1)!==Number(Scene.round||1)||countDelta("actors")>0||countDelta("spaces")>0||countDelta("objects")>1||countDelta("markers")>1||countDelta("walls")>1;if(structural&&!window.confirm(`«${step.label}» меняет Раунд или сразу несколько элементов Сцены. Отменить именно этот крупный шаг?`))return;restoreSceneHistory(step,source,Scene.redo||[],"Отменено")}
function redoScene(){const step=Scene.redo?.[0];if(!step)return toast("Нет действия для повтора");restoreSceneHistory(step,Scene.redo,Scene.undo||[],"Повторено")}
function applyNarratorOverride({targets,damage=0,effectId="",note=""}){
  const unique=[],seen=new Set();
  for(const target of targets){const compound=SceneEngine.compoundEnemyStatus(Scene,target.id),key=compound.active?`compound:${compound.id}`:`actor:${target.id}`;if(!seen.has(key)){seen.add(key);unique.push(target)}}
  const label=`Решение Нарратора: ${unique.map(item=>item.name).join(", ")}${note?` · ${note}`:""}`;
  return commitScene(label,scene=>{for(const target of unique){const compound=SceneEngine.compoundEnemyStatus(scene,target.id),parts=compound.active?compound.parts:[scene.actors.find(item=>item.id===target.id)].filter(Boolean);if(damage>0){let remaining=Math.max(0,(compound.active?compound.hp:Number(parts[0]?.hp||0))-damage);for(const part of parts){part.hp=Math.min(Number(part.maxHp||remaining),remaining);remaining-=part.hp}const knockedOut=parts.every(part=>Number(part.hp||0)<=0);parts.forEach(part=>part.knockedOut=knockedOut);if(knockedOut&&parts.some(part=>part.id===scene.activeActorId))scene.activeActorId=null}if(effectId){for(const part of parts){part.effects=[...new Set([...(part.effects||[]),effectId])];part.effectStates||={};part.effectStates[effectId]={duration:"default",removable:true,appliedTurnSerial:Number(scene.turnSerial||0),appliedRound:Number(scene.round||1),appliedEventId:"narrator.override",sourceBound:false,exclusiveBySource:false,sources:[]}}}}});
}
function narratorActorValue(actor,key){if(key!=="hp")return Math.max(0,Number(actor?.[key]||0));const compound=actor&&SceneEngine.compoundEnemyStatus(Scene,actor.id);return Math.max(0,Number(compound?.active?compound.hp:actor?.hp||0))}
function setNarratorActorValue(actor,key,value,label=""){const next=Math.max(0,Number(value)||0);return commitScene(label||`Решение Нарратора: ${actor.name} · ${key} = ${next}`,scene=>{if(key==="hp"){const compound=SceneEngine.compoundEnemyStatus(scene,actor.id),parts=compound.active?compound.parts:[scene.actors.find(item=>item.id===actor.id)].filter(Boolean);let remaining=next;for(const part of parts){part.hp=Math.min(Number(part.maxHp||remaining),remaining);remaining-=part.hp}if(remaining>0&&parts[0])parts[0].hp=Number(parts[0].hp||0)+remaining;const knockedOut=parts.every(part=>Number(part.hp||0)<=0);parts.forEach(part=>part.knockedOut=knockedOut);if(knockedOut&&parts.some(part=>part.id===scene.activeActorId))scene.activeActorId=null;return}const target=scene.actors.find(item=>item.id===actor.id);if(target)target[key]=next})}
function adjustNarratorActorValue(actor,key,delta,label){return setNarratorActorValue(actor,key,narratorActorValue(actor,key)+Number(delta||0),label)}
function setNarratorEffect(actor,effectId,remove=false){if(!actor||!effectId)return false;const effect=sceneEffectList().find(item=>item.id===effectId),compound=SceneEngine.compoundEnemyStatus(Scene,actor.id);return commitScene(`Решение Нарратора: ${remove?"снят":"наложен"} «${effect?.name||effectId}» · ${actor.name}`,scene=>{const liveCompound=SceneEngine.compoundEnemyStatus(scene,actor.id),parts=liveCompound.active?liveCompound.parts:[scene.actors.find(item=>item.id===actor.id)].filter(Boolean);for(const part of parts){part.effects=remove?(part.effects||[]).filter(id=>id!==effectId):[...new Set([...(part.effects||[]),effectId])];part.effectStates||={};if(remove)delete part.effectStates[effectId];else part.effectStates[effectId]={duration:"default",removable:true,appliedTurnSerial:Number(scene.turnSerial||0),appliedRound:Number(scene.round||1),appliedEventId:"narrator.override",sourceBound:false,exclusiveBySource:false,sources:[]}}})}
function forceNarratorTurn(actor){
  if(!actor)return false;
  const ending=Scene.activeActorId===actor.id,current=Scene.actors.find(item=>item.id===Scene.activeActorId),warning=[Scene.pendingAction?"цепочка Реакций":"",Scene.pendingPrompt?"ожидающее решение":"",Scene.pendingActionPlan?"составное действие":""].filter(Boolean).join(", ");
  return commitScene(`Решение Нарратора: ${ending?"завершён":"начат"} Ход ${actor.name}${warning?` · прервано: ${warning}`:""}`,scene=>{
    scene.pendingAction=null;scene.pendingPrompt=null;scene.pendingActionPlan=null;scene.triggerQueue=[];
    const live=scene.actors.find(item=>item.id===actor.id),previous=scene.actors.find(item=>item.id===scene.activeActorId);if(!live)return;live.knockedOut=false;live.hp=Math.max(1,Number(live.hp||0));
    const events=ending?[{type:"turn.end",actorId:live.id,payload:{narratorOverride:true}}]:[...(previous&&previous.id!==live.id?[{type:"turn.end",actorId:previous.id,payload:{narratorOverride:true}}]:[]),{type:"turn.start",actorId:live.id,payload:{narratorOverride:true}}],result=SceneEngine.dispatchMany(scene,events,{expectedVersion:Number(scene.version||0),narratorOverride:true});
    for(const key of Object.keys(scene))delete scene[key];Object.assign(scene,result.scene);
  });
}
function forceNarratorRound(){return commitScene(`Решение Нарратора: завершён Раунд ${Scene.round} вопреки незавершённым Ходам`,scene=>{scene.pendingAction=null;scene.pendingPrompt=null;scene.pendingActionPlan=null;scene.triggerQueue=[];const result=SceneEngine.dispatchMany(scene,[{type:"round.end",actorId:null,payload:{narratorOverride:true}}],{expectedVersion:Number(scene.version||0),narratorOverride:true});for(const key of Object.keys(scene))delete scene[key];Object.assign(scene,result.scene)})}
function deploymentCells(space,team){if(sceneCombatStarted(Scene))return Array.from({length:space.width*space.height},(_,i)=>`${i%space.width},${Math.floor(i/space.width)}`);const explicit=Scene.objects.filter(object=>object.space===space.id&&object.type===`deploy-${team}`).flatMap(object=>object.cells||[]);if(explicit.length)return[...new Set(explicit)];if(space.mode==="cinematic")return team==="hero"?["0,0","1,0"]:["5,0","6,0"];const x=team==="hero"?0:space.width-1;return Array.from({length:space.height},(_,y)=>`${x},${y}`)}
function firstEmptyCell(spaceId,team="hero"){const space=Scene.spaces.find(item=>item.id===spaceId)||activeSceneSpace(),cinematic=space.mode==="cinematic",busy=new Set(Scene.actors.filter(actor=>actor.kind!=="crowd"&&actor.space===space.id&&SceneEngine.effectPresenceStatus(Scene,actor.id).onField&&!sceneActorEffects(actor).includes("positive.изгнан")).map(actor=>`${actor.x},${actor.y}`)),blocked=new Set(Scene.objects.filter(object=>object.space===space.id&&object.type==="terrain").flatMap(object=>object.cells||[])),removed=SceneEngine.removedCellKeys(Scene,space.id),ordered=[...deploymentCells(space,team),...Array.from({length:space.width*space.height},(_,i)=>`${i%space.width},${Math.floor(i/space.width)}`)];for(const key of ordered){const[x,y]=key.split(",").map(Number);if((cinematic||!busy.has(key))&&!blocked.has(key)&&!removed.has(key))return{x,y}}return{x:0,y:0}}
function scaledEnemyStats(profile,tier){const scaled={};for(const key of ["health","speed","armor","evasion"]){const raw=profile?.stats?.[key]??profile?.statistics?.[key],value=Logic.scaleTierFormula(raw,tier);scaled[key]=value===null?0:value}return scaled}
function availableSceneTemplates(){return TECH_SCENE_TEMPLATES.filter(template=>(S.techniques[template.tech]||0)>=template.level).concat(GIFT_SCENE_TEMPLATES.filter(template=>hasGift(template.gift)))}
function applySceneTemplate(index){const template=availableSceneTemplates()[index];if(!template)return;if(template.kind==="area"){Scene.tool="area";persist();renderScene();$("scene-area-type").value=template.type;$("scene-area-shape").value=template.shape;$("scene-area-label").value=template.name;$("scene-area-duration").value=template.duration;if([...$("scene-area-source").options].some(option=>option.value===template.source))$("scene-area-source").value=template.source;toast(`Шаблон готов: укажите клетку или направление${template.hint?` · ${template.hint}`:""}`);return}if(template.kind==="marker"){Scene.tool="marker";persist();renderScene();$("scene-marker-kind").value=template.markerKind;$("scene-marker-label").value=template.name;$("scene-marker-duration").value=template.duration;$("scene-marker-color").value=template.color;if([...$("scene-marker-source").options].some(option=>option.value===template.source))$("scene-marker-source").value=template.source;toast("Шаблон готов: поставьте маркер на нужную клетку");return}if(template.kind==="token"){const position=firstEmptyCell(Scene.activeSpace);commitScene(`Добавлен токен «${template.tokenName}»`,scene=>{const actor={id:uid(),kind:"token",team:"hero",heroId:null,profileId:null,name:template.tokenName,tier:1,space:scene.activeSpace,...position,hp:1,maxHp:1,speed:0,armor:0,evasion:0,effects:[],acted:false,hidden:false,tokenSymbol:template.tokenSymbol,tokenColor:template.tokenColor,tokenImage:"",portraitImage:""};scene.actors.push(actor);scene.selectedActor=actor.id});return}const selected=Scene.actors.find(actor=>actor.id===Scene.selectedActor),targets=Scene.actors.filter(actor=>Scene.targetIds.includes(actor.id));if(!selected||selected.heroId!==S.id)return toast("Сначала выберите текущего героя, использующего Технику");commitScene(`Применён «${template.name}»`,scene=>{let space=scene.spaces.find(item=>item.name===template.spaceName);if(!space){space={id:uid(),name:template.spaceName,width:template.width,height:template.height};scene.spaces.push(space)}const moving=[selected,...targets.filter(actor=>actor.id!==selected.id)];moving.forEach((actor,index)=>{actor.space=space.id;actor.x=index%space.width;actor.y=Math.floor(index/space.width)%space.height});scene.activeSpace=space.id;scene.selectedActor=selected.id;scene.targetIds=targets.map(actor=>actor.id)})}
function sceneSources(){const sources=["Ручное правило"];if(S.ability.enabled)sources.push(`Способность: ${S.ability.name||abilityFormula()}`);if(S.mods.taintedBody&&S.taintedAbility.enabled)sources.push(`Способность «Порченого тела»: ${S.taintedAbility.name||abilityFormula(S.taintedAbility)}`);for(const gift of selectedGifts())sources.push(`Дар: ${gift.name} (${gift.en})`);for(const [id,level]of Object.entries(S.techniques)){const tech=techById(id);if(!tech||level<1)continue;for(const rule of tech.levels.slice(0,level))sources.push(`Техника: ${tech.name} — ${rule.name}`)}const selected=Scene.actors.find(actor=>actor.id===Scene.selectedActor),profile=enemyProfile(selected?.profileId);if(profile)sources.push(`Враг: ${profile.name}`);return sources}
function renderEnemySelect(){const select=$("scene-enemy-select"),previous=select.value,q=sceneEnemyFilter.trim().toLowerCase(),matches=enemy=>!q||`${enemy.name} ${enemy.en} ${enemy.tags} ${enemy.examples} ${enemy.text}`.toLowerCase().includes(q),groups=sceneUsesLionwing()?[["NPC LionWing · ручное разрешение",enemyProfiles()]]:[["Именные",D.enemies?.named||[]],["Обычные враги",D.enemies?.common||[]],["Враги-Модификаторы",D.enemies?.modifiers||[]]];select.innerHTML=groups.map(([label,list])=>`<optgroup label="${label}">${list.filter(enemy=>!RETIRED_ENEMY_PROFILE_IDS.has(enemy.id)&&matches(enemy)).map(enemy=>`<option value="${enemy.id}">${enemyProfileAutomation(enemy)!=="assisted"?"⚙ ":""}${esc(enemy.name)} · ${esc(enemy.tags)}</option>`).join("")}</optgroup>`).join("");if([...select.options].some(option=>option.value===previous))select.value=previous;const traitSelect=$("scene-enemy-trait");if(traitSelect){const selected=traitSelect.value;traitSelect.innerHTML=`<option value="">Без Черты Антагониста</option>${antagonistTraits().map(trait=>`<option value="${trait.id}">${esc(trait.name)} · ${esc(trait.tags)}</option>`).join("")}`;traitSelect.value=antagonistTraits().some(trait=>trait.id===selected)?selected:""}}
function compoundBuilderParts(){return Scene.actors.filter(actor=>(actor.kind==="enemy"||actor.profileId)&&actor.kind!=="crowd"&&!actor.compoundId)}
function updateCompoundBuilderSelection(){const root=$("scene-compound-parts"),button=$("scene-create-compound"),status=$("scene-compound-status");if(!root||!button||!status)return;const checked=[...root.querySelectorAll("[data-compound-part]:checked")],teams=new Set(checked.map(input=>input.dataset.team));button.disabled=checked.length<2||teams.size!==1;status.textContent=checked.length<2?`Выбрано ${checked.length}: нужно минимум две части.`:teams.size>1?"Нельзя объединить союзников и противников в одного босса.":`Готово к объединению: ${checked.length} части.`;status.classList.toggle("ready",!button.disabled)}
function renderCompoundBuilder(){const root=$("scene-compound-parts");if(!root)return;const selected=new Set([...root.querySelectorAll("[data-compound-part]:checked")].map(input=>input.value)),parts=compoundBuilderParts();root.innerHTML=`<legend>Части босса</legend>${parts.map(actor=>`<label class="compound-part"><input type="checkbox" data-compound-part value="${actor.id}" data-team="${actor.team}" ${selected.has(actor.id)?"checked":""}><i style="--token-color:${actor.tokenColor}">${esc(actor.tokenSymbol||actor.name.slice(0,2).toUpperCase())}</i><span><strong>${esc(actor.name)}</strong><small>${actor.team==="hero"?"Союзник героев":"Противник"} · Ступень ${actor.tier}</small></span></label>`).join("")||`<p class="autosave">Выставьте минимум два отдельных профильных НПС.</p>`}`;updateCompoundBuilderSelection()}
function renderSceneSources(){const sources=sceneSources();for(const id of ["scene-area-source","scene-marker-source"]){const select=$(id),previous=select.value;select.innerHTML=sources.map(source=>`<option value="${esc(source)}">${esc(source)}</option>`).join("");if([...select.options].some(option=>option.value===previous))select.value=previous}}
function hideSceneTokenTip(delay=80){clearTimeout(sceneTokenTipTimer);sceneTokenTipTimer=setTimeout(()=>{const tip=$("scene-token-popover");if(tip)tip.hidden=true},delay)}
function showSceneTokenTip(token,{sticky=false}={}){
  const actor=Scene.actors.find(item=>item.id===token?.dataset.sceneActor),tip=$("scene-token-popover");if(!actor||!tip)return;
  clearTimeout(sceneTokenTipTimer);
  const maxHp=Number(actor.maxHp)||0,hpRatio=maxHp?clamp(actor.hp/maxHp*100,0,100):100,effects=sceneActorEffects(actor).map(id=>sceneEffectList().find(effect=>effect.id===id)?.name||id),state=actor.knockedOut?"Вне боя":Scene.activeActorId===actor.id?"Сейчас Ход":actor.acted?"Уже действовал":"Готов";
  const definition=SceneEngine.ruleResourceDefinitions(actor)[0],resourceLabel=definition?.label||"Фокус",resourceValue=definition?SceneEngine.ruleResourceStatus(Scene,actor.id,{resource:definition.resource}).balance:actor.focus;tip.innerHTML=`<header><i style="--token-color:${actor.tokenColor}">${esc(actor.tokenSymbol||actor.name.slice(0,2).toUpperCase()||"?")}</i><span><strong>${esc(actor.name)}</strong><small>${actor.team==="enemy"?"Противник":"Союзник"} · ${esc(state)}</small></span></header><div class="scene-tip-health"><span><b>ЗД ${actor.hp}/${maxHp||"—"}</b><small>${Math.round(hpRatio)}%</small></span><i><u style="width:${hpRatio}%"></u></i></div><div class="scene-tip-stats"><span><small>ОД</small><b>${actor.ap}</b></span>${actor.team==="hero"?`<span><small>${esc(resourceLabel)}</small><b>${resourceValue}</b></span>`:""}<span><small>Скорость</small><b>${actor.speed}</b></span><span><small>Броня</small><b>${actor.armor}</b></span><span><small>Уклонение</small><b>${actor.evasion}</b></span></div><p>${effects.length?`Эффекты: ${effects.map(esc).join(" · ")}`:"Нет активных Эффектов"}</p>`;
  tip.hidden=false;
  const rect=token.getBoundingClientRect(),width=tip.offsetWidth,height=tip.offsetHeight,left=clamp(rect.left+rect.width/2-width/2,8,Math.max(8,innerWidth-width-8)),above=rect.top-height-10;
  tip.style.left=`${left}px`;tip.style.top=`${above>8?above:Math.min(innerHeight-height-8,rect.bottom+10)}px`;
  sceneTokenTipTimer=setTimeout(()=>{tip.hidden=true},sticky?3200:8000);
}
function toggleSceneTarget(actorId){
  const actor=Scene.actors.find(item=>item.id===actorId);
  if(!actor)return;
  if(actor.knockedOut)return toast("Персонаж вне боя и недоступен как цель");
  Scene.targetIds=Scene.targetIds.includes(actor.id)?Scene.targetIds.filter(id=>id!==actor.id):Scene.targetIds.concat(actor.id);
  persist();renderScene();
  toast(Scene.targetIds.includes(actor.id)?`${actor.name}: добавлен в цели`:`${actor.name}: убран из целей`);
}
function hideSceneContextMenu(){
  const menu=$("scene-context-menu");if(menu)menu.hidden=true;sceneContextTarget=null;
}
function showSceneContextMenu(event,{actor=null,cell=null,marker=null}={}){
  const menu=$("scene-context-menu");if(!menu)return;
  sceneContextTarget={actorId:actor?.id||null,cell:cell?.dataset.sceneCell||null,markerId:marker?.id||null};
  const gm=activeSceneView()==="gm",targeted=actor&&Scene.targetIds.includes(actor.id),selected=actor&&Scene.selectedActor===actor.id,active=actor&&Scene.activeActorId===actor.id;
  const actorItems=actor?[
    ["target",targeted?"Убрать из целей":"Добавить в цели","T"],
    ["inspect",selected?"Открыть карточку":"Выбрать и открыть","↵"],
    ["focus","Показать на поле","F"],
    ...(gm?[["turn",active?"Завершить Ход":"Начать Ход",""]]:[]),
  ]:[];
  const cellItems=cell?[
    ...(gm&&Scene.selectedActor?[["move","Переставить выбранного сюда без затрат",""]]:[]),
    ["measure","Начать измерение отсюда","M"],
    ...(gm?[["marker","Поставить маркер здесь",""],["area","Создать область отсюда",""]]:[]),
  ]:[];
  const markerItems=marker&&gm?[["remove-marker",`Удалить «${marker.label}»`,""]]:[];
  const items=[...actorItems,...markerItems,...cellItems,["clear-targets","Снять все цели","Esc"]].filter(Boolean);
  menu.innerHTML=items.map(([action,label,key])=>`<button type="button" role="menuitem" data-scene-context-action="${action}"><span>${esc(label)}</span>${key?`<kbd>${esc(key)}</kbd>`:""}</button>`).join("");
  menu.hidden=false;
  const width=menu.offsetWidth,height=menu.offsetHeight;
  menu.style.left=`${clamp(event.clientX,8,Math.max(8,innerWidth-width-8))}px`;
  menu.style.top=`${clamp(event.clientY,8,Math.max(8,innerHeight-height-8))}px`;
  menu.querySelector("button")?.focus({preventScroll:true});
}
function shortTechniqueLineCells(anchor,orientation){const space=activeSceneSpace(),vectors={horizontal:[1,0],vertical:[0,1],"diagonal-down":[1,1],"diagonal-up":[1,-1]},[dx,dy]=vectors[orientation]||vectors.horizontal,cells=[];for(let offset=-1;offset<=1;offset+=1){const x=anchor.x+dx*offset,y=anchor.y+dy*offset;if(x>=0&&y>=0&&x<space.width&&y<space.height)cells.push(`${x},${y}`)}return cells}
function insetSceneTracePoints(points,startPadding=.22,endPadding=.3){
  const centered=points.map(point=>({x:Number(point.x)+.5,y:Number(point.y)+.5}));if(centered.length<2)return centered;
  const inset=(point,toward,padding)=>{const dx=toward.x-point.x,dy=toward.y-point.y,length=Math.hypot(dx,dy)||1,amount=Math.min(padding,length*.34);return{x:point.x+dx/length*amount,y:point.y+dy/length*amount}};
  const first=centered[0],second=centered[1],last=centered.at(-1),beforeLast=centered.at(-2);
  centered[0]=inset(first,second,startPadding);centered[centered.length-1]=inset(last,beforeLast,endPadding);
  return centered
}
function sceneTracePathData(points,actors,actorId,space){
  if(points.length<2)return"";
  const occupied=actors.filter(actor=>actor.id!==actorId).map(actor=>({x:Number(actor.x)+.5,y:Number(actor.y)+.5}));
  points=points.map((point,index)=>{if(index===0||index===points.length-1||!occupied.some(actor=>Math.hypot(actor.x-point.x,actor.y-point.y)<.1))return point;const previous=points[index-1],next=points[index+1],dx=next.x-previous.x,dy=next.y-previous.y,length=Math.hypot(dx,dy)||1,nx=-dy/length,ny=dx/length,plus={x:point.x+nx*.43,y:point.y+ny*.43},minus={x:point.x-nx*.43,y:point.y-ny*.43},edgeScore=value=>Math.min(value.x,value.y,Number(space.width)-value.x,Number(space.height)-value.y);return edgeScore(plus)>=edgeScore(minus)?plus:minus});
  const commands=[`M ${points[0].x.toFixed(3)} ${points[0].y.toFixed(3)}`];
  for(let index=1;index<points.length;index+=1){
    const from=points[index-1],to=points[index],dx=to.x-from.x,dy=to.y-from.y,length=Math.hypot(dx,dy)||1,ux=dx/length,uy=dy/length,nx=-uy,ny=ux;
    const obstacles=occupied.map(point=>{const projection=(point.x-from.x)*ux+(point.y-from.y)*uy,closest={x:from.x+ux*projection,y:from.y+uy*projection},distance=Math.hypot(point.x-closest.x,point.y-closest.y);return{...point,projection,distance}}).filter(point=>point.projection>.18&&point.projection<length-.18&&point.distance<.31).sort((a,b)=>a.projection-b.projection);
    for(const obstacle of obstacles){
      const plus={x:obstacle.x+nx*.36,y:obstacle.y+ny*.36},minus={x:obstacle.x-nx*.36,y:obstacle.y-ny*.36},edgeScore=point=>Math.min(point.x,point.y,Number(space.width)-point.x,Number(space.height)-point.y),side=edgeScore(plus)>=edgeScore(minus)?1:-1,before={x:obstacle.x-ux*.26+nx*.34*side,y:obstacle.y-uy*.26+ny*.34*side},around={x:obstacle.x+nx*.43*side,y:obstacle.y+ny*.43*side},after={x:obstacle.x+ux*.26+nx*.34*side,y:obstacle.y+uy*.26+ny*.34*side};
      commands.push(`L ${before.x.toFixed(3)} ${before.y.toFixed(3)} Q ${around.x.toFixed(3)} ${around.y.toFixed(3)} ${after.x.toFixed(3)} ${after.y.toFixed(3)}`)
    }
    commands.push(`L ${to.x.toFixed(3)} ${to.y.toFixed(3)}`)
  }
  return commands.join(" ")
}
function sceneTracePartGlyph(part,kind){
  const from={x:Number(part.from.x)+.5,y:Number(part.from.y)+.5},destination={x:Number(part.destination.x)+.5,y:Number(part.destination.y)+.5},dx=destination.x-from.x,dy=destination.y-from.y,length=Math.hypot(dx,dy)||1,middle={x:(from.x+destination.x)/2,y:(from.y+destination.y)/2},badge={x:destination.x-dy/length*.31,y:destination.y+dx/length*.31},angle=Math.atan2(dy,dx)*180/Math.PI;
  if(kind==="teleport")return"";
  const symbol=kind==="forced"?"»":kind==="jump"?"⌁":"›";
  return `<g class="trace-part trace-part-${kind}" transform="translate(${middle.x.toFixed(3)} ${middle.y.toFixed(3)}) rotate(${angle.toFixed(2)})"><circle r=".105"/><text x="0" y=".006">${symbol}</text></g><g class="trace-part-number" transform="translate(${badge.x.toFixed(3)} ${badge.y.toFixed(3)})"><circle r=".095"/><text x="0" y=".006">${part.index}</text></g>`
}
function sceneMovementTracesSvg(space,actors){
  if(!sceneCombatStarted(Scene))return"";
  const status=SceneEngine.movementTraceStatus(Scene,{space:space.id});if(!status.available)return"";
  const actorMap=new Map(actors.map(actor=>[actor.id,actor]));
  const traces=status.traces.map((trace,index)=>{const actor=actorMap.get(trace.actorId);if(!actor)return"";const color=safeColor(actor.tokenColor,"#47bfd3"),marker=`move-arrow-${index}`,geometry=insetSceneTracePoints(trace.teleport?[trace.from,trace.destination]:trace.points),path=sceneTracePathData(geometry,actors,actor.id,space),origin=geometry[0],destination=geometry.at(-1),kind=trace.kind||"step",title=esc(`${actor.name}: ${trace.movement} · ${trace.parts?.length||1} ч. пути`),parts=(trace.parts||[]).map(part=>sceneTracePartGlyph(part,kind)).join("");
    if(trace.teleport)return `<g class="scene-move-trace teleport" style="--trace-color:${color}"><title>${title}</title><path class="trace-halo" d="${path}"/><path class="trace-line" d="${path}"/><circle class="trace-origin-ring" cx="${origin.x}" cy="${origin.y}" r=".13"/><circle class="trace-origin" cx="${origin.x}" cy="${origin.y}" r=".045"/><path class="trace-rift" d="M ${((origin.x+destination.x)/2)-.12} ${(origin.y+destination.y)/2} l .08 -.08 l .08 .08 l .08 -.08"/><path class="trace-destination" d="M ${destination.x} ${destination.y-.14} L ${destination.x+.14} ${destination.y} L ${destination.x} ${destination.y+.14} L ${destination.x-.14} ${destination.y} Z"/><text class="trace-destination-symbol" x="${destination.x}" y="${destination.y+.012}">✦</text></g>`;
    return `<g class="scene-move-trace normal ${kind}" style="--trace-color:${color}"><title>${title}</title><defs><marker id="${marker}" viewBox="0 0 8 8" markerWidth="4" markerHeight="4" refX="6.8" refY="4" orient="auto"><path d="M0,0 L8,4 L0,8 L2.2,4 Z" fill="${color}"/></marker></defs><path class="trace-halo" d="${path}"/><path class="trace-line" d="${path}" marker-end="url(#${marker})"/><circle class="trace-origin-ring" cx="${origin.x}" cy="${origin.y}" r=".105"/><circle class="trace-origin" cx="${origin.x}" cy="${origin.y}" r=".04"/>${parts}</g>`
  }).join("");
  return traces?`<svg class="scene-movement-traces" viewBox="0 0 ${space.width} ${space.height}" preserveAspectRatio="none" aria-hidden="true">${traces}</svg>`:""
}
function wallEventText(event){const payload=event.payload||{},actor=Scene.actors.find(item=>item.id===event.actorId)?.name||"Нарратор";return event.type==="wall.create"?`${actor}: проведена Стена «${payload.label||"Стена"}»`:event.type==="wall.damage"?`${actor}: Стена «${payload.label||"Стена"}» получает ${payload.dealt??payload.amount??0} урона${payload.destroyed?" и разрушается":""}`:event.type==="wall.remove"?`${actor}: убрана Стена «${payload.label||"Стена"}»`:""}
function sceneMarkerGlyph(marker){
  const wispGlyphs={dreamy:"М",angry:"З",insightful:"П",bright:"Я",kind:"Д",fierce:"Р"},spirits=Array.isArray(marker.metadata?.spiritTypes)?marker.metadata.spiritTypes:[];
  return spirits.length?spirits.map(type=>wispGlyphs[type]||"?").join("").slice(0,2):sceneMarkerType(marker.kind).symbol;
}
function renderSceneWalls(board,walls){
  walls.forEach(wall=>{const [ax,ay]=wall.a.split(",").map(Number),[bx,by]=wall.b.split(",").map(Number),side=bx>ax?"east":bx<ax?"west":by>ay?"south":"north",cell=board.querySelector(`[data-scene-cell="${CSS.escape(wall.a)}"]`);if(!cell)return;cell.insertAdjacentHTML("beforeend",`<button type="button" class="scene-wall side-${side}" data-scene-wall="${wall.id}" title="${esc(`${wall.label} · ЗД ${wall.hp}/${wall.maxHp} · блокирует движение и проведение цели`)}" aria-label="${esc(`${wall.label}, Здоровье ${wall.hp} из ${wall.maxHp}`)}"><span>${wall.hp}</span></button>`)});
}
function renderSceneBoard(){
  const space=activeSceneSpace(),board=$("scene-board");
  board.dataset.battlefieldMode=space.mode||"custom";
  board.setAttribute("aria-label",`Поле DAWN ${space.width} на ${space.height}`);
  board.style.setProperty("--scene-cols",space.width);board.style.setProperty("--scene-rows",space.height);
const objects=Scene.objects.filter(object=>object.space===space.id),actors=Scene.actors.filter(actor=>actor.space===space.id&&SceneEngine.effectPresenceStatus(Scene,actor.id).onField).filter(actor=>!SceneEngine.isEnemyModifier?.(actor)||["enemy.modifier.legion","enemy.modifier.vip"].includes(actor.profileId)||actor.profileId==="enemy.modifier.collateral"&&actor.modifierState?.deployed),markers=Scene.markers.filter(marker=>marker.space===space.id&&(marker.kind!=="hidden"||activeSceneView()==="gm")),removed=SceneEngine.removedCellKeys(Scene,space.id),placementPrompt=canControlScenePrompt()&&["marker-move-cell","dim-mak-weak-point-cell","empath-rush-cell","reappear-cell","knife-pickup-step","meister-overclock-move","egomaniac-style-move","thunder-surge-cell","siren-irresistible-cell","untouchable-weave-cell","constrictor-move-cell","enemy-move-cell","enemy-crowd-move-cell","fodder-move-cell"].includes(Scene.pendingPrompt?.kind),reactionTarget=pendingCoreReaction&&Scene.actors.find(actor=>actor.id===pendingCoreReaction.actorId),reactionOption=reactionTarget&&SceneEngine.reactionOptions(Scene,D,reactionTarget.id).find(option=>option.id===SceneEngine.canonicalActionId(pendingCoreReaction.choice)),reactionActor=reactionOption?.enemyTrait?Scene.actors.find(actor=>actor.id===(reactionOption.enemyTrait.postMove?reactionOption.enemyTrait.reactionActorId:reactionOption.enemyTrait.defenderActorId)):reactionTarget,actionPlan=Scene.pendingActionPlan,planAppearance=actionPlan?.phase==="reappear",planDestination=actionPlan?.phase==="destination",planModifierDestination=planDestination&&actionPlan.context?.destinationKind==="attack-modifier",planActor=actionPlan&&Scene.actors.find(actor=>actor.id===actionPlan.actorId),enemyStepActor=pendingEnemyStepActorId&&Scene.actors.find(actor=>actor.id===pendingEnemyStepActorId),movementActor=planAppearance||planDestination?planActor:enemyStepActor||pendingCoreAction&&currentHeroActor(),movementAction=enemyStepActor?SceneEngine.actionByKey(D,"step"):D.actions.list.find(action=>action.id===(actionPlan?actionPlan.actionId:pendingCoreAction)),appearanceAction=Boolean(planAppearance||!actionPlan&&movementActor&&SceneEngine.actionIsAny(movementAction,["skirmish","spell","finish"])&&sceneActorEffects(movementActor).includes("positive.изгнан")),assassinAppearance=appearanceAction&&Number(movementActor?.techniques?.["vagabond.assassin"]||0)>=2,busy=new Set(actors.filter(actor=>actor.id!==reactionActor?.id).filter(actor=>!movementActor||!sceneActorEffects(movementActor).includes("positive.изгнан")&&!sceneActorEffects(actor).includes("positive.изгнан")).map(actor=>`${actor.x},${actor.y}`));
  const walls=(Scene.walls||[]).filter(wall=>wall.space===space.id),innerWorldPrompt=canControlScenePrompt()&&["inner-world-target-cell","inner-world-caster-cell"].includes(Scene.pendingPrompt?.kind);
  const wispAuraCells=new Set(markers.filter(marker=>/altruist\.will-o-wisp\.1/.test(`${marker.ruleId||""} ${marker.source||""}`)).flatMap(marker=>Array.from({length:space.width*space.height},(_,cellIndex)=>({x:cellIndex%space.width,y:Math.floor(cellIndex/space.width)})).filter(point=>Math.abs(marker.x-point.x)+Math.abs(marker.y-point.y)<=1).map(point=>`${point.x},${point.y}`)));
  const cells=Array.from({length:space.width*space.height},(_,index)=>{
    const x=index%space.width,y=Math.floor(index/space.width),key=`${x},${y}`,cellObjects=objects.filter(object=>object.cells.includes(key)),cellActors=actors.filter(actor=>actor.x===x&&actor.y===y),cellMarkers=markers.filter(marker=>marker.x===x&&marker.y===y),reactionBaseDistance=reactionOption?.destinationKind==="move"?Number(reactionOption.maxDistance||3):Number(reactionActor?.techniques?.["vagabond.untouchable"]||0)>=2?3:2,reactionDistance=reactionActor?SceneEngine.effectMovementStatus(Scene,reactionActor.id,{distance:reactionBaseDistance}).distance:0,reactionPath=reactionActor?.space===space.id?SceneEngine.movementPath(Scene,reactionActor.id,{x,y},{maxDistance:reactionDistance}):[],reactionAnchor=reactionOption?.destinationKind==="adjacent-attacker"?Scene.actors.find(actor=>actor.id===Scene.pendingAction?.actorId):reactionOption?.destinationKind==="adjacent-trait-owner"?Scene.actors.find(actor=>actor.id===reactionOption.enemyTrait?.reactionActorId):null,reactionOccupied=reactionActor&&SceneEngine.effectCellOccupancyStatus(Scene,reactionActor.id,{space:space.id,x,y}).available,reactionSpecialValid=reactionOption?.destinationKind==="edge"?reactionOccupied&&(x===0||y===0||x===space.width-1||y===space.height-1):reactionOption?.destinationKind?.startsWith("adjacent")?reactionOccupied&&reactionAnchor?.space===space.id&&Math.max(Math.abs(reactionAnchor.x-x),Math.abs(reactionAnchor.y-y))<=1:reactionOption?.destinationKind==="move"?reactionOccupied&&(reactionPath.length||(reactionActor.x===x&&reactionActor.y===y)):false,reactionValid=Boolean(reactionActor&&(reactionOption?.enemyTrait?reactionSpecialValid:reactionPath.length&&reactionOccupied)),continuingStep=SceneEngine.actionIs(movementAction,"step")&&(movementActor?.usedActions||[]).includes(movementAction.id)&&Number(movementActor?.stepRemaining||0)>0,armamentBlade=pendingCoreActionContext?.actionId===pendingCoreAction&&pendingCoreActionContext?.context?.armamentMode==="blade",movementBase=armamentBlade?1:SceneEngine.actionIs(movementAction,"jump")?Number(movementActor?.attrs?.talent||0):continuingStep?Number(movementActor?.stepRemaining||0):Number(movementActor?.speed||0),movementLimit=movementActor?SceneEngine.effectMovementStatus(Scene,movementActor.id,{distance:movementBase}).distance:0,movementPath=!appearanceAction&&!planModifierDestination&&movementActor?.space===space.id?SceneEngine.movementPath(Scene,movementActor.id,{x,y},{maxDistance:movementLimit,straight:SceneEngine.actionIs(movementAction,"jump"),ignoreEnemies:SceneEngine.actionIs(movementAction,"jump"),ignoreDifficult:SceneEngine.actionIs(movementAction,"jump")}):[],appearanceValid=appearanceAction&&SceneEngine.effectCellOccupancyStatus(Scene,movementActor.id,{space:space.id,x,y}).available&&(assassinAppearance||!actors.some(item=>item.id!==movementActor.id&&!item.knockedOut&&Math.abs(item.x-x)+Math.abs(item.y-y)<=1)),modifierDestinationValid=planModifierDestination&&SceneEngine.attackModifierDestinationStatus(Scene,movementActor.id,actionPlan.context?.targetIds||[],actionPlan.context?.attackModifierIds||[],{x,y},{actionId:movementAction?.id,origin:actionPlan.context?.reappearance||null}).available,bladeDestinationValid=armamentBlade&&SceneEngine.masterAtArmsStatus(Scene,movementActor.id,{modeId:"blade",targetIds:pendingCoreActionContext.context.targetIds||[],destination:{x,y}}).available,movementValid=Boolean(movementActor&&SceneEngine.effectCellOccupancyStatus(Scene,movementActor.id,{space:space.id,x,y}).available&&(appearanceValid||modifierDestinationValid||(armamentBlade?bladeDestinationValid:movementPath.length))),classes=[...cellObjects.map(object=>`has-${object.type}`),...(!sceneCombatStarted(Scene)?(deploymentCells(space,"hero").includes(key)?["has-deploy-hero"]:deploymentCells(space,"enemy").includes(key)?["has-deploy-enemy"]:[]):[])].join(" ");
    const tokens=cellActors.map(actor=>{const maxHp=Number(actor.maxHp)||0,grimFocus=Boolean(actor.ruleState?.grimTransformed),insideGas=cellObjects.some(object=>object.type==="gas"),hudValue=grimFocus?Number(actor.focus||0):Number(actor.hp||0),hudMaximum=grimFocus?Math.max(10,hudValue):maxHp,hpRatio=hudMaximum?clamp(hudValue/hudMaximum*100,0,100):100,effectCount=sceneActorEffects(actor).length,status=actor.knockedOut?" · вне боя":Scene.activeActorId===actor.id?" · сейчас Ход":"";return `<button type="button" draggable="${canDragSceneActor(actor)}" class="scene-token ${actor.team} ${insideGas?"inside-gas":""} ${grimFocus?"grim-focus":""} ${actor.knockedOut?"knocked-out":""} ${Scene.activeActorId===actor.id?"active-turn":""} ${Scene.selectedActor===actor.id?"selected":""} ${Scene.targetIds.includes(actor.id)?"targeted":""}" data-scene-actor="${actor.id}" data-scene-actor-team="${actor.team}" style="--token-color:${actor.tokenColor};--token-hp:${hpRatio}%" aria-label="${esc(grimFocus?`${actor.name}, Фокус ${hudValue}, ОД ${actor.ap}${status}`:`${actor.name}, ЗД ${actor.hp} из ${maxHp||"неизвестно"}, ОД ${actor.ap}${status}`)}"><span class="scene-token-visual">${actor.tokenImage?`<img src="${actor.tokenImage}" alt="">`:esc(actor.tokenSymbol||actor.name.trim().slice(0,2).toUpperCase()||"?")}</span><span class="scene-token-hud" aria-hidden="true"><i><u></u></i><b>${grimFocus?`Ф ${hudValue}`:`${actor.hp}/${maxHp||"—"}`}</b></span>${actor.kind!=="token"?`<span class="scene-token-ap" aria-hidden="true">${actor.ap}</span>`:""}${effectCount?`<span class="scene-token-effects" aria-hidden="true">${effectCount}</span>`:""}</button>`}).join("");
    const topologyCut=(Scene.topology?.cuts||[]).find(cut=>cut.space===space.id&&cut.cells.includes(key)),topologySelected=sceneTopologyCells.has(key),topologyInvalid=Scene.tool==="topology"&&(removed.has(key)||busy.has(key)),innerWorldCellValid=innerWorldPrompt&&(Scene.pendingPrompt.options||[]).includes(`cell:${x},${y}`),promptPlacementValid=placementPrompt&&SceneEngine.preparePromptPlacement(Scene,{destination:{x,y}}).ok;
    return `<div class="scene-cell ${classes} ${removed.has(key)?"topology-removed":""} ${topologyCut?.crossing==="opposite"?"topology-crossing":""} ${topologySelected?"topology-selected":""} ${topologyInvalid?"topology-invalid":""} ${reactionValid?"reaction-valid":""} ${movementValid?"movement-valid":""} ${promptPlacementValid||innerWorldCellValid?"prompt-placement-valid":""} ${scenePreviewCells.has(key)?"preview":""} ${sceneMeasureCells.has(key)?"measured":""}" role="gridcell" tabindex="0" data-scene-cell="${key}" aria-disabled="${removed.has(key)||innerWorldPrompt&&!innerWorldCellValid}" aria-label="Клетка ${x+1}, ${y+1}${innerWorldPrompt&&!innerWorldCellValid?", занята или недоступна":removed.has(key)?topologyCut?.crossing==="opposite"?", разрыв телепортирует на противоположную сторону":", удалена из поля":""}">${removed.has(key)?`<span class="scene-topology-glyph" aria-hidden="true">${topologyCut?.crossing==="opposite"?"⇥":"▧"}</span>`:""}${cellObjects.length?`<span class="scene-cell-rules" title="${esc(cellObjects.map(object=>`${object.label}: ${object.source}`).join("; "))}">${cellObjects.map(object=>SCENE_TYPE_NAMES[object.type][0]).join("")}</span>`:""}${cellMarkers.length?`<span class="scene-cell-markers">${cellMarkers.map(marker=>`<button type="button" class="scene-marker marker-${marker.kind} ${Scene.pendingPrompt?.context?.markerId===marker.id?"marker-selected":""}" data-scene-marker="${marker.id}" style="--marker-color:${marker.color}" title="${esc(`${marker.label} · ${String.fromCharCode(65+marker.x)}${marker.y+1} · ${SCENE_DURATION_NAMES[marker.duration]||"срок не указан"}`)}">${esc(sceneMarkerGlyph(marker))}</button>`).join("")}</span>`:""}<span class="scene-coord">${String.fromCharCode(65+x)}${y+1}</span><span class="scene-tokens">${tokens}</span></div>`}).join("");
  const empty=actors.length||["topology","area","wall","marker"].includes(Scene.tool)||pendingZealotPlan?"":`<div class="scene-empty-state" role="note"><span aria-hidden="true">✦</span><strong>Сцена ждёт героев</strong><small>${activeSceneView()==="gm"?"Добавьте героя или противника — они сразу появятся на поле.":"Нарратор ещё не добавил участников."}</small>${activeSceneView()==="gm"?`<button type="button" class="primary" data-open-scene-panel="add">Добавить участника</button>`:""}</div>`;
  board.innerHTML=cells+sceneMovementTracesSvg(space,actors)+empty;
  for(const key of Scene.targetCells||[])board.querySelector(`[data-scene-cell="${CSS.escape(key)}"]`)?.classList.add("targeted-cell");
  for(const object of Scene.objects.filter(item=>item.space===space.id&&item.metadata?.enemyModifier==="haven"))for(const key of object.cells||[])board.querySelector(`[data-scene-cell="${CSS.escape(key)}"]`)?.classList.add("haven-safe-cell");
  for(const actor of actors.filter(item=>Number(item.occupiedWidth||1)>1||Number(item.occupiedHeight||1)>1))for(let oy=0;oy<Number(actor.occupiedHeight||1);oy++)for(let ox=0;ox<Number(actor.occupiedWidth||1);ox++){const cell=board.querySelector(`[data-scene-cell="${Number(actor.x)+ox},${Number(actor.y)+oy}"]`);cell?.classList.add("giant-footprint-cell");cell?.style.setProperty("--giant-color",actor.tokenColor||"#902a3d")}
  for(const key of wispAuraCells)board.querySelector(`[data-scene-cell="${key}"]`)?.classList.add("wisp-aura");
  renderSceneWalls(board,walls);
}
function applyEnemyModifierVisuals(){
  const board=$("scene-board"),space=activeSceneSpace(),labels={contagion:"☣",earthquake:"≋",gargantuan:"▰",giant:"⬚",haven:"◇",isolation:"◌",vortex:"↻"};
  const markCell=(key,className)=>board.querySelector(`[data-scene-cell="${CSS.escape(key)}"]`)?.classList.add(className);
  for(const object of Scene.objects.filter(item=>item.space===space.id&&item.metadata?.enemyModifier)){const owner=Scene.actors.find(item=>item.id===object.ownerActorId),carrier=Scene.actors.find(item=>item.id===owner?.modifierState?.carrierId),bright=showAllModifierOverlays||[owner?.id,carrier?.id].includes(Scene.selectedActor)||Scene.activeActorId===carrier?.id,siblings=Scene.actors.filter(item=>item.profileId===owner?.profileId&&!item.knockedOut),alternate=Math.max(0,siblings.findIndex(item=>item.id===owner?.id))%3;for(const key of object.cells||[]){const cell=board.querySelector(`[data-scene-cell="${CSS.escape(key)}"]`);if(cell){cell.classList.add(`modifier-${object.metadata.enemyModifier}-cell`,"modifier-overlay",`modifier-overlay-alt-${alternate}`,bright?"modifier-overlay-active":"modifier-overlay-muted");cell.dataset.modifierOwner=owner?.id||""}}}
  for(const modifier of Scene.actors.filter(item=>String(item.profileId||"").startsWith("enemy.modifier.")&&!item.knockedOut)){
    const kind=modifier.profileId.split(".").at(-1),state=modifier.modifierState||{},carrier=Scene.actors.find(item=>item.id===state.carrierId),anchor=Scene.actors.find(item=>item.id===state.targetId);
    const bright=showAllModifierOverlays||[modifier.id,carrier?.id].includes(Scene.selectedActor)||Scene.activeActorId===carrier?.id,markOwned=(key,className)=>{const cell=board.querySelector(`[data-scene-cell="${CSS.escape(key)}"]`);if(cell){cell.classList.add(className,"modifier-overlay",bright?"modifier-overlay-active":"modifier-overlay-muted");cell.dataset.modifierOwner=modifier.id}};
    const tokenActor=carrier||modifier,token=document.querySelector(`[data-scene-actor="${CSS.escape(tokenActor.id)}"]`);
    if(token){token.classList.add("modifier-carrier",`modifier-${kind}`);if(kind==="giant")token.classList.add("giant-carrier-token");token.dataset.modifierGlyph=labels[kind]||"◆";token.title=`${token.title?`${token.title} · `:""}Модификатор: ${modifier.name}`}
    if(kind==="gargantuan"&&carrier){token?.remove();const body=Scene.objects.find(item=>item.ownerActorId===modifier.id&&item.metadata?.enemyModifier==="gargantuan-body"),compound=SceneEngine.compoundEnemyStatus(Scene,carrier.id),middle=Math.floor(Number(body?.cells?.length||1)/2);for(const[index,key]of(body?.cells||[]).entries()){const cell=board.querySelector(`[data-scene-cell="${CSS.escape(key)}"]`);if(!cell)continue;cell.insertAdjacentHTML("beforeend",`<button type="button" class="gargantuan-edge-segment ${state.mode||""} ${Scene.targetIds.includes(carrier.id)?"targeted":""} ${Scene.selectedActor===carrier.id?"selected":""}" data-scene-actor="${carrier.id}" aria-label="Тело Громадины ${esc(carrier.name)}, общие ЗД ${compound.hp} из ${compound.maxHp}" title="${esc(carrier.name)} · тело Громадины · общие ЗД ${compound.hp}/${compound.maxHp}">${index===middle?`<span>${esc(carrier.name)}<b>${compound.hp}/${compound.maxHp} ЗД</b></span>`:""}</button>`)} }
    const anchorToken=anchor&&document.querySelector(`[data-scene-actor="${CSS.escape(anchor.id)}"]`);if(anchorToken){anchorToken.classList.add("modifier-anchor",`modifier-anchor-${kind}`);anchorToken.dataset.modifierGlyph=labels[kind]||"◆"}
    if(anchor&&anchor.space===space.id&&["contagion","isolation"].includes(kind))for(let y=0;y<space.height;y++)for(let x=0;x<space.width;x++)if(Math.abs(anchor.x-x)+Math.abs(anchor.y-y)<=3)markOwned(`${x},${y}`,`modifier-${kind}-cell`);
    if(carrier&&carrier.space===space.id&&kind==="earthquake")for(let y=0;y<space.height;y++)for(let x=0;x<space.width;x++){const dx=Math.max(0,carrier.x-x,x-carrier.x-Math.max(1,Number(carrier.occupiedWidth||1))+1),dy=Math.max(0,carrier.y-y,y-carrier.y-Math.max(1,Number(carrier.occupiedHeight||1))+1),near=dx+dy<=2;if(state.mode==="outward"?near:!near)markOwned(`${x},${y}`,"modifier-earthquake-cell")}
    if(anchor&&anchor.space===space.id&&kind==="vortex"){const edges=[{name:"left",distance:anchor.x},{name:"right",distance:space.width-1-anchor.x},{name:"top",distance:anchor.y},{name:"bottom",distance:space.height-1-anchor.y}],farthest=Math.max(...edges.map(item=>item.distance)),active=new Set(edges.filter(item=>item.distance===farthest).map(item=>item.name));for(let y=0;y<space.height;y++)for(let x=0;x<space.width;x++)if(active.has("left")&&x===0||active.has("right")&&x===space.width-1||active.has("top")&&y===0||active.has("bottom")&&y===space.height-1)markOwned(`${x},${y}`,"modifier-vortex-edge")}
  }
}
const renderSceneBoardWithCrowdBase=renderSceneBoard;
renderSceneBoard=function(){renderSceneBoardWithCrowdBase();for(const actor of Scene.actors){const token=document.querySelector(`[data-scene-actor="${CSS.escape(actor.id)}"]`);if(!token)continue;if(actor.kind==="crowd"){token.classList.add("crowd");token.title=`Зона массовки · ${actor.name} · 1 ЗД`;token.setAttribute("draggable","false");continue}const compound=SceneEngine.compoundEnemyStatus(Scene,actor.id);if(compound.active){token.classList.add("compound");token.title=`Составной враг «${compound.id}» · общие ЗД ${compound.hp}/${compound.maxHp}`}}applyEnemyModifierVisuals()};
function renderSceneInspector(){
  const actor=Scene.actors.find(item=>item.id===Scene.selectedActor),space=activeSceneSpace();if(!actor&&activeSceneView()==="player"){$("scene-inspector").innerHTML=`<p class="autosave">Выберите видимого участника на поле. Скрытые правила и управление объектами доступны только Нарратору.</p>`;return}if(!actor){const objects=Scene.objects.filter(object=>object.space===space.id),markers=Scene.markers.filter(marker=>marker.space===space.id&&(marker.kind!=="hidden"||activeSceneView()==="gm")),cuts=(Scene.topology?.cuts||[]).filter(cut=>cut.space===space.id),cards=objects.map(object=>`<article class="scene-rule-card"><strong>${esc(object.label)}</strong><small>${SCENE_TYPE_NAMES[object.type]} · ${SCENE_DURATION_NAMES[object.duration]}${object.maxHp!=null?` · ЗД ${object.hp}/${object.maxHp}`:""}</small><span>${esc(object.source)}</span>${object.maxHp!=null?`<div class="button-row compact"><button type="button" data-scene-damage-object="${object.id}" data-damage="1">−1 ЗД</button><button type="button" data-scene-damage-object="${object.id}" data-damage="5">−5 ЗД</button></div>`:""}<button type="button" data-scene-remove-object="${object.id}">Удалить</button></article>`).concat(markers.map(marker=>{const type=sceneMarkerType(marker.kind);return`<article class="scene-rule-card marker-card"><strong><i style="--marker-color:${marker.color}">${type.symbol}</i> ${esc(marker.label)}</strong><small>${esc(type.name)} · ${SCENE_DURATION_NAMES[marker.duration]||"срок не указан"} · ${String.fromCharCode(65+marker.x)}${marker.y+1}</small><span>${esc(marker.source)}</span><button type="button" data-scene-remove-marker="${marker.id}">Удалить</button></article>`}),cuts.map(cut=>`<article class="scene-rule-card topology-card"><strong>${cut.crossing==="opposite"?"⇥":"▧"} ${esc(cut.label)}</strong><small>Удалено клеток: ${cut.cells.length} · ${cut.crossing==="opposite"?"телепорт насквозь":"непроходимо"}</small><span>${esc(cut.source)}</span><button type="button" data-scene-restore-topology="${cut.id}">Восстановить клетки</button></article>`));$("scene-inspector").innerHTML=cards.length?`<p>Области, маркеры и топология текущего поля.</p>${cards.join("")}`:`<p class="autosave">На этом поле пока нет выбранных участников или правил.</p>`;return}
  if(activeSceneView()==="player"){const effectNames=sceneActorEffects(actor).map(id=>sceneEffectList().find(effect=>effect.id===id)?.name||id);$("scene-inspector").innerHTML=`<article class="public-actor-card"><span class="kind">${actor.team==="enemy"?"ПРОТИВНИК":"СОЮЗНИК"}</span><h3>${esc(actor.name)}</h3><div class="scene-sheet-vitals"><span>ЗД <b>${actor.hp}/${actor.maxHp||"—"}</b></span><span>Состояние <b>${actor.knockedOut?"Вне боя":Scene.activeActorId===actor.id?"Сейчас Ход":actor.acted?"Действовал":"Готов"}</b></span></div><p>${effectNames.length?`Эффекты: ${effectNames.map(esc).join(" · ")}`:"Нет видимых Эффектов"}</p>${actor.heroId===S.id?`<button type="button" data-open-scene-panel="sheet">Открыть мой лист</button>`:""}</article>`;return}
  const profile=enemyProfile(actor.profileId),trait=antagonistTrait(actor.antagonistTraitId),defense=antagonistDefense(actor),effects=sceneEffectList(),available=new Map(SceneEngine.availableEnemyRules(Scene,D,actor.id).map(rule=>[rule.id,rule])),kindNames={action:"Действие",attack:"Атака",trump:"Козырь"};
  const enemyActions=profile?`<section class="scene-inspector-actions"><div class="scene-inspector-identity"><span class="kind">ПРОТИВНИК · СТУПЕНЬ ${actor.tier}</span><h3>${esc(profile.name)}</h3><p>${esc([profile.tags,trait?`Черта: ${trait.name}`:"",enemyRuleStateLabel(actor)].filter(Boolean).join(" · "))}</p></div>${defense?`<article class="enemy-trait-defense"><span>РЕАКЦИЯ ЗАЩИТЫ · ${esc(trait.name)}</span><strong>${esc(defense.name)}</strong><p>${md(defense.text)}</p><small>При подходящей Атаке появится автоматически в списке ответов.</small></article>`:""}${profile.passive?`<p class="enemy-passive"><b>Пассив:</b> ${md(profile.passive)}</p>`:""}<div class="enemy-rule-list">${(profile.rules||[]).map(rule=>{const state=available.get(rule.id)||rule,automation=enemyAutomationName(state.automation),meta=[kindNames[rule.kind]||rule.kind,`${rule.apCost||1} ОД`,automation,rule.kind==="trump"?`Н${rule.tension}`:"",rule.dice?`${rule.dice}D6`:"",rule.directDamage?`${rule.directDamage} урона`:"",rule.area?.length?`${rule.area[0]}×${rule.area[1]}`:""].filter(Boolean).join(" · ");return `<article class="enemy-rule ${rule.kind}"><button type="button" data-enemy-rule="${esc(rule.id)}" data-enemy-actor="${actor.id}" ${state.available?"":"disabled"} title="${esc(state.reason||rule.reward||rule.text)}"><strong>${esc(rule.name)}</strong><small>${esc(meta)}</small></button><details><summary>Правило</summary><p>${md(rule.text)}</p>${rule.reward?`<p><b>Награда:</b> ${md(rule.reward)}</p>`:""}</details>${enemyRuleOptionsHtml(rule,actor,state)}</article>`}).join("")}</div></section>`:"";
  $("scene-inspector").innerHTML=`${enemyActions}<details class="scene-inspector-settings" ${profile?"":"open"}><summary>${profile?"Настроить участника":"Параметры участника"}</summary><div class="scene-inspector-settings-body"><label>Имя<input data-scene-actor-name="${actor.id}" value="${esc(actor.name)}"></label>${actor.team==="enemy"?`<label>Черта Антагониста<select data-scene-antagonist-trait="${actor.id}"><option value="">Без Черты</option>${antagonistTraits().map(item=>`<option value="${item.id}" ${item.id===actor.antagonistTraitId?"selected":""}>${esc(item.name)}</option>`).join("")}</select><small>Обычные НПС не получают базовые защиты игроков.</small></label>`:""}<div class="scene-stat-grid"><label>Здоровье<input type="number" min="0" max="9999" data-scene-actor-hp="${actor.id}" value="${actor.hp}"></label><span><small>Максимум</small><strong>${actor.maxHp||"—"}</strong></span><span><small>Скорость</small><strong>${actor.speed}</strong></span><span><small>Броня / Уклонение</small><strong>${actor.armor} / ${actor.evasion}</strong></span></div><fieldset class="token-editor"><legend>Медиа участника</legend><label>Знак<select data-scene-token-symbol="${actor.id}">${TOKEN_SYMBOLS.map(symbol=>`<option value="${symbol}" ${symbol===actor.tokenSymbol?"selected":""}>${symbol||"Авто по имени"}</option>`).join("")}</select></label><label>Цвет<input type="color" data-scene-token-color="${actor.id}" value="${actor.tokenColor}"></label><label class="token-upload">Изображение токена<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" data-scene-token-upload="${actor.id}"><small>Компаньон обрежет до 192×192.</small></label>${actor.tokenImage?`<button type="button" data-scene-clear-token-image="${actor.id}">Вернуть знак токена</button>`:""}<label class="portrait-upload">Портрет для галереи<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" data-scene-portrait-upload="${actor.id}"></label>${actor.portraitImage?`<button type="button" data-scene-clear-portrait-image="${actor.id}">Убрать портрет</button>`:""}</fieldset><label>Пространство<select data-scene-actor-space="${actor.id}">${Scene.spaces.map(item=>`<option value="${item.id}" ${item.id===actor.space?"selected":""}>${esc(item.name)}</option>`).join("")}</select></label><label class="switch"><input type="checkbox" data-scene-actor-acted="${actor.id}" ${actor.acted?"checked":""}><span>Уже действовал в Раунде</span></label><div class="button-row compact"><button type="button" data-scene-turn="start" data-scene-actor-id="${actor.id}">Начать Ход</button><button type="button" data-scene-turn="end" data-scene-actor-id="${actor.id}">Завершить Ход</button></div><h3>Эффекты</h3><div class="chip-row">${effects.map(effect=>{const status=SceneEngine.effectStatus(Scene,actor.id,effect.id);return`<button type="button" class="effect-chip ${status.active?"on":""}" data-scene-actor-effect="${actor.id}" data-effect="${effect.id}" data-effect-duration="${status.duration||""}" title="${esc(sceneEffectTitle(actor,effect))}">${esc(effect.name)}${status.direct&&!status.removable?" · 🔒":""}</button>`}).join("")}</div>${profile?`<details class="rules-details"><summary>${esc(profile.name)} · полный профиль</summary><p>${md(profile.statsRaw)}</p><p>${md(profile.text)}</p></details>`:""}${trait?`<details class="rules-details"><summary>${esc(trait.name)} · полная Черта</summary>${trait.rules.map(rule=>`<p><b>${esc(rule.name)} (${esc(rule.trigger)})</b>: ${md(rule.text)}</p>`).join("")}</details>`:""}<button type="button" class="remove-wide" data-scene-remove-actor="${actor.id}">Убрать из Сцены</button></div></details>`;
}
function sceneNarratorBasicActionsHtml(actor){
  const actions=SceneEngine.availableActions(Scene,D,actor.id).filter(action=>!action.reaction);
  return `<section class="gm-manual-console gm-basic-actions"><header><div><span class="kind">ДЕЙСТВИЯ НАРРАТОРА</span><h3>Базовые действия · ${esc(actor.name)}</h3></div><small>Цели берутся с поля</small></header><div class="core-action-list">${actions.map(action=>`<button type="button" class="planned-action" data-gm-core-action="${esc(action.id)}" data-gm-actor="${actor.id}" ${action.available?"":"disabled"} title="${esc(action.reason||action.text)}"><strong>${esc(action.continuation?`Шаг · ${action.remaining} кл.`:action.name)}</strong><small>${esc(action.cost||action.group||"")}</small></button>`).join("")}</div></section>`;
}
function sceneNarratorTechniquesHtml(actor){
  const coverage=TechniqueEngine.techniqueCoverage(D,actor.techniques||{}),ready=Scene.activeActorId===actor.id&&!actor.knockedOut;
  if(!coverage.length)return"";
  return `<details class="gm-manual-console gm-technique-console" data-gm-technique-actor="${actor.id}" open><summary><span><span class="kind">ТЕХНИКИ НАРРАТОРА</span><strong>Техники · ${esc(actor.name)}</strong></span><small>${coverage.filter(entry=>entry.automation!=="manual").length} автоматизировано</small></summary><div class="core-techniques">${coverage.map(entry=>`<article><span class="automation-${entry.automation}">${entry.automation==="full"?"авто":entry.automation==="decision"?"выбор":entry.automation==="partial"?"частично":"вручную"}</span><div><strong>${automationBadge(entry.automation)}${esc(entry.techniqueName)} · ${entry.level}: ${esc(entry.name)}</strong><p>${md(entry.text)}</p><div class="technique-buttons">${techniqueButtonsHtml(entry,actor,ready)}</div></div></article>`).join("")}</div></details>`;
}
function sceneNarratorConsoleHtml(actor){
  const effectData=sceneEffects(),effects=sceneEffectList(),activeEffects=effects.filter(effect=>(actor.effects||[]).includes(effect.id)),targets=Scene.actors.filter(item=>Scene.targetIds.includes(item.id)),usesWounds=actor.kind==="hero"||Boolean(actor.heroId),durationOptions=[["default","По обычному правилу"],["roundEnd","До конца Раунда"],["startTurn","До начала следующего Хода"],["actionOrStartTurn","До действия или следующего Хода"],["scene","До конца Сцены"],["persistent","Постоянно"]];
  const effectOptions=group=>effectData[group].map(effect=>`<option value="${esc(effect.id)}">${esc(effect.name)}</option>`).join("");
  const resource=(id,label,value)=>`<div class="gm-resource-control"><span>${label} <b>${Number(value||0)}</b></span><div><button type="button" data-gm-resource="${id}" data-gm-actor="${actor.id}" data-delta="-1" aria-label="Уменьшить ${label}">−</button><button type="button" data-gm-resource="${id}" data-gm-actor="${actor.id}" data-delta="1" aria-label="Увеличить ${label}">+</button></div></div>`;
  return `<section class="gm-manual-console"><header><div><span class="kind">РУЧНОЕ РАЗРЕШЕНИЕ</span><h3>${esc(actor.name)}</h3></div><small>Все изменения записываются и отменяются</small></header><div class="gm-health-console"><div><span>Здоровье</span><strong>${actor.hp}/${actor.maxHp||"—"}</strong></div><div class="gm-health-buttons"><button type="button" data-gm-health="damage" data-gm-actor="${actor.id}" data-amount="5">−5 урона</button><button type="button" data-gm-health="damage" data-gm-actor="${actor.id}" data-amount="1">−1</button><button type="button" data-gm-health="heal" data-gm-actor="${actor.id}" data-amount="1">+1</button><button type="button" data-gm-health="heal" data-gm-actor="${actor.id}" data-amount="5">+5 лечения</button></div>${actor.knockedOut?`<button type="button" class="primary" data-gm-revive="${actor.id}">Вернуть в бой с 1 ЗД</button>`:""}</div><div class="gm-resource-grid">${usesWounds?resource("wounds","Раны",actor.wounds):""}${resource("ap","ОД",actor.ap)}${actor.team==="hero"?resource("focus","Фокус",actor.focus)+resource("influence","Влияние",actor.influence):""}</div><div class="gm-effect-console"><div class="gm-console-heading"><strong>Эффекты</strong><small>Выберите эффект и срок; активные снимаются одной кнопкой</small></div><div class="gm-effect-form"><label>Эффект<select data-gm-effect-select="${actor.id}"><optgroup label="Положительные">${effectOptions("positive")}</optgroup><optgroup label="Негативные">${effectOptions("negative")}</optgroup></select></label><label>Длительность<select data-gm-effect-duration="${actor.id}">${durationOptions.map(([value,label])=>`<option value="${value}">${label}</option>`).join("")}</select></label><button type="button" class="primary" data-gm-effect-apply="${actor.id}">Наложить</button></div><div class="gm-active-effects">${activeEffects.map(effect=>{const status=SceneEngine.effectStatus(Scene,actor.id,effect.id);return`<button type="button" data-gm-effect-remove="${actor.id}" data-effect="${esc(effect.id)}" title="Снять ${esc(effect.name)}"><span>${esc(effect.name)}</span><small>${esc(status.expiresAt||"активен")}</small><b>×</b></button>`}).join("")||`<span class="autosave">Активных Эффектов нет.</span>`}</div></div><details class="gm-manual-rule"><summary>Зафиксировать неавтоматизированное правило</summary><label>Название Техники или правила<input data-gm-manual-name="${actor.id}" maxlength="120" placeholder="Например: Импровизированный ритуал"></label><label>Как разрешено<textarea data-gm-manual-note="${actor.id}" maxlength="800" rows="3" placeholder="Результат, выбранные цели, договорённость Нарратора…"></textarea></label><p>Отмеченные цели: ${targets.length?targets.map(target=>esc(target.name)).join(", "):"нет"}</p><button type="button" data-gm-manual-resolve="${actor.id}">Записать ручное разрешение</button></details></section>`;
}
function sceneQuickEffectsHtml(actor){
  if(!actor)return"";
  const group=(effects,kind,symbol,label)=>`<div class="scene-quick-effect-group ${kind}" aria-label="${label}"><i aria-hidden="true">${symbol}</i><div>${effects.map(effect=>{const status=SceneEngine.effectStatus(Scene,actor.id,effect.id);return`<button type="button" class="effect-chip ${status.active?"on":""}" data-scene-actor-effect="${actor.id}" data-effect="${effect.id}" data-effect-duration="${status.duration||""}" title="${esc(sceneEffectTitle(actor,effect))}" aria-pressed="${status.active}">${esc(effect.name)}${status.direct&&!status.removable?` · 🔒`:""}</button>`}).join("")}</div></div>`;
  const effects=sceneEffects();return `<section class="scene-quick-effects" aria-label="Быстрые Эффекты для ${esc(actor.name)}">${group(effects.positive,"positive","+","Положительные Эффекты")}${group(effects.negative,"negative","−","Отрицательные Эффекты")}</section>`;
}
function sceneInspectorSpacesHtml(){
  if(Scene.spaces.length<2)return"";
  return `<nav class="scene-inspector-spaces" aria-label="Открытые поля Сцены">${Scene.spaces.map(space=>`<button type="button" data-scene-inspector-space="${space.id}" class="${space.id===Scene.activeSpace?"on":""}" aria-current="${space.id===Scene.activeSpace?"true":"false"}" title="Открыть поле «${esc(space.name)}»"><span>${esc(space.name)}</span><small>${Scene.actors.filter(actor=>actor.space===space.id&&!actor.knockedOut).length}</small></button>`).join("")}</nav>`;
}
const baseRenderSceneInspectorV27=renderSceneInspector;
function enemyModifierActionHtml(actor){const actions={"enemy.modifier.gargantuan":["gargantuan-strike","Атака Громадины"],"enemy.modifier.giant":["giant-charge","Рывок Гиганта"],"enemy.modifier.legion":["legion-return","Вернуть павших / создать массовку"]},entry=actions[actor.profileId];if(!entry)return"";const carrier=Scene.actors.find(item=>item.id===actor.modifierState?.carrierId),turnBound=["gargantuan-strike","giant-charge"].includes(entry[0]),used=Number(actor.modifierState?.lastActionRound||0)===Number(Scene.round||1),ready=!used&&(!turnBound||Scene.activeActorId===carrier?.id),picking=activeModifierActionId===actor.id;return`<article class="scene-rule-card enemy-modifier-action"><strong>${turnBound?"Дополнительное действие носителя":"Действие модификатора"} · бесплатно</strong><small>${turnBound?`Только в Ход ${esc(carrier?.name||"носителя")} · один раз за Раунд`:"Один раз за Раунд · отдельный Ход не требуется"}</small><span>${entry[0]==="gargantuan-strike"?"Выберите до двух зон 2×2: в режиме разметки клик задаёт левую верхнюю клетку каждой зоны.":entry[0]==="giant-charge"?"Выберите конечную левую верхнюю клетку тела 2×2 на прямой до 4 клеток.":"В режиме разметки кликайте павших врагов и свободные клетки края; требуется по одному месту на каждое возвращение, а остаток станет массовкой."}</span><button type="button" class="${picking?"primary":""}" data-modifier-action-picker="${entry[0]}" data-modifier-actor="${actor.id}" ${ready?"":"disabled"}>${picking?"Разметка включена":"Выбрать на поле"}</button><button type="button" data-modifier-action="${entry[0]}" data-modifier-actor="${actor.id}" ${ready?"":"disabled"}>${used?"Уже использовано в этом Раунде":entry[1]}</button></article>`}
function enemyModifierConsoleHtml(actor){
  if(!SceneEngine.isEnemyModifier?.(actor))return"";const state=actor.modifierState||{},draftMode=modifierModeDrafts.get(actor.id)||state.mode,attached=SceneEngine.isAttachedModifier(actor),carrierOptions=Scene.actors.filter(item=>item.id!==actor.id&&item.team===actor.team&&!item.knockedOut&&item.kind!=="crowd"&&!String(item.profileId||"").startsWith("enemy.modifier.")),needsTarget=["enemy.modifier.contagion","enemy.modifier.isolation","enemy.modifier.vortex"].includes(actor.profileId),needsCells=["enemy.modifier.artillery","enemy.modifier.haven","enemy.modifier.collateral"].includes(actor.profileId),targetOptions=Scene.actors.filter(item=>item.id!==actor.id&&item.team!==actor.team&&!item.knockedOut&&item.kind!=="crowd"&&!SceneEngine.isEnemyModifier?.(item)),carrier=Scene.actors.find(item=>item.id===state.carrierId),editingCells=activeModifierPickerId===actor.id,selectedCells=editingCells?Scene.targetCells:(state.cells||[]),playerCount=targetOptions.length,cellHint=actor.profileId==="enemy.modifier.collateral"?`Нужно ${Math.ceil(playerCount*1.5)} · выбрано ${selectedCells.length}`:actor.profileId==="enemy.modifier.haven"?`Нужна область 3×3 · выбрано ${selectedCells.length}`:`Выбрано клеток: ${selectedCells.length}`,modes=actor.profileId==="enemy.modifier.earthquake"?[["inward","Внутрь · бьёт дальше 2"],["outward","Наружу · бьёт в пределах 2"]]:actor.profileId==="enemy.modifier.artillery"?[["square3","Область 3×3"],["rect2x5","Область 2×5"],["lines","Две несмежные бесконечные Линии"],["edges","Все края поля"]]:actor.profileId==="enemy.modifier.gargantuan"?[["left","Расширить слева"],["right","Расширить справа"],["top","Расширить сверху"],["bottom","Расширить снизу"]]:[];
  return`<article class="scene-rule-card enemy-modifier-console ${editingCells?"is-picking-cells":""}" data-enemy-modifier-console="${actor.id}"><strong>Автоматизация врага‑Модификатора</strong><small>${attached?"Враг‑Техника · прикрепляется к носителю":"Развёртываемый модификатор"}</small><label class="switch"><input type="checkbox" data-modifier-show-all ${showAllModifierOverlays?"checked":""}><span>Всегда показывать все области Нарратору</span></label>${attached?`<label>Носитель<select data-modifier-carrier="${actor.id}"><option value="">Выберите врага</option>${carrierOptions.map(item=>`<option value="${item.id}" ${item.id===state.carrierId?"selected":""}>${esc(item.name)}</option>`).join("")}</select></label>`:""}${modes.length?`<label>Режим<select data-modifier-mode="${actor.id}">${modes.map(([id,label])=>`<option value="${id}" ${id===state.mode?"selected":""}>${label}</option>`).join("")}</select></label>`:""}${needsTarget?`<label>Цель правила<select data-modifier-target="${actor.id}"><option value="">Выберите персонажа</option>${targetOptions.map(item=>`<option value="${item.id}" ${item.id===state.targetId?"selected":""}>${esc(item.name)}</option>`).join("")}</select><small>Выбирается здесь и не зависит от общего инструмента целей.</small></label>`:""}${needsCells?`<div class="modifier-cell-picker"><span><b>${cellHint}</b><small>${editingCells?actor.profileId==="enemy.modifier.haven"?"Наведите и кликните центральную клетку — зона 3×3 заполнится сама.":"Кликайте по любым клеткам поля — пустым или занятым. Повторный клик снимает выбор.":"Откройте режим разметки; Shift не требуется."}</small></span><div class="button-row compact"><button type="button" class="${editingCells?"primary":""}" data-modifier-cell-picker="${actor.id}">${editingCells?"Готово":"Редактировать клетки"}</button><button type="button" data-modifier-cell-clear="${actor.id}" ${selectedCells.length?"":"disabled"}>Очистить</button></div></div>`:""}<button type="button" class="primary" data-modifier-configure="${actor.id}">${state.configuredRound===Scene.round?"Изменить настройку":"Подтвердить настройку"}</button>${attached&&carrier?`<small>Прикреплён к: ${esc(carrier.name)}. Состояние сохраняется и передаётся при переподключении.</small>`:""}</article>`;
}
renderSceneInspector=function(){
  baseRenderSceneInspectorV27();
  const actor=Scene.actors.find(item=>item.id===Scene.selectedActor),root=$("scene-inspector"),profile=enemyProfile(actor?.profileId),available=actor?SceneEngine.availableEnemyRules(Scene,D,actor.id):[],hasAutomatedAttack=available.some(rule=>rule.kind==="attack"&&rule.automation==="attack");
  const controlledModifiers=actor?(SceneEngine.isEnemyModifier?.(actor)?[actor]:Scene.actors.filter(item=>!item.knockedOut&&SceneEngine.isEnemyModifier?.(item)&&item.modifierState?.carrierId===actor.id)):[];
  if(controlledModifiers.length&&activeSceneView()==="gm")root.insertAdjacentHTML("afterbegin",controlledModifiers.map(modifier=>enemyModifierConsoleHtml(modifier)+enemyModifierActionHtml(modifier)).join(""));
  for(const modifier of controlledModifiers){const draft=modifierModeDrafts.get(modifier.id),select=root.querySelector(`[data-modifier-mode="${CSS.escape(modifier.id)}"]`);if(draft&&select)select.value=draft}
  if(["enemy.modifier.haven","enemy.modifier.artillery"].includes(actor?.profileId)&&activeSceneView()==="gm")root.insertAdjacentHTML("afterbegin",`<article class="scene-rule-card"><strong>Быстрый конструктор области</strong><span>Нажмите форму, затем кликните её центр на поле. Режим Артиллерии переключится автоматически.</span><div class="button-row compact"><button type="button" data-modifier-area-preset="square3" data-modifier-actor="${actor.id}">3×3 под мышью</button>${actor.profileId==="enemy.modifier.artillery"?`<button type="button" data-modifier-area-preset="rect-h" data-modifier-actor="${actor.id}">5×2 ↔</button><button type="button" data-modifier-area-preset="rect-v" data-modifier-actor="${actor.id}">2×5 ↕</button><button type="button" data-modifier-area-preset="line-h" data-modifier-actor="${actor.id}">Линия ↔</button><button type="button" data-modifier-area-preset="line-v" data-modifier-actor="${actor.id}">Линия ↕</button><button type="button" data-modifier-area-preset="edges" data-modifier-actor="${actor.id}">Все края</button>`:""}</div></article>`);
  if(actor?.profileId==="enemy.modifier.collateral")root.insertAdjacentHTML("afterbegin",`<article class="scene-rule-card collateral-rescue-card"><strong>Спасти Случайную жертву</strong><span>${activeSceneView()==="gm"?"Выберите смежного героя целью.":"Ваш персонаж должен стоять рядом."} Взаимодействие тратит 1 ОД; успешное Испытание убирает жертву без продвижения Часов.</span><label>Кости Испытания<input type="number" min="1" max="99" value="3" data-collateral-rescue-pool></label><button type="button" class="primary" data-collateral-rescue="${actor.id}">Взаимодействовать и бросить</button></article>`);
  if(profile){const stats=root.querySelector(".scene-stat-grid");stats?.insertAdjacentHTML("beforebegin",`<label>Сторона<select data-scene-actor-team="${actor.id}"><option value="enemy" ${actor.team==="enemy"?"selected":""}>Противники</option><option value="hero" ${actor.team==="hero"?"selected":""}>Союзники героев</option></select><small>Профиль и его действия сохранятся; изменятся цели, развёртывание и условия победы.</small></label>`);if(!root.querySelector("[data-scene-antagonist-trait]"))stats?.insertAdjacentHTML("beforebegin",`<label>Черта Антагониста<select data-scene-antagonist-trait="${actor.id}"><option value="">Без Черты</option>${antagonistTraits().map(item=>`<option value="${item.id}" ${item.id===actor.antagonistTraitId?"selected":""}>${esc(item.name)}</option>`).join("")}</select><small>Профильные НПС используют реакции своего профиля, а не защиты героев.</small></label>`);const kind=root.querySelector(".scene-inspector-identity .kind");if(kind)kind.textContent=`${actor.team==="hero"?"СОЮЗНЫЙ ПРОФИЛЬ":"ПРОТИВНИК"} · СТУПЕНЬ ${actor.tier}`}
  if(profile&&actor.kind!=="crowd")root.querySelector(".scene-stat-grid")?.insertAdjacentHTML("beforebegin",`<label>Группа Составного врага<input data-scene-compound-id="${actor.id}" value="${esc(actor.compoundId||"")}" maxlength="120" placeholder="Пусто — отдельный враг"><small>Части с одинаковой группой и стороной делят клетку и перемещаются вместе.</small></label>`);
  const compound=actor&&SceneEngine.compoundEnemyStatus(Scene,actor.id);if(compound?.active){const hpInput=root.querySelector(`[data-scene-actor-hp="${CSS.escape(actor.id)}"]`);if(hpInput){hpInput.value=compound.hp;hpInput.max=compound.maxHp;const maximum=hpInput.closest("label")?.nextElementSibling?.querySelector("strong");if(maximum)maximum.textContent=compound.maxHp}root.querySelector(".scene-stat-grid")?.insertAdjacentHTML("beforebegin",`<article class="scene-rule-card compound-summary"><strong>Составной враг · ${esc(compound.id)}</strong><small>${compound.parts.length} части · общие ЗД ${compound.hp}/${compound.maxHp} · порог ${compound.gate}</small><span>Скорость ${compound.speed} · защита: ${compound.defenseType==="armor"?`Броня ${compound.armor}`:`Уклонение ${compound.evasion}`}. Каждая часть совершает отдельный Ход; весь состав считается одной целью.</span>${compound.defenseTied?`<label>Равная защита — выберите одну<select data-scene-compound-defense="${actor.id}"><option value="armor" ${compound.defenseType==="armor"?"selected":""}>Броня ${compound.armorMax}</option><option value="evasion" ${compound.defenseType==="evasion"?"selected":""}>Уклонение ${compound.evasionMax}</option></select></label>`:`<small>Автоматически выбрано большее значение: ${compound.defenseType==="armor"?"Броня":"Уклонение"}.</small>`}</article>`)}
  if(actor?.kind==="crowd"){const move=SceneEngine.fodderMoveStatus(Scene,actor.id),space=Scene.spaces.find(item=>item.id===actor.space),directions=[["↑",0,-1],["→",1,0],["↓",0,1],["←",-1,0]],victims=Scene.actors.filter(item=>item.team!==actor.team&&!item.knockedOut&&item.space===actor.space&&Math.abs(item.x-actor.x)+Math.abs(item.y-actor.y)<=1);root.querySelector("[data-scene-antagonist-trait]")?.closest("label")?.remove();root.insertAdjacentHTML("afterbegin",`<article class="scene-rule-card crowd-inspector"><strong>Зона массовки · ${esc(actor.name)}</strong><small>Обычная Зона · 1 ЗД · без Хода</small><span>Вражеская трудная местность; союзникам не мешает и может пересекаться с персонажами.</span><div class="button-row compact">${directions.map(([label,dx,dy])=>{const x=actor.x+dx,y=actor.y+dy,valid=move.available&&x>=0&&y>=0&&x<space.width&&y<space.height;return`<button type="button" data-fodder-move="${actor.id}" data-dx="${dx}" data-dy="${dy}" ${valid?"":"disabled"}>${label}</button>`}).join("")}</div><small>${move.available?`После последнего Хода врага осталось движения: ${move.remaining}`:esc(move.reason)}</small>${victims.length?`<div class="button-stack">${victims.map(target=>`<button type="button" class="danger-quiet" data-fodder-damage="${actor.id}" data-target="${target.id}">2 урона: ${esc(target.name)}</button>`).join("")}</div><small>Урон массовки необязателен и доступен в конце Раунда.</small>`:""}</article>`)}
  if(actor?.kind==="crowd"){const group=Scene.actors.filter(item=>item.kind==="crowd"&&item.crowdGroupId===actor.crowdGroupId),alive=group.filter(item=>!item.knockedOut),styles=[["mob","Отряд"],["swarm","Рой"],["guards","Стража"],["undead","Нежить"],["hounds","Звери"],["civilians","Толпа"],["custom","Свой вид"]],directions=[["↑",0,-1],["→",1,0],["↓",0,1],["←",-1,0]];root.insertAdjacentHTML("afterbegin",`<article class="scene-rule-card crowd-group-console"><strong>Группа «${esc(actor.name)}»</strong><small>${alive.length} активных / ${group.length} всего · ${actor.team==="enemy"?"противники":"союзники героев"}</small><label>Оформление группы<select data-crowd-group-style="${actor.id}">${styles.map(([id,name])=>`<option value="${id}" ${id===(actor.crowdType||"mob")?"selected":""}>${name}</option>`).join("")}</select></label><div class="button-row compact"><button type="button" data-crowd-add-group="${actor.id}">+ зоны кистью</button><button type="button" data-crowd-remove-defeated="${actor.id}" ${group.length===alive.length?"disabled":""}>Убрать павшие</button></div><span>Сдвинуть всю группу после Хода врага:</span><div class="button-row compact">${directions.map(([label,dx,dy])=>`<button type="button" data-fodder-group-move="${actor.id}" data-dx="${dx}" data-dy="${dy}">${label}</button>`).join("")}</div><button type="button" class="danger-quiet" data-crowd-remove-group="${actor.id}">Убрать всю группу</button></article>`)}
  if(actor?.crowdSubtype==="seeker"){const target=Scene.actors.find(item=>item.id===actor.seekerTargetId);root.insertAdjacentHTML("afterbegin",`<article class="scene-rule-card"><strong>Особая Зона · Ищейка Псаря</strong><small>Цель: ${esc(target?.name||"цель недоступна")} · взрыв ${Number(actor.seekerDamage||7)} урона</small><span>Когда Ищейка после движения окажется смежной со своей целью, она исчезнет и автоматически нанесёт урон всем смежным персонажам.</span></article>`)}
  if(profile){root.querySelector(".scene-inspector-identity h3")?.insertAdjacentHTML("afterbegin",automationBadge(enemyProfileAutomation(profile)));for(const button of root.querySelectorAll("[data-enemy-rule]")){const status=available.find(rule=>rule.id===button.dataset.enemyRule)?.automation||"assisted";button.querySelector("strong")?.insertAdjacentHTML("afterbegin",automationBadge(status))}}
  if(actor?.kind==="crowd"){root.querySelector("[data-scene-actor-acted]")?.closest("label")?.remove();root.querySelector("[data-scene-portrait-upload]")?.closest("label")?.remove();root.querySelector("[data-scene-turn]")?.closest(".button-row")?.remove()}
  if(actor&&actor.kind!=="crowd"&&!SceneEngine.isEnemyModifier?.(actor)&&activeSceneView()==="gm")root.insertAdjacentHTML("afterbegin",sceneNarratorConsoleHtml(actor)+sceneNarratorBasicActionsHtml(actor)+sceneNarratorTechniquesHtml(actor));
  if(profile&&Scene.activeActorId===actor.id&&hasAutomatedAttack){const controls=attackModifierControlsHtml(actor);if(controls)root.insertAdjacentHTML("afterbegin",controls)}
  if(actor&&activeSceneView()==="gm"&&usingNextSceneInterface()){
    root.querySelector(".gm-effect-console")?.remove();
    const settings=root.querySelector(".scene-inspector-settings-body"),heading=[...(settings?.children||[])].find(element=>element.tagName==="H3"&&element.textContent.trim()==="Эффекты"),chips=heading?.nextElementSibling;
    if(chips?.classList.contains("chip-row"))chips.remove();heading?.remove();
    root.insertAdjacentHTML("afterbegin",sceneQuickEffectsHtml(actor));
  }
  if(usingNextSceneInterface()){const spaces=sceneInspectorSpacesHtml();if(spaces)root.insertAdjacentHTML("afterbegin",spaces)}
  if(!actor&&activeSceneView()==="gm"){const options=(current,marker=false)=>[...(marker?[]:[["instant","Мгновенно"]]),["endTurn","До конца Хода"],["nextTurn","До начала следующего Хода"],["round","До конца Раунда"],["scene","До конца Сцены"],["persistent","Постоянно"]].map(([value,label])=>`<option value="${value}" ${value===current?"selected":""}>${label}</option>`).join("");for(const button of root.querySelectorAll("[data-scene-remove-object]")){const object=Scene.objects.find(item=>item.id===button.dataset.sceneRemoveObject);if(object)button.insertAdjacentHTML("beforebegin",`<label class="scene-duration-editor">Срок<select data-scene-object-duration="${object.id}">${options(object.duration)}</select></label>`)}for(const button of root.querySelectorAll("[data-scene-remove-marker]")){const marker=Scene.markers.find(item=>item.id===button.dataset.sceneRemoveMarker);if(marker)button.insertAdjacentHTML("beforebegin",`<label class="scene-duration-editor">Срок<select data-scene-marker-duration="${marker.id}">${options(marker.duration,true)}</select></label>`)}}
};
function renderSceneManager(){
  const root=$("scene-space-manager");if(!root)return;
  const modeNames={standard:"Стандарт 7×7",cinematic:"Кинематографичный 1×7",custom:"Авторский"};root.innerHTML=Scene.spaces.map(space=>{const actors=Scene.actors.filter(actor=>actor.space===space.id).length,objects=Scene.objects.filter(object=>object.space===space.id).length,markers=Scene.markers.filter(marker=>marker.space===space.id).length,cuts=(Scene.topology?.cuts||[]).filter(cut=>cut.space===space.id).length,owner=space.ownerActorId?Scene.actors.find(actor=>actor.id===space.ownerActorId):null,ownerLabel=owner?` · Создал: ${esc(owner.name)}`:"";return`<article class="${space.id===Scene.activeSpace?"on":""}"><button type="button" data-manage-space-open="${space.id}"><strong>${esc(space.name)}</strong><small>${modeNames[space.mode]||modeNames.custom}${ownerLabel} · ${actors} уч. · ${objects+markers+cuts} объектов</small></button>${space.id===Scene.activeSpace?`<select data-manage-space-mode="${space.id}" aria-label="Режим поля"><option value="standard" ${space.mode==="standard"?"selected":""}>Стандарт 7×7</option><option value="cinematic" ${space.mode==="cinematic"?"selected":""}>Кинематографичный 1×7</option><option value="custom" ${space.mode==="custom"?"selected":""}>Авторский</option></select>`:""}${space.id!=="main"?`<button type="button" class="danger-quiet" data-manage-space-remove="${space.id}" aria-label="Удалить ${esc(space.name)}">×</button>`:`<span title="Основное поле нельзя удалить">основное</span>`}</article>`}).join("");
}
function appendSceneWallInspector(){
  if(Scene.selectedActor||activeSceneView()!=="gm")return;
  const walls=(Scene.walls||[]).filter(wall=>wall.space===Scene.activeSpace),root=$("scene-inspector");if(!walls.length)return;
  root.insertAdjacentHTML("beforeend",`<h3>Стены</h3>${walls.map(wall=>`<article class="scene-rule-card wall-card"><strong>┃ ${esc(wall.label)}</strong><small>Стена · ЗД ${wall.hp}/${wall.maxHp}</small><span>${esc(wall.source)}</span><div class="button-row compact"><button type="button" data-scene-damage-wall="${wall.id}" data-damage="1">−1</button><button type="button" data-scene-damage-wall="${wall.id}" data-damage="5">−5</button><button type="button" data-scene-restore-wall="${wall.id}" data-amount="1">+1</button><button type="button" data-scene-restore-wall="${wall.id}" data-amount="5">+5</button><button type="button" data-scene-remove-wall="${wall.id}">Удалить</button></div></article>`).join("")}`);
}
function appendTerrainRepairControls(){if(Scene.selectedActor||activeSceneView()!=="gm")return;for(const button of $("scene-inspector").querySelectorAll("[data-scene-remove-object]")){const object=Scene.objects.find(item=>item.id===button.dataset.sceneRemoveObject);if(object?.maxHp!=null)button.insertAdjacentHTML("beforebegin",`<div class="button-row compact"><button type="button" data-scene-restore-object="${object.id}" data-amount="1">+1 ЗД</button><button type="button" data-scene-restore-object="${object.id}" data-amount="5">+5 ЗД</button></div>`)}}
function sceneMediaSize(){return Scene.actors.reduce((sum,actor)=>sum+(actor.tokenImage?.length||0)+(actor.portraitImage?.length||0),0)+Scene.artworks.reduce((sum,art)=>sum+art.image.length,0)}
function applySceneBackground(){
  const playerView=activeSceneView()==="player",background=Scene.artworks.find(art=>art.id===Scene.backgroundArt&&(!playerView||!art.hidden)),board=$("scene-board"),wrap=$("scene-board-wrap"),view=Scene.backgroundView;
  if(!board||!wrap)return;wrap.classList.remove("has-background");wrap.style.backgroundImage="";board.classList.toggle("has-map",Boolean(background));board.style.backgroundImage=background?`linear-gradient(rgba(8,8,11,${view.dim/100}),rgba(8,8,11,${view.dim/100})),url("${background.image}")`:"";board.style.backgroundSize=background?view.fit:"";board.style.backgroundPosition=background?view.position:"";board.style.backgroundRepeat=background?"no-repeat":"";board.style.setProperty("--scene-grid-opacity",String(view.gridOpacity/100));
}
function renderSceneMedia(){
  const playerView=activeSceneView()==="player",actors=Scene.actors.filter(actor=>!playerView||!actor.hidden),featured=Scene.artworks.find(art=>art.id===Scene.featuredArt&&(!playerView||!art.hidden)),background=Scene.artworks.find(art=>art.id===Scene.backgroundArt&&(!playerView||!art.hidden));
  $("scene-portrait-strip").innerHTML=actors.map(actor=>{const image=actor.portraitImage||actor.tokenImage,space=Scene.spaces.find(item=>item.id===actor.space)?.name||"";return `<button type="button" class="scene-portrait-card ${Scene.selectedActor===actor.id?"on":""}" data-scene-portrait-actor="${actor.id}" title="${esc(`${actor.name} · ${space}`)}">${image?`<img src="${image}" alt="">`:`<i class="portrait-fallback" style="--token-color:${actor.tokenColor}">${esc(actor.tokenSymbol||actor.name.slice(0,2).toUpperCase()||"?")}</i>`}<span>${esc(actor.name)}</span></button>`}).join("")||`<p class="autosave">В Сцене пока нет активных персонажей.</p>`;
  $("scene-featured-art").innerHTML=featured?`<figure><img src="${featured.image}" alt="${esc(featured.name)}"><figcaption><b>${esc(featured.name)}</b><span>Сейчас показан игрокам</span></figcaption></figure>`:`<div class="scene-media-empty"><span>◇</span><p>Показываемый арт не выбран</p></div>`;
  $("scene-art-library").innerHTML=Scene.artworks.map(art=>{const isFeatured=Scene.featuredArt===art.id,isBackground=Scene.backgroundArt===art.id;return `<article class="scene-art-card ${isFeatured?"is-featured":""} ${isBackground?"is-background":""}"><button type="button" class="scene-art-preview" data-scene-art-action="feature" data-scene-art-id="${art.id}" aria-label="Показать ${esc(art.name)}"><img src="${art.image}" alt=""></button><div class="scene-art-meta"><strong>${esc(art.name)}</strong><span>${isFeatured?"Показывается":isBackground?"Карта поля":art.kind==="background"?"Загружен как фон":"В библиотеке"}</span></div><div class="scene-art-actions"><button type="button" data-scene-art-action="feature" data-scene-art-id="${art.id}" aria-pressed="${isFeatured}">${isFeatured?"Скрыть":"Показать"}</button><button type="button" data-scene-art-action="background" data-scene-art-id="${art.id}" aria-pressed="${isBackground}">${isBackground?"Снять карту":"На поле"}</button><button type="button" class="danger-quiet" data-scene-art-action="remove" data-scene-art-id="${art.id}" aria-label="Удалить ${esc(art.name)}">×</button></div></article>`}).join("")||`<div class="scene-media-empty"><span>＋</span><p>Загрузите карту, локацию или иллюстрацию</p></div>`;
  $("scene-background-controls").innerHTML=background?`<section><header><div><span>КАРТА ПОЛЯ</span><strong>${esc(background.name)}</strong></div><button type="button" data-scene-art-action="background" data-scene-art-id="${background.id}">Снять</button></header><img src="${background.image}" alt=""><div class="scene-background-options"><label>Заполнение<select data-scene-background-setting="fit"><option value="cover" ${Scene.backgroundView.fit==="cover"?"selected":""}>Заполнить поле</option><option value="contain" ${Scene.backgroundView.fit==="contain"?"selected":""}>Показать целиком</option></select></label><label>Позиция<select data-scene-background-setting="position"><option value="center" ${Scene.backgroundView.position==="center"?"selected":""}>По центру</option><option value="top" ${Scene.backgroundView.position==="top"?"selected":""}>Сверху</option><option value="bottom" ${Scene.backgroundView.position==="bottom"?"selected":""}>Снизу</option><option value="left" ${Scene.backgroundView.position==="left"?"selected":""}>Слева</option><option value="right" ${Scene.backgroundView.position==="right"?"selected":""}>Справа</option></select></label><label class="range-label">Затемнение <output>${Scene.backgroundView.dim}%</output><input type="range" min="0" max="85" step="5" value="${Scene.backgroundView.dim}" data-scene-background-setting="dim"></label><label class="range-label">Плотность клеток <output>${Scene.backgroundView.gridOpacity}%</output><input type="range" min="12" max="96" step="4" value="${Scene.backgroundView.gridOpacity}" data-scene-background-setting="gridOpacity"></label></div></section>`:`<div class="scene-background-empty"><span>Карта поля не выбрана</span><small>Нажмите «На поле» у изображения из библиотеки.</small></div>`;
  applySceneBackground();
}
function usingNextSceneInterface(){return sceneInterfaceVersion==="next"}
function scenePanelSide(panel){if(!usingNextSceneInterface()||activeSceneView()==="player")return"right";if(scenePanelLayoutMode==="right")return"right";if(scenePanelLayoutMode==="left")return"left";return scenePanelLayoutMode==="custom"?(scenePanelSides[panel]||"right"):(DEFAULT_SCENE_PANEL_SIDES[panel]||"right")}
function isScenePanelOpen(panel){return usingNextSceneInterface()?activeScenePanels.left===panel||activeScenePanels.right===panel:activeScenePanel===panel}
function syncScenePanels(previous=null){
  const workbench=$("scene-workbench"),player=activeSceneView()==="player",leftRail=$("scene-rail-left"),rightRail=$("scene-rail-right");
  if(!usingNextSceneInterface()){
    activeScenePanels.left=null;activeScenePanels.right=activeScenePanel;
    for(const content of $$('[data-scene-panel-content]')){if(rightRail&&content.parentElement!==rightRail)rightRail.append(content);const active=content.dataset.scenePanelContent===activeScenePanel;content.classList.toggle("rail-active",active);if(content instanceof HTMLDetailsElement)content.open=active;if(active&&content.dataset.scenePanelContent!==previous)content.scrollTop=0}
    document.body.classList.toggle("scene-panel-open",Boolean(activeScenePanel));for(const name of ["scene-panel-left","scene-panel-open-left","scene-panel-open-right","scene-panel-open-both","scene-density-comfortable"])document.body.classList.remove(name);if(workbench){workbench.style.removeProperty("--scene-left-panel");workbench.style.removeProperty("--scene-right-panel")}$$('[data-scene-panel]').forEach(button=>button.setAttribute("aria-pressed",String(button.dataset.scenePanel===activeScenePanel)));return
  }
  if(player)for(const side of ["left","right"]){const panel=activeScenePanels[side],content=panel&&document.querySelector(`[data-scene-panel-content="${panel}"]`);if(content?.classList.contains("gm-only"))activeScenePanels[side]=null}
  if(player&&activeScenePanels.left){activeScenePanels.right=activeScenePanels.right||activeScenePanels.left;activeScenePanels.left=null}
  if(!player)for(const [side,panel] of Object.entries({...activeScenePanels})){
    if(!panel)continue;const desired=scenePanelSide(panel);if(desired===side)continue;
    activeScenePanels[side]=null;if(!activeScenePanels[desired]||panel===activeScenePanel)activeScenePanels[desired]=panel;
  }
  for(const content of $$('[data-scene-panel-content]')){
    const id=content.dataset.scenePanelContent,side=scenePanelSide(id),rail=side==="left"?leftRail:rightRail;
    if(rail&&content.parentElement!==rail)rail.append(content);
    const active=activeScenePanels[side]===id;
    content.classList.toggle("rail-active",active);
    if(content instanceof HTMLDetailsElement)content.open=active;
    if(active&&id!==previous)content.scrollTop=0;
  }
  const leftOpen=Boolean(activeScenePanels.left),rightOpen=Boolean(activeScenePanels.right);
  if(activeScenePanel&&!isScenePanelOpen(activeScenePanel))activeScenePanel=activeScenePanels.right||activeScenePanels.left||null;
  document.body.classList.toggle("scene-panel-open",leftOpen||rightOpen);
  document.body.classList.toggle("scene-panel-left",leftOpen&&!rightOpen);
  document.body.classList.toggle("scene-panel-open-left",leftOpen);
  document.body.classList.toggle("scene-panel-open-right",rightOpen);
  document.body.classList.toggle("scene-panel-open-both",leftOpen&&rightOpen);
  document.body.classList.toggle("scene-density-comfortable",sceneInterfaceDensity==="comfortable");
  if(workbench){workbench.style.setProperty("--scene-left-panel",leftOpen?`${SCENE_PANEL_WIDTHS[scenePanelWidths.left]}px`:"0px");workbench.style.setProperty("--scene-right-panel",rightOpen?`${SCENE_PANEL_WIDTHS[scenePanelWidths.right]}px`:"0px")}
  $$('[data-scene-panel]').forEach(button=>button.setAttribute("aria-pressed",String(isScenePanelOpen(button.dataset.scenePanel))));
}
function setScenePanel(panel,toggle=false){
  if(panel&&activeSceneView()==="player"&&document.querySelector(`[data-scene-panel-content="${panel}"]`)?.classList.contains("gm-only"))panel=null;
  const previous=activeScenePanel;
  if(!usingNextSceneInterface()){
    const next=toggle&&previous===panel?null:panel;if(next&&!previous&&document.activeElement instanceof HTMLElement)scenePanelTrigger=document.activeElement;activeScenePanel=next;syncScenePanels(previous);if(next&&next!==previous)requestAnimationFrame(()=>document.querySelector(`[data-scene-panel-content="${next}"] [data-close-scene-panel]`)?.focus({preventScroll:true}));else if(!next&&previous&&scenePanelTrigger?.isConnected){const trigger=scenePanelTrigger;scenePanelTrigger=null;requestAnimationFrame(()=>trigger.focus({preventScroll:true}))}return
  }
  if(!panel){
    const side=previous?Object.entries(activeScenePanels).find(([,open])=>open===previous)?.[0]:(activeScenePanels.right?"right":activeScenePanels.left?"left":null);
    if(side)activeScenePanels[side]=null;
    activeScenePanel=activeScenePanels.right||activeScenePanels.left||null;
  }else{
    const side=scenePanelSide(panel),other=side==="left"?"right":"left",already=activeScenePanels[side]===panel;
    if(activeScenePanels[other]===panel)activeScenePanels[other]=null;
    if(toggle&&already){activeScenePanels[side]=null;activeScenePanel=activeScenePanels[other]||null}
    else{if(!isScenePanelOpen(panel)&&document.activeElement instanceof HTMLElement)scenePanelTrigger=document.activeElement;activeScenePanels[side]=panel;activeScenePanel=panel}
  }
  syncScenePanels(previous);
  if(activeScenePanel&&activeScenePanel!==previous)requestAnimationFrame(()=>document.querySelector(`[data-scene-panel-content="${activeScenePanel}"] [data-close-scene-panel]`)?.focus({preventScroll:true}));
  else if(!activeScenePanel&&previous&&scenePanelTrigger?.isConnected){const trigger=scenePanelTrigger;scenePanelTrigger=null;requestAnimationFrame(()=>trigger.focus({preventScroll:true}))}
}
function closeAllScenePanels(){activeScenePanels.left=null;activeScenePanels.right=null;activeScenePanel=null;syncScenePanels()}
function renderSceneRoster(){
  const roster=$("scene-enemy-roster"),enemies=Scene.actors.filter(actor=>actor.kind==="enemy"||Boolean(actor.profileId)).sort((left,right)=>Number(right.id===Scene.activeActorId)-Number(left.id===Scene.activeActorId)||Number(left.knockedOut)-Number(right.knockedOut)||Number(left.acted)-Number(right.acted));
  const kindNames={action:"Действие",attack:"Атака",trump:"Козырь"};
  roster.innerHTML=enemies.map(actor=>{const profile=enemyProfile(actor.profileId),trait=antagonistTrait(actor.antagonistTraitId),defense=antagonistDefense(actor),available=new Map(SceneEngine.availableEnemyRules(Scene,D,actor.id).map(rule=>[rule.id,rule])),active=Scene.activeActorId===actor.id,compound=SceneEngine.compoundEnemyStatus(Scene,actor.id),vitals=compound.active?`${compound.hp}/${compound.maxHp} общие ЗД · часть · ${actor.ap} ОД · ${compound.defenseType==="armor"?`Б ${compound.armor}`:`У ${compound.evasion}`}`:`${actor.hp}/${actor.maxHp} ЗД · ${actor.ap} ОД · Б ${actor.armor} · У ${actor.evasion}`;return `<article class="enemy-roster-card ${actor.knockedOut?"knocked-out":""}"><header><button type="button" data-scene-roster-actor="${actor.id}"><strong>${automationBadge(enemyProfileAutomation(profile))}${esc(actor.name)} · Ст.${actor.tier}</strong><span>${esc(vitals)}</span></button><button type="button" class="enemy-turn-button" data-enemy-turn="${active?"end":"start"}" data-enemy-actor="${actor.id}" ${(actor.acted&&!active)||actor.knockedOut?"disabled":""}>${active?"Завершить":"Начать Ход"}</button></header><div class="enemy-roster-tags">${esc([profile?.tags||actor.effects.join(" · ")||"противник",trait?`Черта: ${trait.name}`:"",enemyRuleStateLabel(actor)].filter(Boolean).join(" · "))}</div>${defense?`<details class="enemy-trait-defense compact"><summary>Реакция защиты · ${esc(defense.name)}</summary><p>${md(defense.text)}</p></details>`:""}${profile?.passive?`<p class="enemy-passive"><b>Пассив:</b> ${md(profile.passive)}</p>`:""}<div class="enemy-basic-actions"><span>БАЗОВОЕ</span>${enemyStepButtonHtml(actor)}</div><div class="enemy-rule-list">${(profile?.rules||[]).map(rule=>{const state=available.get(rule.id)||rule,automation=enemyAutomationName(state.automation),meta=[kindNames[rule.kind]||rule.kind,`${rule.apCost||1} ОД`,automation,rule.kind==="trump"?`Н${rule.tension}`:"",rule.dice?`${rule.dice}D6`:"",rule.directDamage?`${rule.directDamage} урона`:"",rule.area?.length?`${rule.area[0]}×${rule.area[1]}`:""].filter(Boolean).join(" · ");return `<article class="enemy-rule ${rule.kind}"><button type="button" data-enemy-rule="${esc(rule.id)}" data-enemy-actor="${actor.id}" ${state.available?"":"disabled"} title="${esc(state.reason||rule.reward||rule.text)}"><strong>${automationBadge(state.automation)}${esc(rule.name)}</strong><small>${esc(meta)}</small></button><details><summary>Правило</summary><p>${md(rule.text)}</p>${rule.reward?`<p><b>Награда:</b> ${md(rule.reward)}</p>`:""}</details>${enemyRuleOptionsHtml(rule,actor,state)}</article>`}).join("")||`<p class="autosave">Профиль пока разрешается вручную.</p>`}</div></article>`}).join("")||`<p class="autosave">В Сцене пока нет противников.</p>`;
  roster.querySelectorAll("[data-scene-roster-actor]").forEach(button=>{const actor=Scene.actors.find(item=>item.id===button.dataset.sceneRosterActor),card=button.closest(".enemy-roster-card");if(actor?.team==="hero")card?.querySelector(".enemy-roster-tags")?.prepend("СОЮЗНИК ГЕРОЕВ · ");if(SceneEngine.isEnemyModifier?.(actor)){const turn=card?.querySelector(".enemy-turn-button");if(turn){turn.disabled=true;turn.textContent="Без Хода"}card?.querySelector(".enemy-basic-actions")?.remove();const empty=card?.querySelector(".enemy-rule-list .autosave");if(empty)empty.textContent=actor.modifierState?.configuredRound?"Правило активно. Откройте карточку для настройки и канонических действий.":"Требуется настройка. Откройте карточку модификатора."}});
}
function sceneTurnApprovalMode(){return Scene.turnApprovalMode==="narrator"?"narrator":"self"}
function sceneControlModeCopy(){if(window.DAWN_LIONWING_ENGINE?.isScene(Scene))return ["LionWing","Общие правила обрабатывает ядро. Автоматизацию доступных Техник можно отдельно включить в Пульте; остальные правила разрешает Нарратор."];return ({auto:["Автоматика","Компаньон проверяет правила, ресурсы, Реакции и применяет доступные исходы."],guided:["Гибрид","Автоматика остаётся основной, а спорные и непокрытые случаи решает Нарратор."],manual:["Ручной стол","Техники не применяются автоматически: игрок заявляет действие, а итог разрешает Нарратор через Пульт."]})[sceneControlMode]}
function renderSceneDirector(){
  const root=$("scene-director");if(!root)return;
  const active=Scene.actors.find(actor=>actor.id===Scene.activeActorId),actor=Scene.actors.find(item=>item.id===Scene.selectedActor)||active||Scene.actors.find(item=>!item.knockedOut)||null,ready=Scene.actors.filter(item=>!item.knockedOut&&!item.acted&&item.kind!=="crowd"&&!SceneEngine.isEnemyModifier?.(item)),done=Scene.actors.filter(item=>(item.acted||item.knockedOut)&&item.kind!=="crowd"&&!SceneEngine.isEnemyModifier?.(item)),targets=Scene.actors.filter(item=>Scene.targetIds.includes(item.id)),roundStatus=SceneEngine.roundEndStatus(Scene),[modeTitle,modeText]=sceneControlModeCopy();
  const phase=!Scene.actors.length?"Подготовка":Scene.pendingPrompt?"Решение правила":Scene.pendingAction?"Реакции и исход":active?`Ход: ${active.name}`:roundStatus.available?"Конец Раунда":"Выбор следующего Хода";
  const overview=`<section class="director-overview"><div><span>ТЕКУЩАЯ ФАЗА</span><strong>${esc(phase)}</strong><small>${ready.length} готовы · ${done.length} завершили · ${Scene.actors.length} всего</small></div><div class="director-tension"><small>НАПРЯЖЕНИЕ</small><strong>${Scene.tension}</strong><span><button type="button" data-director-tension="-1" ${Scene.tension<=0?"disabled":""} aria-label="Уменьшить Напряжение">−</button><button type="button" data-director-tension="1" aria-label="Увеличить Напряжение">+</button></span></div></section>`;
  const mode=`<section class="director-mode mode-${sceneControlMode}"><span>${esc(modeTitle)}</span><p>${esc(modeText)}</p></section>`;
  if(!actor){root.innerHTML=`${overview}${mode}<section class="director-empty"><span>✦</span><strong>Соберите участников боя</strong><p>Добавьте героя и противников, расставьте их на поле — после этого пульт проведёт сцену через Ходы, действия и новый Раунд.</p><button type="button" class="primary" data-open-scene-panel="add">Добавить участников</button></section>`;return}
  const effects=sceneEffectList(),turnStatus=actor.id===active?.id?{available:true,reason:""}:sceneTurnStatusAfterCurrent(actor),turnLocked=Boolean(Scene.pendingAction||Scene.pendingPrompt||actor.id!==active?.id&&!turnStatus.available);
  const roster=Scene.actors.map(item=>{const status=item.id===active?.id?{available:true,reason:""}:sceneTurnStatusAfterCurrent(item),state=item.knockedOut?"Вне боя":item.id===active?.id?"Сейчас Ход":item.acted?status.available?"Можно повторить Ход":"Ход завершён":"Готов действовать",locked=Boolean(Scene.pendingAction||Scene.pendingPrompt||item.id!==active?.id&&!status.available),targeted=Scene.targetIds.includes(item.id);return `<article class="director-roster-item ${item.id===actor.id?"is-selected":""} ${item.id===active?.id?"is-active":""} ${item.knockedOut?"is-out":""}"><button type="button" class="director-roster-pick" data-director-select="${item.id}"><i style="--token-color:${safeColor(item.tokenColor,item.team==="enemy"?"#a84141":"#3ea7c4")}">${esc(item.tokenSymbol||item.name.slice(0,2).toUpperCase())}</i><span><strong>${esc(item.name)}</strong><small>${esc(state)} · ${item.hp}/${item.maxHp||"—"} ЗД · ${item.ap||0} ОД</small></span></button><div><button type="button" class="director-target-toggle ${targeted?"on":""}" data-director-toggle-target="${item.id}" ${item.knockedOut?"disabled":""} aria-pressed="${targeted}" title="${targeted?"Убрать из целей":"Добавить в цели ручного исхода"}">${targeted?"Цель ✓":"В цели"}</button><button type="button" data-director-quick-turn="${item.id}" ${locked?"disabled":""} title="${esc(status.reason||"")}">${item.id===active?.id?"Завершить":"Начать"}</button></div></article>`}).join("");
  const targetChips=targets.map(item=>`<button type="button" data-director-toggle-target="${item.id}" title="Убрать цель">${esc(item.name)} ×</button>`).join("")||`<small>Цели не выбраны. Добавьте их кнопками в составе.</small>`;
  const exactResource=(key,label,value)=>`<div class="director-exact-resource"><span><small>${label}</small><b>${value}</b></span><input id="scene-director-exact-${key}" type="number" min="0" max="9999" value="${value}" aria-label="Точное значение ${label}"><button type="button" data-director-set-resource="${key}">Задать</button></div>`,heroSheet=actor.kind==="hero"||Boolean(actor.heroId),profileActor=actor.kind==="enemy"||Boolean(actor.profileId),focus=heroSheet?exactResource("focus","Фокуса",actor.focus||0):"",enemyStep=profileActor?enemyStepButtonHtml(actor):"";
  root.innerHTML=`${overview}${mode}<section class="director-section director-roster"><header><span>СОСТАВ И ХОДЫ</span><small>Порядок выбирает стол</small></header><div class="director-roster-list">${roster}</div></section><section class="director-actor"><label>Выбранный участник<select id="scene-director-actor">${Scene.actors.map(item=>`<option value="${item.id}" ${item.id===actor.id?"selected":""}>${esc(item.name)}${item.knockedOut?" · вне боя":item.id===active?.id?" · сейчас Ход":item.acted?" · завершил":""}</option>`).join("")}</select></label><div class="director-actor-card"><div><span>${actor.team==="enemy"?"ПРОТИВНИК":"СОЮЗНИК"}</span><strong>${esc(actor.name)}</strong><small>${actor.knockedOut?"Вне боя":actor.id===active?.id?"Сейчас Ход":turnStatus.available?actor.acted?"Доступен повторный Ход":"Готов действовать":turnStatus.reason||"Сейчас недоступен"}</small></div><button type="button" data-director-turn="toggle" ${turnLocked?"disabled":""} title="${esc(turnStatus.reason||"")}">${actor.id===active?.id?"Завершить Ход":"Начать Ход"}</button></div></section><section class="director-section"><header><span>ЗД И РЕСУРСЫ</span><small>Запись в журнал и один шаг отката</small></header><div class="director-resource-row health"><span><small>Здоровье</small><b>${actor.hp}/${actor.maxHp||"—"}</b></span><button type="button" data-director-damage="1" ${actor.knockedOut?"disabled":""}>−1 урон</button><button type="button" data-director-damage="5" ${actor.knockedOut?"disabled":""}>−5 урона</button><button type="button" data-director-heal="1">+1</button><button type="button" data-director-heal="5">+5</button></div><div class="director-exact-grid">${exactResource("ap","ОД",actor.ap||0)}${focus}</div>${enemyStep?`<div class="director-enemy-basic"><span>БАЗОВОЕ ДЕЙСТВИЕ ВРАГА</span>${enemyStep}</div>`:""}<div class="director-state-actions"><button type="button" data-director-knockout="${actor.knockedOut?"restore":"knockout"}" class="${actor.knockedOut?"":"danger-quiet"}">${actor.knockedOut?"Вернуть в бой":"Вывести из боя"}</button><button type="button" data-open-scene-panel="inspector">Полные параметры</button></div></section><section class="director-section director-outcome"><header><span>РУЧНОЙ ИСХОД</span><small>Одним пакетом после всех решений</small></header><div class="director-targets"><div>${targetChips}</div>${targets.length?`<button type="button" data-director-clear-targets>Снять все</button>`:""}</div><div class="director-outcome-grid"><label>Урон каждой цели<input id="scene-director-outcome-damage" type="number" min="0" max="9999" value="0" inputmode="numeric"></label><label>Эффект<select id="scene-director-outcome-effect"><option value="">Без Эффекта</option>${effects.map(effect=>`<option value="${effect.id}">${esc(effect.name)}</option>`).join("")}</select></label></div><div class="director-outcome-options"><label><input id="scene-director-ignore-armor" type="checkbox"> Игнорировать Броню</label><label><input id="scene-director-ignore-evasion" type="checkbox"> Игнорировать Уклонение</label></div><label>Причина или решение<textarea id="scene-director-note" rows="3" maxlength="320" placeholder="Что решил Нарратор и почему…"></textarea></label><div class="button-row"><button type="button" data-director-open-reactions ${targets.length?"":"disabled"}>Сначала запросить защитные Реакции</button><button type="button" class="primary" data-director-resolve-outcome ${targets.length?"":"disabled"}>Применить к ${targets.length||0} ${targets.length===1?"цели":"целям"}</button></div><small class="director-outcome-hint">Для неавтоматизированной Атаки сначала откройте Реакции; после ответов Нарратор разрешит урон обычным конвейером.</small></section><section class="director-section"><header><span>ЭФФЕКТ ВЫБРАННОМУ</span><small>Быстрое точечное решение</small></header><div class="director-effect"><select id="scene-director-effect">${effects.map(effect=>`<option value="${effect.id}">${esc(effect.name)}</option>`).join("")}</select><button type="button" data-director-effect="apply">Наложить</button><button type="button" data-director-effect="remove">Снять</button></div><button type="button" data-director-note>Только записать решение</button></section><section class="director-round"><div><span>РАУНД ${Scene.round}</span><strong>${roundStatus.available?"Все обязательные Ходы завершены":"Бой продолжается"}</strong><small>${roundStatus.available?"Напряжение вырастет на 1, ОД восстановятся.":esc(roundStatus.reason||"Выберите следующего участника.")}</small></div><button type="button" class="primary" data-director-round ${roundStatus.available?"":"disabled"}>Новый Раунд</button></section>`;
}
function sceneDirectorReminderSection(root){
  const names={turnStart:"В начале следующего Хода",turnEnd:"В конце Хода",roundEnd:"В конце Раунда",manual:"Без срока"},cards=(Scene.reminders||[]).map(item=>`<article class="director-reminder ${item.due?"due":""}"><div><strong>${esc(item.label)}</strong><small>${item.due?"ПОРА РАЗРЕШИТЬ":names[item.boundary]||"Напоминание"}${item.ownerActorId?` · ${esc(Scene.actors.find(entry=>entry.id===item.ownerActorId)?.name||"участник")}`:""}</small><p>${esc(item.text)}</p></div><div>${item.due?`<button type="button" class="primary" data-director-reminder-resolve="${item.id}">Разрешено</button>`:""}<button type="button" data-director-reminder-remove="${item.id}">Удалить</button></div></article>`).join("")||`<small class="autosave">Отложенных решений пока нет.</small>`,section=document.createElement("section");
  section.className="director-section director-reminders";section.innerHTML=`<header><span>ОТЛОЖЕННЫЕ ЭФФЕКТЫ</span><small>Система напомнит на границе правил</small></header><label>Короткое имя<input id="scene-reminder-label" maxlength="120" placeholder="Взрыв руны"></label><label>Что разрешить<textarea id="scene-reminder-text" maxlength="800" rows="2" placeholder="Нанести 3 урона всем в области"></textarea></label><div class="director-reminder-create"><select id="scene-reminder-boundary"><option value="turnStart">В начале следующего Хода выбранного</option><option value="turnEnd">В конце Хода выбранного</option><option value="roundEnd">В конце Раунда</option><option value="manual">Без автоматического срока</option></select><button type="button" data-director-reminder-create>Создать</button></div><div class="director-reminder-list">${cards}</div>`;root.querySelector(".director-roster")?.before(section);
}
const renderSceneDirectorBase=renderSceneDirector;
renderSceneDirector=function(){renderSceneDirectorBase();const root=$("scene-director");if(!root?.querySelector(".director-roster"))return;sceneDirectorReminderSection(root);for(const pick of root.querySelectorAll("[data-director-select]")){const actor=Scene.actors.find(item=>item.id===pick.dataset.directorSelect),row=pick.closest(".director-roster-item");row?.classList.toggle("is-done",Boolean(actor?.acted));row?.classList.toggle("is-ready",Boolean(actor&&!actor.acted&&!actor.knockedOut))}for(const button of root.querySelectorAll("[data-director-toggle-target], [data-director-damage], [data-director-heal], [data-director-knockout], [data-director-effect], [data-director-set-resource]")){if(button.disabled)button.title=`Предупреждение: ${button.title||"обычные условия не выполнены"}. Нарратор всё равно может продолжить.`;button.disabled=false}const options=root.querySelector(".director-outcome-options");if(options)options.innerHTML=`<small><strong>Решение Нарратора обходит правила.</strong> Броня, Уклонение, лимиты и автоматические триггеры не запрещают назначенный итог; причина останется в журнале.</small>`;const selected=Scene.actors.find(item=>item.id===Scene.selectedActor),actor=selected||Scene.actors.find(item=>item.id===Scene.activeActorId);const exact=root.querySelector(".director-exact-grid");if(actor&&exact)exact.insertAdjacentHTML("afterbegin",`<div class="director-exact-resource"><span><small>ЗД без правил</small><b>${SceneEngine.compoundEnemyStatus(Scene,actor.id).active?SceneEngine.compoundEnemyStatus(Scene,actor.id).hp:actor.hp}</b></span><input id="scene-director-exact-hp" type="number" min="0" max="9999" value="${SceneEngine.compoundEnemyStatus(Scene,actor.id).active?SceneEngine.compoundEnemyStatus(Scene,actor.id).hp:actor.hp}"><button type="button" data-director-set-resource="hp">Задать</button></div><div class="director-exact-resource"><span><small>Раны без правил</small><b>${actor.wounds||0}</b></span><input id="scene-director-exact-wounds" type="number" min="0" max="99" value="${actor.wounds||0}"><button type="button" data-director-set-resource="wounds">Задать</button></div>`);const activeEnemy=Scene.actors.find(item=>item.id===Scene.activeActorId&&item.team==="enemy");if(activeEnemy){const profile=enemyProfile(activeEnemy.profileId),available=new Map(SceneEngine.availableEnemyRules(Scene,D,activeEnemy.id).map(rule=>[rule.id,rule])),section=document.createElement("section");section.className="director-section director-active-enemy";section.innerHTML=`<header><span>ДЕЙСТВИЯ АКТИВНОГО ВРАГА</span><small>${esc(activeEnemy.name)}</small></header>${enemyStepButtonHtml(activeEnemy)}<div class="enemy-rule-list">${(profile?.rules||[]).map(rule=>{const status=available.get(rule.id)||rule;return `<button type="button" data-enemy-rule="${esc(rule.id)}" data-enemy-actor="${activeEnemy.id}" ${status.available?"":"disabled"} title="${esc(status.reason||rule.text)}"><strong>${automationBadge(status.automation)}${esc(rule.name)}</strong><small>${rule.apCost||1} ОД · ${esc(enemyAutomationName(status.automation))}</small></button>`}).join("")||`<small>Профиль разрешается вручную.</small>`}</div>`;root.querySelector(".director-outcome")?.before(section)}};
const renderSceneDirectorExactBase=renderSceneDirector;
renderSceneDirector=function(){renderSceneDirectorExactBase();const selected=Scene.actors.find(item=>item.id===Scene.selectedActor);if(!selected)return;const compound=SceneEngine.compoundEnemyStatus(Scene,selected.id),hp=compound.active?compound.hp:selected.hp,hpInput=$("scene-director-exact-hp"),woundsInput=$("scene-director-exact-wounds");if(hpInput){hpInput.value=hp;hpInput.previousElementSibling?.querySelector("b")&&(hpInput.previousElementSibling.querySelector("b").textContent=hp)}if(woundsInput){woundsInput.value=selected.wounds||0;woundsInput.previousElementSibling?.querySelector("b")&&(woundsInput.previousElementSibling.querySelector("b").textContent=selected.wounds||0)}};
const renderSceneDirectorProfileBase=renderSceneDirector;
renderSceneDirector=function(){renderSceneDirectorProfileBase();const actor=Scene.actors.find(item=>item.id===Scene.activeActorId),profile=actor&&enemyProfile(actor.profileId),root=$("scene-director");if(!actor||!profile||root?.querySelector(".director-active-enemy"))return;const available=new Map(SceneEngine.availableEnemyRules(Scene,D,actor.id).map(rule=>[rule.id,rule])),section=document.createElement("section");section.className="director-section director-active-enemy";section.innerHTML=`<header><span>ДЕЙСТВИЯ АКТИВНОГО ПРОФИЛЯ</span><small>${esc(actor.name)} · ${actor.team==="hero"?"союзник героев":"противник"}</small></header>${enemyStepButtonHtml(actor)}<div class="enemy-rule-list">${(profile.rules||[]).map(rule=>{const status=available.get(rule.id)||rule;return `<button type="button" data-enemy-rule="${esc(rule.id)}" data-enemy-actor="${actor.id}" ${status.available?"":"disabled"} title="${esc(status.reason||rule.text)}"><strong>${automationBadge(status.automation)}${esc(rule.name)}</strong><small>${rule.apCost||1} ОД · ${esc(enemyAutomationName(status.automation))}</small></button>`}).join("")||`<small>Профиль разрешается вручную.</small>`}</div>`;root.querySelector(".director-outcome")?.before(section)};
function directorEnemyProfileSection(actor){
  const profile=enemyProfile(actor?.profileId);if(!actor||!profile)return null;
  const available=new Map(SceneEngine.availableEnemyRules(Scene,D,actor.id).map(rule=>[rule.id,rule])),defense=antagonistDefense(actor),kindNames={action:"Действие",attack:"Атака",trump:"Козырь"},section=document.createElement("section");
  section.className="director-section director-active-enemy";
  section.innerHTML=`<header><span>СПОСОБНОСТИ ПРОФИЛЯ</span><small>${esc(actor.name)} · ${actor.id===Scene.activeActorId?"сейчас Ход":actor.team==="hero"?"союзник героев":"противник"}</small></header>${profile.passive?`<article class="enemy-passive"><b>Пассив:</b> ${md(profile.passive)}</article>`:""}${defense?`<details class="enemy-trait-defense compact" open><summary>Защита · ${esc(defense.name)}</summary><p>${md(defense.text)}</p></details>`:""}<div class="director-enemy-basic"><span>БАЗОВОЕ ДЕЙСТВИЕ</span>${enemyStepButtonHtml(actor)}</div><div class="enemy-rule-list">${(profile.rules||[]).map(rule=>{const status=available.get(rule.id)||rule,meta=[kindNames[rule.kind]||rule.kind,`${rule.apCost||1} ОД`,enemyAutomationName(status.automation),rule.kind==="trump"?`Н${rule.tension}`:"",rule.dice?`${rule.dice}D6`:"",rule.directDamage?`${rule.directDamage} урона`:""].filter(Boolean).join(" · ");return `<article class="enemy-rule ${rule.kind}"><button type="button" data-enemy-rule="${esc(rule.id)}" data-enemy-actor="${actor.id}" ${status.available?"":"disabled"} title="${esc(status.reason||rule.text)}"><strong>${automationBadge(status.automation)}${esc(rule.name)}</strong><small>${esc(meta)}</small></button><details><summary>Полное правило</summary><p>${md(rule.text)}</p>${rule.reward?`<p><b>Награда:</b> ${md(rule.reward)}</p>`:""}</details>${enemyRuleOptionsHtml(rule,actor,status)}</article>`}).join("")||`<small>У профиля нет отдельных действий — используйте базовое действие и ручные решения Нарратора.</small>`}</div>`;
  return section;
}
const renderSceneDirectorEnemyProfileBase=renderSceneDirector;
renderSceneDirector=function(){renderSceneDirectorEnemyProfileBase();const root=$("scene-director");if(!root)return;root.querySelectorAll(".director-active-enemy").forEach(section=>section.remove());const selected=Scene.actors.find(actor=>actor.id===Scene.selectedActor&&actor.profileId),active=Scene.actors.find(actor=>actor.id===Scene.activeActorId&&actor.profileId),section=directorEnemyProfileSection(Scene.actors.find(actor=>actor.id===Scene.selectedActor)||active);if(section)root.querySelector(".director-outcome")?.before(section)};
function directorManualStateEditor(actor){
  if(!actor)return"";
  const fields=[["maxHp","Максимум ЗД",actor.maxHp],["baseAp","ОД в начале Хода",actor.baseAp],["speed","Скорость",actor.speed],["armor","Броня",actor.armor],["evasion","Уклонение",actor.evasion],["guts","Стойкость",actor.guts],["stress","Стресс",actor.stress],["influence","Влияние",actor.influence],["tier","Ступень",actor.tier],["attr.body","Тело",actor.attrs?.body],["attr.talent","Талант",actor.attrs?.talent],["attr.spirit","Дух",actor.attrs?.spirit],["attr.mind","Разум",actor.attrs?.mind]].filter(([key])=>actor.rulesEdition!=="lionwing"||key!=="guts"),fieldRows=fields.map(([key,label,value])=>`<label><span>${label}</span><input type="number" min="0" max="9999" value="${Number(value)||0}" data-director-field-input="${key}"><button type="button" data-director-set-field="${key}">Задать</button></label>`).join("");
  const resources=SceneEngine.ruleResourceDefinitions(actor).map(definition=>{const status=SceneEngine.ruleResourceStatus(Scene,actor.id,{resource:definition.resource});return`<label><span>${esc(definition.label||definition.resource)}</span><input type="number" min="0" max="9999" value="${Number(status.balance)||0}" data-director-rule-resource-input="${esc(definition.resource)}"><button type="button" data-director-set-rule-resource="${esc(definition.resource)}">Задать</button></label>`}).join("");
  const clocks=SceneEngine.ruleClockDefinitions(actor).map(definition=>{const status=SceneEngine.clockStatus(Scene,actor.id,definition.clockId);return`<label><span>${esc(status.label||definition.clockId)} <small>/ ${status.size}</small></span><input type="number" min="0" max="${status.size}" value="${status.value}" data-director-rule-clock-input="${esc(definition.clockId)}"><button type="button" data-director-set-rule-clock="${esc(definition.clockId)}">Задать</button></label>`}).join("");
  return`<details class="director-state-editor"><summary>Все параметры и часы <b>${fields.length+SceneEngine.ruleResourceDefinitions(actor).length+SceneEngine.ruleClockDefinitions(actor).length}</b></summary><div><p>Точные значения применяются решением Нарратора и остаются в журнале.</p><div class="director-state-grid">${fieldRows}${resources}${clocks||`<small class="autosave">Активных часов Техник пока нет.</small>`}</div></div></details>`;
}
function organizeSceneDirector(root){
  const roster=root.querySelector(".director-roster");if(!roster)return;
  const sections=[...root.querySelectorAll(":scope > .director-section")],byTitle=title=>sections.find(section=>section.querySelector(":scope > header span")?.textContent.trim()===title),resources=byTitle("ЗД И РЕСУРСЫ"),quickEffect=byTitle("ЭФФЕКТ ВЫБРАННОМУ"),reminders=root.querySelector(".director-reminders"),enemy=root.querySelector(".director-active-enemy"),actor=root.querySelector(".director-actor"),outcome=root.querySelector(".director-outcome"),round=root.querySelector(".director-round");
  if(!["turn","manual","roster"].includes(activeDirectorTab))activeDirectorTab="turn";
  const controlActor=Scene.actors.find(item=>item.id===Scene.selectedActor)||Scene.actors.find(item=>item.id===Scene.activeActorId)||Scene.actors.find(item=>!item.knockedOut),active=Scene.actors.find(item=>item.id===Scene.activeActorId),focusBar=document.createElement("section");
  focusBar.className="director-focus-bar";focusBar.innerHTML=`<button type="button" data-director-cycle="-1" aria-label="Предыдущий участник">‹</button><label><span>Управляем</span><select id="scene-director-actor">${Scene.actors.map(item=>`<option value="${item.id}" ${item.id===controlActor?.id?"selected":""}>${esc(item.name)}${item.id===active?.id?" · сейчас Ход":item.knockedOut?" · вне боя":""}</option>`).join("")}</select></label><button type="button" data-director-cycle="1" aria-label="Следующий участник">›</button>`;
  actor?.remove();
  const tabs=document.createElement("nav");tabs.className="director-tabs";tabs.setAttribute("aria-label","Разделы пульта");tabs.innerHTML=`<button type="button" data-director-tab="turn" class="${activeDirectorTab==="turn"?'on':''}">Ход</button><button type="button" data-director-tab="manual" class="manual ${activeDirectorTab==="manual"?'on':''}">Ручное решение${Scene.targetIds.length?` <b>${Scene.targetIds.length}</b>`:''}</button><button type="button" data-director-tab="roster" class="${activeDirectorTab==="roster"?'on':''}">Состав <b>${Scene.actors.filter(item=>!item.knockedOut&&!item.acted).length}</b></button>`;
  const panes={turn:document.createElement("div"),manual:document.createElement("div"),roster:document.createElement("div")};for(const [id,pane] of Object.entries(panes)){pane.className="director-pane";pane.dataset.directorPane=id;pane.hidden=id!==activeDirectorTab}
  const handoff=document.createElement("section"),turnActors=Scene.actors.filter(item=>!item.knockedOut&&(item.id===active?.id||!item.acted&&sceneTurnStatusAfterCurrent(item).available)),turnChoice=turnActors.find(item=>item.id===controlActor?.id)||turnActors[0];handoff.className="director-turn-handoff";handoff.innerHTML=`<label><span>${active?`Сейчас: ${esc(active.name)}`:"Ход не начат"}</span><select id="scene-director-next-turn">${turnActors.map(item=>`<option value="${item.id}" ${item.id===turnChoice?.id?"selected":""}>${esc(item.name)}${item.id===active?.id?" · завершить":""}</option>`).join("")}</select></label><button type="button" class="primary" data-director-assign-turn ${turnChoice?"":"disabled"}>${active&&turnChoice?.id===active.id?"Завершить Ход":active?"Передать Ход":"Начать Ход"}</button>`;
  const manualIntro=document.createElement("aside");manualIntro.className="director-manual-intro";manualIntro.innerHTML=`<div><strong>Решение вне автоматики</strong><small>Выберите цели на поле или в составе, затем примените итог одним пакетом.</small></div><button type="button" data-open-scene-panel="utility">Бросить XD6</button>`;
  const reminderShell=document.createElement("details");reminderShell.className="director-reminder-shell";reminderShell.open=Boolean(Scene.reminders?.some(item=>item.due));reminderShell.innerHTML=`<summary>Отложенные эффекты <b>${Scene.reminders?.length||0}</b></summary>`;if(reminders)reminderShell.append(reminders);
  const stateEditor=document.createElement("div");stateEditor.innerHTML=directorManualStateEditor(controlActor);
  panes.turn.append(handoff);[resources,enemy].filter(Boolean).forEach(node=>panes.turn.append(node));panes.manual.append(manualIntro);[outcome,quickEffect].filter(Boolean).forEach(node=>panes.manual.append(node));if(stateEditor.firstElementChild)panes.manual.append(stateEditor.firstElementChild);if(reminders)panes.manual.append(reminderShell);[roster,round].filter(Boolean).forEach(node=>panes.roster.append(node));
  if(controlActor){const compound=SceneEngine.compoundEnemyStatus(Scene,controlActor.id),values={hp:compound.active?compound.hp:controlActor.hp,wounds:controlActor.wounds||0};for(const [key,value] of Object.entries(values)){const input=root.querySelector(`#scene-director-exact-${key}`);if(input){input.value=value;const label=input.previousElementSibling?.querySelector("b");if(label)label.textContent=value}}}
  const mode=root.querySelector(".director-mode");(mode||root.querySelector(".director-overview"))?.after(tabs,focusBar,...Object.values(panes));
}
const renderSceneDirectorOrganizedBase=renderSceneDirector;
renderSceneDirector=function(){renderSceneDirectorOrganizedBase();if(usingNextSceneInterface())organizeSceneDirector($("scene-director"))};
function setSheetTab(tab){activeSheetTab=["main","bio","combat","utility"].includes(tab)?tab:"combat";$$('[data-sheet-tab]').forEach(button=>{const on=button.dataset.sheetTab===activeSheetTab;button.classList.toggle("on",on);button.setAttribute("aria-selected",String(on));button.tabIndex=on?0:-1});$$('[data-sheet-pane]').forEach(pane=>pane.hidden=pane.dataset.sheetPane!==activeSheetTab)}
function sceneRollShortcuts(actor){
  if(!actor)return"";
  const skills=(actor.skills||[]).filter(skill=>skillDisplayName(skill,actor).trim()),abilities=[["ability",actor.ability],["taintedAbility",actor.taintedAbility]].filter(([,ability])=>Boolean(ability));
  return `<div class="scene-roll-shortcuts"><div class="scene-shortcut-head"><div><span class="kind">БРОСОК С ЛИСТА</span><strong>Выберите подходящую особенность</strong></div><small>Затем останется выбрать Атрибут</small></div>${skills.length?`<div class="scene-feature-group"><h4>Навыки</h4><div class="scene-feature-buttons">${skills.map(skill=>`<button type="button" data-sheet-roll-skill="${esc(skill.id)}"><span>${esc(skillDisplayName(skill,actor))}</span><b>+${skill.rank}D6</b></button>`).join("")}</div></div>`:""}${abilities.length?`<div class="scene-feature-group"><h4>Способности</h4><div class="scene-feature-buttons">${abilities.map(([key,ability])=>`<button type="button" class="ability" data-sheet-roll-ability="${key}"><span>${esc(ability.name||"Способность")}</span><b>+${ability.rank}D6</b><small>${esc(ability.desc||"")}</small></button>`).join("")}</div></div>`:""}</div>`;
}
function sceneSheetPanel(){
  const localActor=Scene.actors.find(item=>item.heroId===S.id)||null,actor=activeSceneView()==="gm"?(Scene.actors.find(item=>item.id===Scene.selectedActor)||Scene.actors.find(item=>item.id===Scene.activeActorId)||localActor):localActor,isLocal=Boolean(actor&&actor.heroId===S.id),techs=Object.entries(actor?.knownTechniques||actor?.techniques||{}).filter(([,level])=>level>0).map(([id,level])=>({tech:techById(id),level})).filter(item=>item.tech),gifts=activeOutlooks().filter(outlook=>(actor?.outlooks||[]).includes(outlook.id)).flatMap(outlook=>(outlook.builtin&&outlook.id===actor?.primaryOutlook?[outlook.builtin]:[]).concat(outlook.gifts.filter(gift=>(actor?.gifts||[]).includes(gift.id))));
  const usesWounds=actor?.kind==="hero"||Boolean(actor?.heroId);
  const sacrificeLabels={eye:"Глаз",arm:"Рука",leg:"Нога",tongue:"Язык",life:"Жизнь"},sacrifice=isLocal&&hasGift("Sacrifice")?`<article class="kit-item"><strong>Жертва · необратимые цены</strong><div class="chip-row">${Object.entries(sacrificeLabels).map(([id,label])=>`<button type="button" class="effect-chip ${S.runtime.sacrifices.includes(id)?"on":""}" data-sacrifice="${id}">${S.runtime.sacrifices.includes(id)?"Пожертвовано: ":"Пожертвовать: "}${label}</button>`).join("")}</div></article>`:"";
  const tabButton=(id,label,icon)=>`<button type="button" role="tab" id="scene-sheet-tab-${id}" aria-controls="scene-sheet-pane-${id}" data-sheet-tab="${id}" data-tab-icon="${icon}">${label}</button>`,paneAttrs=id=>`id="scene-sheet-pane-${id}" role="tabpanel" aria-labelledby="scene-sheet-tab-${id}" data-sheet-pane="${id}"`;
  const tabs=`<div class="scene-sheet-tabs" role="tablist" aria-label="Разделы листа">${tabButton("main","Основное","◆")}${tabButton("combat","Бой","⚔")}${tabButton("utility","Броски","⬡")}${tabButton("bio","История","✎")}</div>`;
  const definition=actor&&SceneEngine.ruleResourceDefinitions(actor)[0],resourceLabel=definition?.label||"Фокус",resourceValue=definition?SceneEngine.ruleResourceStatus(Scene,actor.id,{resource:definition.resource}).balance:actor?.focus??0,concept=isLocal?S.concept:"",main=`<section class="scene-sheet-pane" ${paneAttrs("main")}><div class="scene-hero-heading"><div><span class="kind">${actor?.team==="enemy"?"ПРОТИВНИК":"ГЕРОЙ"} · СТУПЕНЬ ${actor?.tier||1}</span><h3>${esc(actor?.name||"Безымянный")}</h3><p>${esc(concept||(!isLocal?"Данные участника со стола":"Концепция не записана"))}</p></div></div><div class="scene-sheet-vitals"><span>Здоровье <b>${actor?.hp??0}/${actor?.maxHp??0}</b></span>${usesWounds?`<span>Раны <b>${actor?.wounds??0}/${actor?.rulesEdition==="lionwing"?3:actor?.guts??0}</b></span>`:""}<span>ОД <b>${actor?.ap??0}</b></span><span>${esc(resourceLabel)} <b>${resourceValue}</b></span></div><div class="scene-attribute-strip">${ATTRS.map(([key,name])=>`<span><small>${name}</small><b>${actor?.attrs?.[key]??0}</b></span>`).join("")}</div>${sceneRollShortcuts(actor)}<h4>Активные Эффекты</h4><div class="scene-sheet-list">${sceneActorEffects(actor).map(id=>`<span>${esc(sceneEffectList().find(item=>item.id===id)?.name||id)}</span>`).join("")||"Нет активных Эффектов"}</div></section>`;
  const abilityCard=(ability,key,label)=>ability?`<article class="kit-item ability-story-card"><strong>${esc(ability.name||label)} · ${ability.rank}</strong><p>${md(ability.desc||"")}</p><button type="button" data-sheet-roll-ability="${key}">Использовать в броске · +${ability.rank}D6</button></article>`:"",bio=`<section class="scene-sheet-pane" ${paneAttrs("bio")}><h3>История и заметки</h3><p>${isLocal?`${esc(S.concept||"Концепция не записана")}${S.player?` · игрок: ${esc(S.player)}`:""}`:"Личные сведения игрока не публикуются в Сцену."}</p>${isLocal?`<label>Личные заметки<textarea rows="5" data-scene-hero-notes placeholder="Планы, связи, зацепки…">${esc(S.runtime.notes||"")}</textarea></label>`:""}<h4>Дары</h4>${gifts.map(gift=>`<article class="kit-item"><strong>${esc(gift.name)}</strong><p>${md(gift.text)}</p></article>`).join("")||"—"}${sacrifice}${abilityCard(actor?.ability,"ability","Способность")}${abilityCard(actor?.taintedAbility,"taintedAbility","Способность Порченого тела")}</section>`;
  const combat=`<section class="scene-sheet-pane" ${paneAttrs("combat")}>${sceneActionPanel(actor)}<details class="full-rules"><summary>Полные тексты Техник</summary>${techs.map(({tech,level})=>`<article class="kit-item"><strong>${esc(tech.name)} · ${level}</strong>${tech.levels.slice(0,level).map(rule=>`<p><b>${rule.n}: ${esc(rule.name)}</b> — ${md(rule.text)}</p>`).join("")}</article>`).join("")||"—"}</details></section>`;
  const utility=`<section class="scene-sheet-pane" ${paneAttrs("utility")}><h3>Броски и ресурсы</h3><p>Нажмите Навык или Способность: компаньон перенесёт её в XD6-компоновщик, где останется выбрать подходящий Атрибут.</p>${sceneRollShortcuts(actor)}${usesWounds?`<h4>Раны</h4><div class="button-row"><button type="button" data-scene-wound="external">Рана извне (+1 Влияние)</button><button type="button" data-scene-wound="self">Рана от себя</button></div>`:""}</section>`;
  return actor?`<div data-scene-sheet-actor="${actor.id}">${tabs+main+bio+combat+utility}</div>`:tabs+main+bio+combat+utility;
}
function openSceneRollPreset({actorId="",skillId="",abilityKey=""}={}){const hero=Scene.actors.find(item=>item.id===actorId)||(activeSceneView()==="gm"?Scene.actors.find(item=>item.id===Scene.selectedActor):null)||Scene.actors.find(item=>item.heroId===S.id);if(!hero)return toast("Сначала добавьте или обновите героя на Сцене");activeUtilityPreset={skillId:String(skillId||""),abilityKey:String(abilityKey||"")};activeUtilityActorId=hero.id;renderSceneUtility();setScenePanel("utility");requestAnimationFrame(()=>$("scene-dice-attr")?.focus())}
function sceneUtilityActor(){const selected=Scene.actors.find(item=>item.id===Scene.selectedActor),preferred=Scene.actors.find(item=>item.id===activeUtilityActorId),hero=currentHeroActor();return preferred||(activeSceneView()==="gm"?(selected||Scene.actors.find(item=>!item.knockedOut)||hero):hero)}
function safeTechniqueEffectIds(entry){const names=entry?.mechanics?.conditional?[]:entry?.mechanics?.directEffects||[];if(names.length!==1)return[];return names.map(name=>D.effects.positive.concat(D.effects.negative).find(effect=>effect.name===name)?.id).filter(Boolean)}
function sceneNarratorRollHistory(){
  const rolls=(Scene.rollFeed||[]).slice(0,20);
  return `<section class="scene-dice-history"><header><div><span>ЖУРНАЛ СТОЛА</span><strong>История бросков</strong></div><small>${rolls.length} из 20</small></header>${rolls.length?`<div class="scene-dice-history-list">${rolls.map(roll=>{const time=roll.at?new Date(roll.at).toLocaleTimeString("ru-RU",{hour:"2-digit",minute:"2-digit",second:"2-digit"}):"—",threshold=Number(roll.dice?.threshold||4),criticalAt=Number(roll.dice?.criticalAt||6),dice=Array.isArray(roll.rolls)?roll.rolls:[];return `<article><div><strong>${esc(roll.actor||"Система")}</strong><time>${esc(time)}</time></div><span>${esc(roll.formula||"Бросок")} · ${Number(roll.successes||0)} усп.${Number(roll.crits||0)?` · ${Number(roll.crits)} крит.`:""}${roll.outcome?` · ${esc(roll.outcome)}`:""}</span>${dice.length?`<div class="scene-dice-history-values">${dice.map(value=>`<i class="${value>=criticalAt?"crit":value>=threshold?"success":""}">${value}</i>`).join("")}</div>`:""}</article>`}).join("")}</div>`:`<p>На сетевом столе ещё не было бросков.</p>`}</section>`;
}
function renderSceneUtility(){
  const root=$("scene-utility"),actor=sceneUtilityActor();if(!actor){root.innerHTML=`<p>Добавьте героя или выберите участника Сцены.</p>`;return}
  const sacrificeLabels={eye:"Глаз",arm:"Рука",leg:"Нога",tongue:"Язык",life:"Жизнь"},sacrificeOptions=Object.entries(sacrificeLabels).filter(([id])=>!(actor.sacrifices||[]).includes(id));
  const skills=(actor.skills||[]).filter(skill=>skillDisplayName(skill,actor).trim()),abilities=[["ability",actor.ability],["taintedAbility",actor.taintedAbility]].filter(([,ability])=>Boolean(ability)),darkUrge=(actor.gifts||[]).includes("wolf.dark-urge"),gm=activeSceneView()==="gm",sync=Sync?.state(),canRequest=gm&&Boolean(sync?.sceneId&&sync.canNarrate&&actor.team==="hero"),request=Scene.challengeRequest,recent=(Scene.rollFeed||[]).filter(roll=>gm||roll.visibility!=="gm").slice(0,3),actorPicker=gm?`<label class="scene-dice-actor-picker">От имени<select id="scene-dice-actor-select">${Scene.actors.filter(item=>!item.knockedOut).map(item=>`<option value="${item.id}" ${item.id===actor.id?"selected":""}>${esc(item.name)} · ${item.team==="enemy"?"враг":"герой"}</option>`).join("")}</select></label>`:`<div class="scene-dice-actor"><span>От имени</span><strong>${esc(actor.name)}</strong></div>`;
  root.innerHTML=`${actorPicker}<div class="scene-dice-grid"><label>Атрибут<select id="scene-dice-attr"><option value="body">Тело · ${actor.attrs?.body||0}</option><option value="talent">Талант · ${actor.attrs?.talent||0}</option><option value="spirit">Дух · ${actor.attrs?.spirit||0}</option><option value="mind">Разум · ${actor.attrs?.mind||0}</option></select></label><label>Навык<select id="scene-dice-skill"><option value="">Без Навыка</option>${skills.map(skill=>`<option value="${esc(skill.id)}">${esc(skillDisplayName(skill,actor))} · +${skill.rank}D6</option>`).join("")}</select></label><label>Способность<select id="scene-dice-ability"><option value="">Без Способности</option>${abilities.map(([key,ability])=>`<option value="${key}">${esc(ability.name)} · +${ability.rank}D6</option>`).join("")}</select></label><label>Костей вручную<input id="scene-dice-pool" type="number" min="1" max="300" placeholder="По листу"></label><label>Сложность<input id="scene-dice-target" type="number" min="1" max="99" value="${Number(actor.tier||1)+1}"></label><label>Преимущество<input id="scene-dice-adv" type="number" min="0" max="30" value="0"></label><label>Помеха<input id="scene-dice-dis" type="number" min="0" max="30" value="0"></label></div>${darkUrge?`<label id="scene-dark-urge-wrap" class="switch"><input id="scene-dark-urge" type="checkbox"><span>Тёмный порыв: +4 Преимущества к проверке со Способностью</span></label>`:""}<output id="scene-dice-total" class="scene-dice-total">Выберите Атрибут и подходящие особенности</output><p class="muted">Ручной пул игнорирует характеристики листа. Бросок из этого инструмента не учитывает расстановку структурированного боя.</p><div class="scene-dice-actions"><button type="button" class="primary scene-roll-button" data-scene-roll="${actor.id}">Бросить сейчас</button>${canRequest?`<button type="button" data-scene-request-roll="${actor.id}">${request?.actorId===actor.id&&!request.result?"Обновить запрос":"Запросить у игрока"}</button>`:""}</div>${request?`<p class="scene-dice-request-state">${request.result?`Получено: ${request.result.successes} Успехов`:`Ожидается бросок · цель ${request.target}`}</p>`:""}${recent.length?`<div class="scene-dice-recent"><strong>Последние броски</strong>${recent.map(roll=>`<span>${esc(roll.actor||"Система")} · ${roll.successes} Усп. · ${esc(roll.outcome||roll.formula)}</span>`).join("")}</div>`:""}`;
  if(gm)root.insertAdjacentHTML("beforeend",sceneNarratorRollHistory());
  if((actor.gifts||[]).includes("cursed.sacrifice")&&sacrificeOptions.length&&recent.some(roll=>!roll.pending&&!roll.sacrificedBy)){root.insertAdjacentHTML("beforeend",`<div class="scene-dice-recent"><strong>Жертва · сделать бросок Крайним успехом</strong><label>Цена <select data-scene-sacrifice-choice>${sacrificeOptions.map(([id,label])=>`<option value="${id}">${label}</option>`).join("")}</select></label>${recent.filter(roll=>!roll.pending&&!roll.sacrificedBy).map(roll=>`<button type="button" data-scene-sacrifice-roll="${esc(roll.id)}">Применить к броску ${esc(roll.actor||"Система")} · ${roll.successes} Усп.</button>`).join("")}</div>`)}
  const skill=$("scene-dice-skill"),ability=$("scene-dice-ability");if([...skill.options].some(option=>option.value===activeUtilityPreset.skillId))skill.value=activeUtilityPreset.skillId;if([...ability.options].some(option=>option.value===activeUtilityPreset.abilityKey))ability.value=activeUtilityPreset.abilityKey;updateSceneDiceTotal(actor);
}
function sceneDiceRequest(actor){
  const attr=$("scene-dice-attr")?.value,skill=(actor.skills||[]).find(item=>item.id===$("scene-dice-skill")?.value),abilityKey=$("scene-dice-ability")?.value,ability=abilityKey?actor[abilityKey]:null,manual=$("scene-dice-pool")?.valueAsNumber,baseCount=Number.isFinite(manual)?clamp(manual,1,300):Math.max(1,Number(actor.attrs?.[attr]||0)+Number(skill?.rank||0)+Number(ability?.rank||0));
  return{scope:"challenge",sceneContext:false,baseCount,advantage:clamp($("scene-dice-adv")?.value,0,30),hindrance:clamp($("scene-dice-dis")?.value,0,30),attribute:attr,usesAbility:Boolean(ability),abilityKey:abilityKey||null,selectedHookIds:$("scene-dark-urge")?.checked?["wolf.dark-urge"]:[],targetIds:[]}
}
function updateSceneDiceTotal(actor){const output=$("scene-dice-total");if(!output||!actor)return;const request=sceneDiceRequest(actor),status=SceneEngine.diceHookStatus(Scene,actor.id,request),dark=$("scene-dark-urge"),wrap=$("scene-dark-urge-wrap");if(dark){dark.disabled=!request.usesAbility;if(!request.usesAbility)dark.checked=false}if(wrap)wrap.classList.toggle("disabled",!request.usesAbility);if(!status.available){output.textContent=status.reason;return}const automatic=status.sources.filter(source=>["advantage","hindrance"].includes(source.type)).map(source=>`${source.type==="hindrance"?"−":"+"}${source.amount} ${source.label}`);output.innerHTML=`Итого <strong>${status.count}D6</strong><small>${esc([`${request.baseCount} базовых`,request.advantage?`+${request.advantage} ручное Преимущество`:"",request.hindrance?`−${request.hindrance} ручная Помеха`:"",...automatic].filter(Boolean).join(" · "))}</small>`}
function sceneReferenceId(item){const raw=String(item?.id||"").toLowerCase().replace(/[^a-z0-9._:-]+/g,"-").replace(/^-+/,"");return(raw||`ref-${ruleKey(`${item?.kind||""}:${item?.name||""}:${item?.text||""}`)}`).slice(0,180)}
const SCENE_RULE_SECTIONS=[
  {id:"combat",label:"Бой",chapter:"combat"},
  {id:"field",label:"Поле",chapter:"field"},
  {id:"actions",label:"Действия",chapter:"actions"},
  {id:"effects",label:"Эффекты",chapter:"actions"},
  {id:"enemies",label:"Враги",chapter:"enemies"},
];
function sceneRuleChapter(id){return RULE_CHAPTERS.find(chapter=>chapter.id===id)}
function sceneCombatReferenceItems(section=null){
  if(sceneUsesLionwing()){
    const core=localizedLionwingCoreRules(),group=id=>id.includes("spatial")||id.includes("terrain")?"field":id.startsWith("lionwing.narrator")?"enemies":"combat";
    const items=[...core.actions.list.map(action=>({...action,cost:actionCostLabel(action),kind:"Действие LionWing",section:"actions"})),...[...core.effects.positive,...core.effects.negative].map(effect=>({...effect,kind:"Эффект LionWing",section:"effects"})),...core.rules.map(rule=>({...rule,kind:rule.category,section:group(rule.id)}))];
    return section?items.filter(item=>item.section===section):items;
  }
  const chapterItems=chapterId=>(sceneRuleChapter(chapterId)?.cards||[]).map(([name,text])=>({id:`scene-${chapterId}-${ruleKey(`${name}:${text}`)}`,name,text,kind:sceneRuleChapter(chapterId)?.name||"Боевое правило",section:chapterId,fullRuleId:`rule-${chapterId}-${ruleKey(`${name}:${text}`)}`}));
  const actions=D.actions.list.map(action=>({...action,id:`scene-action-${ruleKey(`${action.name}:${action.text}`)}`,kind:`Действие · ${action.group}`,section:"actions",fullRuleId:`rule-action-${ruleKey(`${action.name}:${action.text}`)}`}));
  const effectData=sceneEffects(),effects=[...effectData.positive.map(effect=>({...effect,id:`scene-positive-${ruleKey(`${effect.name}:${effect.text}`)}`,kind:"Положительный Эффект",section:"effects",fullRuleId:`rule-positive-effect-${ruleKey(`${effect.name}:${effect.text}`)}`})),...effectData.negative.map(effect=>({...effect,id:`scene-negative-${ruleKey(`${effect.name}:${effect.text}`)}`,kind:"Отрицательный Эффект",section:"effects",fullRuleId:`rule-negative-effect-${ruleKey(`${effect.name}:${effect.text}`)}`}))];
  const items=[...chapterItems("combat"),...chapterItems("field"),...actions,...effects,...chapterItems("enemies")];return section?items.filter(item=>item.section===section):items;
}
function sceneReferenceItem(ruleId){return sceneCombatReferenceItems().find(item=>sceneReferenceId(item)===ruleId)||referenceItems().find(item=>sceneReferenceId(item)===ruleId)}
function ruleExternalLink(item){const url=new URL(location.href);url.search="";url.hash=item.fullRuleId||"";url.searchParams.set("mode",item.fullRuleId?"rules":"reference");if(!item.fullRuleId)url.searchParams.set("q",item.name||"");return url.href}
function renderSceneReference(){
  const root=$("scene-reference-list"),q=sceneReferenceQuery.trim().toLowerCase(),section=SCENE_RULE_SECTIONS.find(item=>item.id===sceneReferenceSection)||SCENE_RULE_SECTIONS[0],pool=sceneCombatReferenceItems(q?null:section.id),items=pool.filter(item=>!q||`${item.name} ${item.kind} ${item.text||""} ${item.group||""}`.toLowerCase().includes(q)).slice(0,q?60:36),latest=Scene.ruleHandouts?.[0],handout=latest?`<article class="scene-rule-handout"><span>${esc(latest.sharedBy)} показывает</span><strong>${esc(latest.title)}</strong><button type="button" data-scene-rule-open="${esc(latest.ruleId)}">Открыть</button></article>`:"",gm=activeSceneView()==="gm",chapter=sceneRuleChapter(section.chapter),tabs=`<nav class="scene-rule-sections" aria-label="Разделы боевых правил">${SCENE_RULE_SECTIONS.map(item=>`<button type="button" class="${item.id===section.id?"on":""}" data-scene-rule-section="${item.id}">${esc(item.label)}</button>`).join("")}</nav>`,intro=!q?`<aside class="scene-rule-section-intro"><div><strong>${esc(section.id==="effects"?"Эффекты":chapter?.name||section.label)}</strong><span>${esc(section.id==="effects"?"Положительные и отрицательные состояния персонажей.":chapter?.desc||"")}</span></div><a href="?mode=rules#rules-${esc(section.chapter)}">Полная глава →</a></aside>`:`<p class="scene-rule-search-state">Результаты по всем боевым разделам</p>`;
  const markup=tabs+handout+intro+(items.map(item=>{const id=sceneReferenceId(item);return`<details class="scene-reference-card" data-scene-rule-id="${esc(id)}"><summary><span>${esc(item.kind)}</span><strong>${esc(item.name)}${item.cost?` · ${esc(item.cost)}`:""}</strong></summary><div>${md(item.text||"")}</div><footer><a href="${esc(ruleExternalLink(item))}">В полной главе</a><button type="button" data-scene-rule-copy="${esc(id)}">Копировать ссылку</button>${gm?`<button type="button" class="primary" data-scene-rule-share="${esc(id)}">Показать столу</button>`:""}</footer></details>`}).join("")||`<p class="autosave">В боевых правилах ничего не найдено.</p>`);if(sceneReferenceMarkup!==markup){const openRuleIds=new Set([...root.querySelectorAll("details[open][data-scene-rule-id]")].map(details=>details.dataset.sceneRuleId));root.innerHTML=markup;sceneReferenceMarkup=markup;root.querySelectorAll("[data-scene-rule-id]").forEach(details=>details.open=openRuleIds.has(details.dataset.sceneRuleId))}
}
function sceneTrayHeroActor(){
  const local=currentHeroActor();
  if(activeSceneView()!=="gm")return local;
  const selected=Scene.actors.find(item=>item.id===Scene.selectedActor);
  if(selected)return selected.team==="hero"&&(selected.kind==="hero"||selected.heroId)&&!selected.knockedOut?selected:null;
  const active=Scene.actors.find(item=>item.id===Scene.activeActorId);
  if(active)return active.team==="hero"&&(active.kind==="hero"||active.heroId)&&!active.knockedOut?active:null;
  return local?.team==="hero"&&(local.kind==="hero"||local.heroId)&&!local.knockedOut?local:null;
}
function mountSceneMapTools(){
  const root=$("scene-map-tools"),tableRoot=$("scene-table-tools");
  if(!root||!tableRoot)return;
  let picker=root.querySelector(".scene-map-tool-picker");
  if(!picker){picker=document.createElement("div");picker.className="scene-map-tool-picker";root.prepend(picker)}
  for(const cluster of $$('[data-tool-cluster="create"],[data-tool-cluster="edit"]'))if(cluster.parentElement!==picker)picker.append(cluster);
  const suggestions=$("scene-rule-suggestions");if(suggestions&&suggestions.parentElement!==root)root.append(suggestions);
  for(const id of ["scene-area-controls","scene-wall-controls","scene-marker-controls","scene-topology-controls"]){const controls=$(id);if(controls&&controls.parentElement!==root)root.append(controls)}
  const management=document.querySelector(".scene-add-body > .scene-management");
  if(management){
    const divider=management.querySelector("hr"),tableManagement=document.createElement("section");
    tableManagement.className="scene-management scene-table-management";
    if(divider){while(divider.nextSibling)tableManagement.append(divider.nextSibling);divider.remove()}
    management.classList.add("scene-space-management");root.append(management);tableRoot.append(tableManagement);
  }
}
function renderSceneLayoutSettings(){
  const root=$("scene-layout-settings");if(!root)return;
  const panels=[['director','Пульт'],['inspector','Цель'],['sheet','Герой'],['utility','Бросок'],['reference','Правила'],['roster','Враги'],['media','Арт'],['map','Карта'],['add','Добавить'],['table','Стол'],['network','Сеть'],['log','Журнал']];
  const widthOptions=side=>Object.entries({compact:"Узкая · 300",normal:"Обычная · 380",wide:"Широкая · 480"}).map(([value,label])=>`<option value="${value}" ${scenePanelWidths[side]===value?'selected':''}>${label}</option>`).join('');
  const version=`<section class="scene-interface-version"><span class="kind">ВЕРСИЯ ИНТЕРФЕЙСА</span><h3>${sceneInterfaceVersion==="next"?"Новый стол":"Классический стол"}</h3><p>${sceneInterfaceVersion==="next"?"Встроенные панели, компактные инструменты и отдельные рабочие области слева и справа.":"Стабильная компоновка из основной версии. Боевые данные и механики остаются теми же."}</p><label class="switch"><input id="scene-interface-next" type="checkbox" ${sceneInterfaceVersion==="next"?'checked':''}><span>Включить новый интерфейс стола</span></label><small>Переключение локальное: оно не меняет стол у других участников. Страница перезагрузится один раз.</small></section>`;
  const nextSettings=`<section><span class="kind">КОМПОНОВКА</span><h3>Рабочие панели</h3><p>Слева и справа можно держать по одной панели одновременно. Колонка кнопок всегда остаётся у правого края.</p><label>Расположение<select id="scene-panel-layout"><option value="split" ${scenePanelLayoutMode==="split"?'selected':''}>Пульт слева, рабочая панель справа</option><option value="right" ${scenePanelLayoutMode==="right"?'selected':''}>Все панели справа</option><option value="left" ${scenePanelLayoutMode==="left"?'selected':''}>Все панели слева</option><option value="custom" ${scenePanelLayoutMode==="custom"?'selected':''}>Настроить каждую</option></select></label><div class="scene-layout-widths"><label>Левая панель<select data-scene-panel-width="left">${widthOptions('left')}</select></label><label>Правая панель<select data-scene-panel-width="right">${widthOptions('right')}</select></label></div><label>Плотность<select id="scene-interface-density"><option value="compact" ${sceneInterfaceDensity==='compact'?'selected':''}>Компактно</option><option value="comfortable" ${sceneInterfaceDensity==='comfortable'?'selected':''}>Просторно</option></select></label><label class="switch"><input id="scene-turn-strip-visible" type="checkbox" ${sceneTurnStripVisible?'checked':''}><span>Показывать ленту участников игроку</span></label></section><div class="scene-layout-custom" ${scenePanelLayoutMode==="custom"?'':'hidden'}>${panels.map(([id,label])=>`<label><span>${label}</span><select data-scene-panel-side="${id}"><option value="left" ${scenePanelSides[id]==="left"?'selected':''}>Слева</option><option value="right" ${scenePanelSides[id]!=="left"?'selected':''}>Справа</option></select></label>`).join('')}</div>`;
  root.innerHTML=version+(sceneInterfaceVersion==="next"?nextSettings:"");
}
function sceneBattleComplete(){const heroes=Scene.actors.filter(actor=>actor.team==="hero"&&actor.kind!=="crowd"),enemies=Scene.actors.filter(actor=>actor.team==="enemy"&&actor.kind!=="crowd");return Boolean(heroes.length&&enemies.length&&(heroes.every(actor=>actor.knockedOut)||enemies.every(actor=>actor.knockedOut)))}
function renderSceneChrome(){
  const feed=$("scene-roll-feed"),now=Date.now(),rolls=(Scene.rollFeed||[]).filter(roll=>activeSceneView()==="gm"||roll.visibility!=="gm").slice(0,3).map(roll=>({...roll,feedAge:Math.max(0,now-(Date.parse(roll.at)||now))})).filter(roll=>roll.feedAge<9000);feed.hidden=!rolls.length;feed.innerHTML=[...rolls].reverse().map(roll=>{const sources=roll.dice?.sources?.map(source=>source.label).join(" · "),redirect=roll.redirected?Scene.actors.find(actor=>actor.id===roll.redirectTargetId)?.name||"другую цель":"";return`<article style="--feed-delay:-${Math.min(roll.feedAge,8999)}ms"><strong>${esc(roll.actor)}</strong><span>${esc(roll.formula||"Бросок")} · ${roll.successes} усп.${roll.crits?` · ${roll.crits} крит.`:""}${sources?` · ${esc(sources)}`:""}${redirect?` · перенаправлен: ${esc(redirect)}`:""}</span><div>${roll.rolls.slice(0,14).map(value=>`<i class="${value>=(roll.dice?.criticalAt||6)?"crit":value>=(roll.dice?.threshold||4)?"success":""}">${value}</i>`).join("")}${roll.rolls.length>14?`<small>+${roll.rolls.length-14}</small>`:""}</div></article>`}).join("");
  const tray=$("scene-action-tray"),actor=sceneTrayHeroActor(),gm=activeSceneView()==="gm",active=Scene.actors.find(item=>item.id===Scene.activeActorId),quickActionKeys=["step","skirmish","spell","breathe","charge"],actions=actor?SceneEngine.availableActions(Scene,D,actor.id).filter(action=>SceneEngine.actionIsAny(action,quickActionKeys)):[],heroTurn=Boolean(actor&&active?.id===actor.id&&!Scene.pendingAction&&!Scene.pendingPrompt),lock=Scene.pendingAction?"Сначала завершите Реакции":Scene.pendingPrompt?"Сначала ответьте на правило":!active?"Сначала начните Ход":active?.id!==actor?.id?`Сейчас Ход: ${active?.name}`:"",latestRoll=rolls[0],rollChip=latestRoll?`<button type="button" class="scene-tray-last-roll" data-open-scene-panel="utility" title="Открыть последний публичный бросок"><b>${latestRoll.successes} усп.</b><small>${latestRoll.crits?`${latestRoll.crits} крит. · `:""}${esc(latestRoll.actor)}</small></button>`:"",resultsButton=Scene.results?`<button type="button" class="primary" data-scene-session-action="results">К итогам</button>`:gm&&sceneBattleComplete()?`<button type="button" class="primary" data-scene-session-action="results">Завершить бой</button>`:"";tray.hidden=!actor&&!gm&&!resultsButton;tray.classList.toggle("is-locked",Boolean(actor&&!heroTurn));tray.innerHTML=tray.hidden?"":`${resultsButton}${rollChip}${actor?`<span class="scene-tray-resources"><b>${esc(actor.name)}</b>${sceneResourceChips(actor)}</span>${heroTurn?actions.map(action=>`<button type="button" data-core-action="${esc(action.id)}" data-core-actor="${esc(actor.id)}" ${action.available?"":"disabled"} title="${esc(action.reason||action.quickSource?.text||action.text)}">${esc(action.continuation?`Шаг · ${action.remaining} кл.`:sceneActionDisplayName(action))}${action.quick?`<small>Быстро</small>`:""}</button>`).join(""):`<span class="scene-tray-lock"><i>⌛</i><b>${esc(lock)}</b><small>Действия героя временно заблокированы</small></span>`}<button type="button" class="scene-more-actions" data-open-scene-panel="sheet" data-core-actor="${esc(actor.id)}" title="Все действия и Техники">Все действия</button>`:""}${gm?`<span class="scene-tray-gm"><button type="button" data-scene-session-action="round">Новый Раунд</button><button type="button" data-scene-session-action="scene">Новая Сцена</button></span>`:""}`;
}
const renderSceneChromeWithoutActionPlanLock=renderSceneChrome;
renderSceneChrome=function(){
  renderSceneChromeWithoutActionPlanLock();
  if(!Scene.pendingActionPlan)return;
  const tray=$("scene-action-tray");
  if(!tray)return;
  tray.classList.add("is-locked");
  for(const button of tray.querySelectorAll("[data-core-action]")){
    button.disabled=true;
    button.title="Сначала завершите или отмените составное действие";
  }
};
function sceneTurnStatusAfterCurrent(actor){
  if(!actor)return{available:false,reason:"Участник не найден"};
  const current=Scene.actors.find(item=>item.id===Scene.activeActorId);if(!current||current.id===actor.id)return SceneEngine.turnStartStatus(Scene,actor.id);
  try{return SceneEngine.turnStartStatus(SceneEngine.dispatch(Scene,{type:"turn.end",actorId:current.id,payload:{}}).scene,actor.id)}catch(error){return{available:false,reason:error.message||"Нельзя начать этот Ход"}}
}
function sceneActionDisplayName(action){
  if(window.DAWN_LIONWING_ENGINE?.isScene(Scene))return localizedLionwingCoreRules().actions.list.find(item=>item.id===action.id)?.name||action.name;
  return action.name;
}
function refreshSceneControlStates(){
  for(const button of $$("#scene-inspector .enemy-rule button:disabled, #scene-enemy-roster .enemy-rule button:disabled")){const reason=button.title?.trim(),meta=button.querySelector("small");if(reason&&meta)meta.textContent=reason}
  for(const button of $$("[data-scene-turn-actor]")){const actor=Scene.actors.find(item=>item.id===button.dataset.sceneTurnActor);if(!actor||Scene.activeActorId===actor.id)continue;const status=sceneTurnStatusAfterCurrent(actor);if((actor.kind==="enemy"||actor.profileId)&&actor.acted&&status.available){const meta=button.querySelector("small");if(meta)meta.textContent=`Можно повторить · ${actor.hp}/${actor.maxHp||"—"} ЗД`;button.title=`${actor.name} · доступен повторный Ход`}}
  for(const button of $$("[data-enemy-turn]")){const actor=Scene.actors.find(item=>item.id===button.dataset.enemyActor),active=actor&&Scene.activeActorId===actor.id,status=active?{available:true,reason:""}:sceneTurnStatusAfterCurrent(actor);button.disabled=Boolean(!actor||actor.knockedOut||!status.available);button.title=active?"Завершить текущий Ход":status.reason||"Начать Ход"}
  for(const button of $$("#scene-inspector [data-scene-turn='start']")){const actor=Scene.actors.find(item=>item.id===button.dataset.sceneActorId),active=actor&&Scene.activeActorId===actor.id,status=active?{available:false,reason:"Этот участник уже ходит"}:sceneTurnStatusAfterCurrent(actor);button.disabled=Boolean(!actor||actor.knockedOut||!status.available);button.title=status.reason||"Начать Ход"}
  const roundStatus=SceneEngine.roundEndStatus(Scene);for(const button of $$("#new-round, [data-scene-session-action='round']")){button.disabled=!roundStatus.available;button.title=roundStatus.reason||"Завершить Раунд"}
}
function rollSceneDice(actorId){
  const actor=Scene.actors.find(item=>item.id===actorId);if(!actor)return toast("Участник броска больше не доступен");
  const request=sceneDiceRequest(actor),status=SceneEngine.diceHookStatus(Scene,actor.id,request);if(!status.available)return toast(status.reason);const result=Logic.rollXd6({count:status.count,threshold:status.threshold,criticalAt:status.criticalAt}),prepared=SceneEngine.diceRollPayload(Scene,actor.id,request,result);if(!prepared.available)return toast(prepared.reason);const target=clamp($("scene-dice-target")?.value||Number(actor.tier||1)+1,1,99),outcome=prepared.payload.successes>=target*2?"Крайний успех":prepared.payload.successes>=target?"Минимальный успех":"Провал",payload={...prepared.payload,target,outcome};
  const sceneRollEvent={type:"roll.public",actorId:actor.id,payload},committed=commitSceneEvents(`Публичный бросок: ${actor.name}`,[sceneRollEvent]);if(!committed)return;if(committed.pending&&typeof applyOptimisticToolsEvents==="function"){applyOptimisticToolsEvents([sceneRollEvent]);renderScene()}toast(`${actor.name}: ${payload.successes} успехов · ${outcome}`)
}
function applySceneZoom(next=sceneZoom){sceneZoom=clamp(next,30,180);const board=$("scene-board"),input=$("scene-zoom"),output=$("scene-zoom-value");board?.style.setProperty("--scene-zoom",String(sceneZoom/100));if(input)input.value=sceneZoom;if(output)output.textContent=`${sceneZoom}%`;persist()}
function fitSceneZoom(comfortable=false){const wrap=$("scene-board-wrap"),space=activeSceneSpace();if(!wrap||!space)return;const width=Math.max(246,wrap.clientWidth-24),height=Math.max(246,wrap.clientHeight-24),boardWidth=820,boardHeight=boardWidth*(space.height/space.width),fitted=clamp(Math.floor(Math.min(width/boardWidth,height/boardHeight)*100)-1,30,180),next=comfortable&&sceneViewportMode==="desktop"?Math.max(58,fitted):fitted;sceneNeedsInitialFit=false;applySceneZoom(next)}
function reconcileLocalSceneFlow(){
  let reset=false;
  const hero=currentHeroActor(),pendingStatus=SceneEngine.pendingActionStatus(Scene,D);
  if(pendingCoreReaction&&!pendingStatus.waitingIds.includes(pendingCoreReaction.actorId)){pendingCoreReaction=null;reset=true}
  if(pendingCoreAction){const action=pendingCoreActionPlan?SceneEngine.cunningPlanStatus(Scene,D,hero?.id,pendingCoreAction):SceneEngine.availableActions(Scene,D,hero?.id).find(item=>item.id===pendingCoreAction);if(!hero||!action?.available){pendingCoreAction=null;pendingCoreActionPlan=false;reset=true}}
  if(pendingEnemyRule){const rule=SceneEngine.availableEnemyRules(Scene,D,pendingEnemyRule.actorId).find(item=>item.id===pendingEnemyRule.ruleId);if(!rule?.available){pendingEnemyRule=null;reset=true}}
  if(pendingEnemyStepActorId){const actor=Scene.actors.find(item=>item.id===pendingEnemyStepActorId),step=SceneEngine.actionByKey(D,"step"),action=actor&&step&&SceneEngine.availableActions(Scene,D,actor.id).find(item=>item.id===step.id);if(!action?.available){pendingEnemyStepActorId=null;reset=true}}
  if(pendingTechniqueRule&&(!hero||hero.knockedOut||Scene.pendingAction||Scene.activeActorId!==hero.id)){pendingTechniqueRule=null;scenePreviewCells.clear();reset=true}
  if(pendingZealotPlan&&(!hero||hero.id!==pendingZealotPlan.actorId||hero.knockedOut||Scene.pendingAction||Scene.activeActorId!==hero.id)){pendingZealotPlan=null;scenePreviewCells.clear();reset=true}
  if(reset)Scene.tool="select";
}
function renderSceneTurnStrip(){
  const playerView=activeSceneView()==="player",actors=Scene.actors.filter(actor=>actor.kind!=="crowd"&&!SceneEngine.isEnemyModifier?.(actor)&&(!playerView||!actor.hidden)),root=$("scene-turn-strip");
  root.innerHTML=actors.map(actor=>{const active=Scene.activeActorId===actor.id,status=actor.knockedOut?"Вне боя":active?"Сейчас Ход":actor.acted?"Ход завершён":`${actor.ap} ОД`,image=actor.tokenImage||actor.portraitImage,compound=SceneEngine.compoundEnemyStatus(Scene,actor.id),health=compound.active?`${compound.hp}/${compound.maxHp} общие ЗД · часть`:`${actor.hp}/${actor.maxHp||"—"} ЗД`;return `<button type="button" class="scene-turn-chip ${actor.team} ${active?"active":""} ${actor.acted?"acted":""} ${actor.knockedOut?"knocked-out":""}" data-scene-turn-actor="${actor.id}" title="${esc(`${actor.name} · ${status}${compound.active?` · часть составного врага ${compound.id}`:""}`)}" ${actor.knockedOut?"disabled":""}>${image?`<img src="${image}" alt="">`:`<i style="--token-color:${actor.tokenColor}">${esc(actor.tokenSymbol||actor.name.slice(0,2).toUpperCase()||"?")}</i>`}<span><b>${esc(actor.name)}</b><small>${esc(status)} · ${esc(health)}</small></span></button>`}).join("")||`<span class="autosave">Добавьте участников Сцены</span>`;
}
function appendMarkerClockControls(){if(Scene.selectedActor||activeSceneView()!=="gm")return;for(const button of $("scene-inspector").querySelectorAll("[data-scene-remove-marker]")){const marker=Scene.markers.find(item=>item.id===button.dataset.sceneRemoveMarker),clock=marker?.metadata?.clock;if(!clock)continue;button.insertAdjacentHTML("beforebegin",`<div class="marker-clock-control"><strong>Часы · ${Number(clock.value||0)}/${Number(clock.size||4)}</strong><div class="button-row compact"><button type="button" data-scene-marker-clock="${marker.id}" data-delta="-1" ${Number(clock.value||0)<=0?"disabled":""}>−1</button><button type="button" data-scene-marker-clock="${marker.id}" data-delta="1" ${Number(clock.value||0)>=Number(clock.size||4)?"disabled":""}>+1</button></div></div>`)} }
function renderSceneResults(){
  const dialog=$("scene-results"),body=$("scene-results-body");if(!dialog||!body)return;
  const actors=new Map(Scene.actors.map(actor=>[actor.id,{name:actor.name||"Неизвестный участник",team:actor.team}])),stats=new Map(),rolls=[],moments=[];
  const stat=(id,name,team)=>{const key=id||name||"system";if(!stats.has(key))stats.set(key,{name:name||actors.get(id)?.name||"Неизвестный участник",team:team||actors.get(id)?.team||"neutral",damage:0,taken:0,heal:0,defenses:0,prevented:0});return stats.get(key)};
  for(const row of [...(Scene.log||[])].reverse()){const p=row.payload||{},source=actors.get(row.actorId),actor=p.actorName||source?.name||(row.actorId?"Неизвестный участник":"Система"),target=p.targetName||actors.get(p.targetId)?.name||"цель";if(row.type==="damage.apply"&&!p.ignored){const dealt=Math.max(0,Number(p.dealt??p.amount??0)),raw=Math.max(dealt,Number(p.amount??dealt));stat(row.actorId,actor,source?.team).damage+=dealt;stat(p.targetId,target,actors.get(p.targetId)?.team).taken+=dealt;stat(p.targetId,target,actors.get(p.targetId)?.team).prevented+=Math.max(0,raw-dealt);if(dealt>0)moments.push({value:dealt,text:`${actor} наносит ${dealt} урона · ${target}`})}if(row.type==="actor.heal")stat(row.actorId,actor,source?.team).heal+=Number(p.restored??p.amount??0);if(row.type==="reaction.respond"&&["block","dodge","clash"].some(kind=>String(p.choice||"").includes(kind)))stat(row.actorId,actor,source?.team).defenses++;if(row.type==="roll.public")rolls.push({actor,formula:p.formula||"Бросок",rolls:Array.isArray(p.rolls)?p.rolls:[],successes:Number(p.successes||0),crits:Number(p.crits||0)});}
  const participants=[...stats.values()].filter(item=>item.name!=="Система").sort((a,b)=>b.damage-a.damage),totalDamage=participants.reduce((n,s)=>n+s.damage,0),totalHeal=participants.reduce((n,s)=>n+s.heal,0),totalDefense=participants.reduce((n,s)=>n+s.defenses,0),bestHit=moments.sort((a,b)=>b.value-a.value)[0],bestRoll=[...rolls].sort((a,b)=>b.successes-a.successes||b.crits-a.crits)[0];
  const cards=participants.map((s,index)=>`<article class="results-combatant ${s.team}"><i>${index+1}</i><div><strong>${esc(s.name)}</strong><small>${s.team==="hero"?"Герой":s.team==="enemy"?"Противник":"Участник"}</small></div><dl><div><dt>Нанёс</dt><dd>${s.damage}</dd></div><div><dt>Получил</dt><dd>${s.taken}</dd></div><div><dt>Защита</dt><dd>${s.defenses}${s.prevented?` · −${s.prevented}`:""}</dd></div><div><dt>Лечение</dt><dd>${s.heal}</dd></div></dl></article>`).join("");
  const replay=rolls.map(r=>`<li><span class="results-dice-name"><b>${esc(r.actor)}</b><small>${esc(r.formula)}</small></span><span class="results-dice">${r.rolls.map(value=>`<i class="${value===6?"crit":value>=4?"success":""}">${value}</i>`).join("")}</span><strong>${r.successes} усп.${r.crits?` · ${r.crits} крит.`:""}</strong></li>`).join("");
  body.innerHTML=`<section class="results-hero"><span>СЦЕНА ЗАВЕРШЕНА</span><h3>${esc(Scene.name||"Структурированный бой")}</h3><p>${Scene.round||1} ${Number(Scene.round)===1?"раунд":"раундов"} · ${(Scene.log||[]).length} событий</p></section><section class="results-grid"><div class="damage"><small>Нанесено урона</small><strong>${totalDamage}</strong></div><div class="defense"><small>Защитных реакций</small><strong>${totalDefense}</strong></div><div class="healing"><small>Восстановлено ЗД</small><strong>${totalHeal}</strong></div><div class="rolls"><small>Публичных бросков</small><strong>${rolls.length}</strong></div></section><section class="results-awards"><article><span>СИЛЬНЕЙШИЙ УДАР</span><strong>${esc(bestHit?.text||"—")}</strong></article><article><span>ЛУЧШИЙ БРОСОК</span><strong>${bestRoll?`${esc(bestRoll.actor)} · ${bestRoll.successes} успехов`:"—"}</strong></article></section><section><header class="results-section-head"><div><span>ВКЛАД В БОЙ</span><h3>Участники</h3></div></header><div class="results-combatants">${cards||`<p class="autosave">Боевых данных пока нет.</p>`}</div></section><details class="results-replay"><summary><span><b>Реплей бросков</b><small>Значения кубиков и результаты</small></span><strong>${rolls.length}</strong></summary><ol>${replay||"<li>Публичных бросков нет</li>"}</ol></details>`;
}
function showSceneResults(){renderSceneResults();const checkbox=$("scene-results-clear-table"),dialog=$("scene-results");if(checkbox)checkbox.checked=false;if(dialog&&!dialog.open)dialog.showModal()}
function reconcileSceneResultsDialog(){const id=Scene.results?.id||"";if(!id||id===lastAutoOpenedSceneResultsId)return;lastAutoOpenedSceneResultsId=id;showSceneResults()}
function renderScene(){
  reconcileLocalSceneFlow();
  if(usingNextSceneInterface())mountSceneMapTools();
  renderSceneLayoutSettings();
  document.body.classList.toggle("scene-turn-strip-hidden",usingNextSceneInterface()&&!sceneTurnStripVisible);
  document.body.classList.toggle("scene-density-comfortable",usingNextSceneInterface()&&sceneInterfaceDensity==="comfortable");
  const workbench=$("scene-workbench"),view=activeSceneView(),forcedPlayer=Sync?.state().sceneId&&Sync.state().role==="player",tool=activeSceneTool();workbench.classList.toggle("player-view",view==="player");document.body.classList.toggle("scene-player-view",view==="player");$("scene-view").value=view;$("scene-view").disabled=Boolean(forcedPlayer);$("scene-name").textContent=Scene.name;$("scene-round-value").textContent=Scene.round;$("scene-tension-value").textContent=Scene.tension;const active=Scene.actors.find(actor=>actor.id===Scene.activeActorId);$("scene-active-turn").textContent=active?`Ход: ${active.name}`:"Ход не начат";$("scene-space-tabs").hidden=Scene.spaces.length<2;$("scene-space-tabs").innerHTML=Scene.spaces.map(space=>`<button type="button" class="${space.id===Scene.activeSpace?"on":""}" data-scene-space="${space.id}">${esc(space.name)} <small>${Scene.actors.filter(actor=>actor.space===space.id).length}</small></button>`).join("");$$('[data-scene-tool]').forEach(button=>{button.classList.toggle("on",button.dataset.sceneTool===tool);button.setAttribute("aria-pressed",button.dataset.sceneTool===tool)});$("scene-area-controls").hidden=tool!=="area";$("scene-marker-controls").hidden=tool!=="marker";$("scene-topology-controls").hidden=tool!=="topology";const selected=Scene.actors.find(actor=>actor.id===Scene.selectedActor),targets=Scene.actors.filter(actor=>Scene.targetIds.includes(actor.id)),templates=availableSceneTemplates(),promptMarker=canControlScenePrompt()?Scene.markers.find(marker=>marker.id===Scene.pendingPrompt?.context?.markerId):null;$("scene-selection-summary").textContent=promptMarker?`Перемещение: ${promptMarker.label} — выберите подсвеченную клетку`:tool==="place"?selected?`Ручная перестановка: ${selected.name} — выберите клетку`:"Ручная перестановка: выберите участника":tool==="topology"?`Разрыв поля: выбрано ${sceneTopologyCells.size} клеток`:sceneMeasureLabel||targets.length?sceneMeasureLabel||`Цели (${targets.length}): ${targets.map(actor=>actor.name).join(", ")}`:selected?`Выбран: ${selected.name}`:"Нет выбранных целей";$("scene-topology-apply").disabled=!sceneTopologyCells.size;$("scene-rule-suggestions").innerHTML=templates.length?`<strong>С листа:</strong>${templates.map((template,index)=>`<button type="button" data-scene-template="${index}">${esc(template.name)}</button>`).join("")}`:"";$("scene-undo").disabled=!(Scene.undo.length||(Scene.turnUndo||[]).length);renderSceneSources();renderEnemySelect();renderSceneManager();renderSceneBoard();renderSceneInspector();renderSceneMedia();$("scene-log").innerHTML=Scene.log.map(row=>`<li><time>${esc(row.at)}</time>${esc(row.text||eventText(row))}</li>`).join("")||`<li class="autosave">Изменения Сцены появятся здесь.</li>`;
  $("scene-clear-targets").disabled=!targets.length&&!Scene.targetCells?.length;
  $("scene-wall-controls").hidden=tool!=="wall";appendTerrainRepairControls();appendSceneWallInspector();appendMarkerClockControls();if($("scene-redo"))$("scene-redo").disabled=!Scene.redo?.length;
  document.body.dataset.sceneControlMode=sceneControlMode;document.body.dataset.sceneTurnApprovalMode=sceneTurnApprovalMode();$("scene-control-mode").value=sceneControlMode;$("scene-turn-approval-mode").value=sceneTurnApprovalMode();const ready=Scene.actors.filter(actor=>!actor.knockedOut&&!actor.acted&&actor.kind!=="crowd"&&!SceneEngine.isEnemyModifier?.(actor)).length,down=Scene.actors.filter(actor=>actor.knockedOut).length;$("scene-encounter-status").textContent=!Scene.actors.length?"Подготовка":`${ready} готовы${down?` · ${down} вне боя`:""}`;
  renderSceneTurnStrip();renderSceneFlow();renderSceneRoster();renderSceneDirector();renderSceneUtility();renderSceneReference();renderGmLibraries();renderCompoundBuilder();renderSceneChrome();refreshSceneControlStates();applySceneZoom();setScenePanel(activeScenePanel);setSheetTab(activeSheetTab);if(sceneNeedsInitialFit)requestAnimationFrame(()=>fitSceneZoom(true));
  document.body.classList.toggle("scene-player-view",store.mode==="play"&&view==="player");
  $("scene-log").innerHTML=Scene.log.filter(row=>activeSceneView()==="gm"||row.visibility!=="gm").map(row=>`<li><time>${esc(String(row.at||"").slice(11,19)||row.at)}</time>${esc(clockEventText(row)||wallEventText(row)||eventText(row))}</li>`).join("")||`<li class="autosave">Изменения Сцены появятся здесь.</li>`;
}
function activeSceneView(){const sync=Sync?.state();return sync?.sceneId?(sync.canNarrate?"gm":"player"):Scene.view}
