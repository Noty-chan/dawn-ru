import assert from "node:assert/strict";
import fs from "node:fs";
import vm from "node:vm";
import { loadSceneEngine } from "./load-scene-engine.mjs";

const context={window:{},console};vm.createContext(context);
for(const file of ["data.js","edition-lionwing.js","lionwing-table-data.js","logic.js"])
  vm.runInContext(fs.readFileSync(new URL(`../${file}`,import.meta.url),"utf8"),context,{filename:file});
const engine=loadSceneEngine(context),data=context.window.DAWN_DATA;
const actor=(id,team,x,y,extra={})=>({id,name:id,kind:team==="hero"?"hero":"enemy",team,rulesEdition:"lionwing",space:"main",x,y,hp:30,maxHp:30,ap:0,baseAp:0,tier:2,speed:3,armor:0,evasion:0,effects:[],usedActions:[],acted:true,knockedOut:false,...extra});
const base=()=>({rulesEdition:"lionwing",version:0,round:1,turnSerial:1,activeActorId:null,tension:3,spaces:[{id:"main",width:7,height:7}],actors:[
  actor("host","enemy",5,5),actor("isolation","enemy",5,5,{profileId:"lionwing.modifier.isolation",hp:0,maxHp:0,hidden:true,modifierState:{}}),
  actor("anchor","hero",1,1),actor("near","hero",2,1),actor("far","hero",6,0),actor("near2","hero",1,2),
],objects:[],walls:[],markers:[],log:[],rollFeed:[],targetIds:[],targetCells:[],triggerQueue:[],lionwing:{started:true,lastTeam:"enemy",lastActorId:"host"}});
const commit=(scene,plan,prefix)=>engine.dispatchMany(scene,plan.events.map((event,index)=>({...event,id:`${prefix}-${index}`})),{expectedVersion:scene.version}).scene;
let scene=base();
assert.equal(engine.prepareModifierConfigure(scene,{actorId:"isolation",targetId:"anchor"}).ok,false,"Isolation needs a deployed Host");
assert.equal(engine.prepareModifierConfigure(scene,{actorId:"isolation",carrierId:"host",targetId:"host"}).ok,false,"the anchor must be a player character");
let plan=engine.prepareModifierConfigure(scene,{actorId:"isolation",carrierId:"host",targetId:"anchor"});
assert.equal(plan.ok,true,plan.errors?.join(" "));
scene=commit(scene,plan,"isolation-configure");
assert.equal(scene.actors.find(item=>item.id==="isolation").modifierState.carrierId,"host");
assert.equal(engine.compoundEnemyStatus(scene,"host").active,false,"attached modifier must not turn Host into a Compound enemy");
const appContext={console,crypto:{randomUUID:()=>"normalized-id"},APP_SCHEMA:14,contentPreferences:{edition:"lionwing"},uid:()=>"normalized-id",clamp:(value,min,max)=>Math.max(min,Math.min(max,Number(value)||0)),cleanArray:value=>Array.isArray(value)?value.filter(item=>typeof item==="string"):[]};
vm.createContext(appContext);
const appSource=fs.readFileSync(new URL("../app-core.js",import.meta.url),"utf8");
vm.runInContext(appSource.slice(appSource.indexOf("function blankScene()"),appSource.indexOf("function normalizeScene(raw)")),appContext,{filename:"app-core.scene-normalizer.js"});
const persisted=vm.runInContext(`sceneCore(${JSON.stringify(scene)})`,appContext);
assert.equal(persisted.actors.find(item=>item.id==="isolation").modifierState.carrierId,"host","reload preserves the canonical attachment");
assert.equal(persisted.actors.find(item=>item.id==="isolation").modifierState.targetId,"anchor","reload preserves the canonical player anchor");
const oldCompound=structuredClone(scene);oldCompound.actors.find(item=>item.id==="host").compoundId="modifier-carrier-host";oldCompound.actors.find(item=>item.id==="isolation").compoundId="modifier-carrier-host";
const normalizedOldCompound=vm.runInContext(`sceneCore(${JSON.stringify(oldCompound)})`,appContext);
assert.equal(normalizedOldCompound.actors.find(item=>item.id==="host").compoundId,null,"reload repairs earlier Host-Modifier Compound grouping");
assert.equal(normalizedOldCompound.actors.find(item=>item.id==="isolation").compoundId,null);
assert.equal(engine.prepareModifierConfigure(scene,{actorId:"isolation",carrierId:"host",targetId:"near"}).ok,false,"the anchor cannot change before Round end");
scene=engine.dispatchMany(scene,[{id:"isolation-round-1",type:"round.end",payload:{}}]).scene;
assert.equal(scene.round,2,"LionWing round lifecycle remains authoritative");
assert.equal(scene.actors.find(item=>item.id==="near").hp,22,"Tier 2, four players and two recipients deal 8 each");
assert.equal(scene.actors.find(item=>item.id==="near2").hp,22);
assert.equal(scene.actors.find(item=>item.id==="anchor").hp,30);
assert.equal(scene.actors.find(item=>item.id==="far").hp,30);
assert.equal(scene.pendingPrompt?.kind,"modifier-refresh");
assert.equal(engine.prepareModifierConfigure(scene,{actorId:"isolation",carrierId:"host",targetId:"anchor"}).ok,false,"next round requires a different anchor");
plan=engine.prepareModifierConfigure(scene,{actorId:"isolation",carrierId:"host",targetId:"near"});
assert.equal(plan.ok,true,plan.errors?.join(" "));
scene=commit(scene,plan,"isolation-refresh");
const answer=engine.respondRulePrompt(scene,data,{choice:"confirm"});
assert.equal(answer.ok,true,answer.errors?.join(" "));
scene=commit(scene,answer,"isolation-confirm");
assert.equal(scene.pendingPrompt,null);
const stopped=structuredClone(scene);stopped.actors.find(item=>item.id==="host").knockedOut=true;
stopped.lionwing.lastTeam="enemy";stopped.actors.filter(item=>item.kind!=="crowd").forEach(item=>{item.acted=true});
const health=stopped.actors.find(item=>item.id==="anchor").hp;
const afterKo=engine.dispatchMany(stopped,[{id:"isolation-round-2",type:"round.end",payload:{}}]).scene;
assert.equal(afterKo.actors.find(item=>item.id==="anchor").hp,health,"a knocked-out Host disables Isolation");
assert.equal(afterKo.pendingPrompt,null,"a disabled Isolation does not ask for a new anchor");
const divided=base();divided.actors.find(item=>item.id==="far").x=2;divided.actors.find(item=>item.id==="far").y=2;
const dividedConfigured=commit(divided,engine.prepareModifierConfigure(divided,{actorId:"isolation",carrierId:"host",targetId:"anchor"}),"isolation-divided");
const dividedRound=engine.dispatchMany(dividedConfigured,[{id:"isolation-divided-round",type:"round.end",payload:{}}]).scene;
assert.equal(dividedRound.actors.find(item=>item.id==="far").hp,24,"16 damage divided among three recipients rounds up to 6 each");
const koPlayer=base();koPlayer.actors.find(item=>item.id==="far").knockedOut=true;
const koConfigured=commit(koPlayer,engine.prepareModifierConfigure(koPlayer,{actorId:"isolation",carrierId:"host",targetId:"anchor"}),"isolation-ko-player");
const koRound=engine.dispatchMany(koConfigured,[{id:"isolation-ko-player-round",type:"round.end",payload:{}}]).scene;
assert.equal(koRound.actors.find(item=>item.id==="near").hp,22,"KO party members still count as player characters in the damage formula");
const mixed=base();
mixed.actors.push(actor("ally-npc","hero",2,2,{profileId:"lionwing.npc.bruiser"}));
mixed.actors.push(actor("ally-token","hero",3,1,{kind:"token"}));
assert.equal(engine.prepareModifierConfigure(mixed,{actorId:"isolation",carrierId:"host",targetId:"ally-npc"}).ok,false,"allied NPC cannot anchor Isolation");
assert.equal(engine.prepareModifierConfigure(mixed,{actorId:"isolation",carrierId:"host",targetId:"ally-token"}).ok,false,"allied token cannot anchor Isolation");
const mixedConfigured=commit(mixed,engine.prepareModifierConfigure(mixed,{actorId:"isolation",carrierId:"host",targetId:"anchor"}),"isolation-mixed");
const mixedRound=engine.dispatchMany(mixedConfigured,[{id:"isolation-mixed-round",type:"round.end",payload:{}}]).scene;
assert.equal(mixedRound.actors.find(item=>item.id==="near").hp,22,"only four player characters count toward Isolation damage");
assert.equal(mixedRound.actors.find(item=>item.id==="near2").hp,22);
assert.equal(mixedRound.actors.find(item=>item.id==="ally-npc").hp,30,"allied NPC is not a recipient");
assert.equal(mixedRound.actors.find(item=>item.id==="ally-token").hp,30,"allied token is not a recipient");
const remote=base();remote.spaces.push({id:"side",width:7,height:7});
for(const player of remote.actors.filter(item=>item.team==="hero"))player.space="side";
const remoteConfigured=commit(remote,engine.prepareModifierConfigure(remote,{actorId:"isolation",carrierId:"host",targetId:"anchor"}),"isolation-remote");
const remoteRound=engine.dispatchMany(remoteConfigured,[{id:"isolation-remote-round",type:"round.end",payload:{}}]).scene;
assert.equal(remoteRound.actors.find(item=>item.id==="near").hp,22,"Host and player anchor may occupy separate spaces");
const allyCarrier=base();allyCarrier.actors.find(item=>item.id==="isolation").team="hero";
assert.equal(engine.prepareModifierConfigure(allyCarrier,{actorId:"isolation",carrierId:"anchor",targetId:"near"}).ok,false,"Isolation cannot be configured on the heroes' side");
console.log("LionWing Isolation: Host, anchor, damage division, refresh and KO lifecycle passed");
