"use strict";
function prepareCollateralRescue(scene,request={}){const collateral=actorById(scene,request.collateralId),rescuer=actorById(scene,request.rescuerId),roll=request.roll,threshold=collateral?modifierTierValue("2(+1)",collateral.tier):0,errors=[];if(collateral?.profileId!==ENEMY_MODIFIER_IDS.collateral||collateral.knockedOut)errors.push("Случайная жертва недоступна.");if(!rescuer||rescuer.team===collateral?.team||rescuer.knockedOut||rescuer.kind==="crowd"||rescuer.space!==collateral?.space||distance(rescuer,collateral)>1)errors.push("Спасатель должен быть живым смежным персонажем игрока.");if(Number(rescuer?.ap||0)<1)errors.push("Для Взаимодействия нужен 1 ОД.");if(!roll||!Array.isArray(roll.rolls)||!roll.rolls.length||roll.rolls.some(value=>!Number.isInteger(value)||value<1||value>6)||Number(roll.successes)!==roll.rolls.filter(value=>value>=4).length)errors.push("Нужен проверяемый бросок испытания.");if(errors.length)return{ok:false,errors,events:[]};const rollId=`collateral-rescue-roll-${eventId()}`;return{ok:true,errors:[],success:Number(roll.successes)>=threshold,events:[{type:"resource.spend",actorId:rescuer.id,payload:{resource:"ap",amount:1,sourceActionId:"enemy.modifier.collateral.rescue",participantIds:[rescuer.id,collateral.id]}},{id:rollId,type:"roll.public",actorId:rescuer.id,payload:{...clone(roll),target:threshold,intent:`Спасти ${collateral.name}`,targetIds:[collateral.id]}},...(Number(roll.successes)>=threshold?[{type:"actor.despawn",actorId:collateral.id,payload:{reason:`Спасён Взаимодействием ${rescuer.name}`,sourceActionId:"enemy.modifier.collateral.rescue",rescuerId:rescuer.id,rollEventId:rollId,threshold,participantIds:[rescuer.id]}}]:[])]}}
